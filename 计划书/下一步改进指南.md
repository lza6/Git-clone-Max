# 下一步改进指南（Git-clone-Max 优化·迭代总纲 · 2026-09-19）

> 本指南为**规划文档**：先头脑风暴 → 写方案 → TDD 清单 → 落地 → 验证 → 闭环。
> 主项目已完成 0-1（M1–M7，v8.0.1）：本指南面向 **2-999 阶段的持续优化迭代**，讲究**渐进式、可落地、证据驱动、不推翻重建**。
> 配套文档：`计划书/规划总览.md`（里程碑）、`计划书/验收清单.md`（逐条验收）、`计划书/workflow_status.md`（推进日志）、`计划书/参考扫描证据/`（1178 个参考项目扫描证据）。

---

## 0. 使用约定（先读）

- **先思考后编码**：任何改动前，先在规划文档/本轮分析中写"方案 + 根因 + 折衷"，再动手。禁止边改边想。
- **TDD 铁律**：每个功能先列测试清单（用例→失败→实现→转绿→回归→覆盖），测试套件要真实且尽量全覆盖；禁止"改了没验证就声称通过"。
- **强制验证**：必须实际运行 `unittest`、`coverage`、`ruff`、`mypy`、真实 git E2E、打包/冒烟，展示命令与输出作为证据。
- **不重构守则**：本阶段除非有明确验收项，否则**严禁大重构**；小逻辑/小 Bug 可顺手修，但要入测试。
- **文档权威化**：建议逐步建立 `计划书/db_structure.md`（数据库权威）与 `计划书/project_specs.md`（任务权威），改动 schema/路线时同步更新，防止上下文丢失。
- **清理纪律**：升级完成后删除旧产物/临时文件/构建缓存（build/dist 内旧版本、._*.log、测试残留），保持仓库"最新且干净"；删除前先确认路径与归属，避免误删用户数据。

---

## 1. 项目现状（0-1 已完成：M1–M7 → v8.0.1）

### 1.1 能力全景（真实交付）
- **下载/同步**：SyncEngine 统一调度（并发 1–32、去重、断点续传 progress.json、取消缓存、finished 收敛、并发自适应 G48-1）、GitService（clone/fetch/增量/缓存/弱网降级 treeless→depth1、强制 HTTP/1.1、限速、镜像、LFS、SSH key、多账号 token、克隆后钩子、mirror、慢速提示）。
- **数据**：repo_db（SQLite + busy_timeout + 迁移 user_version、健康评分、dir_size、多标签 AND/OR、元数据 JSON 导入导出、历史/统计/备份清理）、settings（加密落盘 DPAPI/XOR、host_tokens、.bak 恢复）。
- **UI/UX**：三 Tab（下载/管理/设置）、三主题+HC+强调色、i18n（zh/en 149 键 + tr()）、批次总览、托盘菜单、帮助对话框、键盘导航/焦点环、右键菜单统一、设置搜索/卡片、动效/reduced-motion。
- **工程**：CLI 批量克隆（--cli/--dir/--portable/--minimized）、单实例锁、全局异常兜底、结构化日志、redact 打码、三平台 CI（unit+coverage 84+mypy+ruff+真实 git E2E job）、健康检查 health_check、发布脚本（tag 补建/版本一致性/sha256 双产物校验）、Inno 安装器、便携版。
- **测试规模**：~90 个测试文件、全量 761 用例绿、覆盖 84%（门禁 84）、e2e_smoke 真实裸仓 PASS。

### 1.2 架构速览（157 个源文件，见附录 C）
- 入口 `gcm/__main__.py` → MainWindow；核心 `gcm/app/engine.py` + `gcm/git/service.py`；数据 `gcm/db/`；UI `gcm/ui/`；i18n `gcm/i18n/`；打包 `packaging/installer.iss`；发布 `scripts/publish_release.py`。
- 体积关注点：`main_window.py` 66KB / `service.py` 47KB / `settings_panel.py` 37KB / `repo_db.py` 31KB / `engine.py` 30KB —— 后续按需增量拆分（仅当有明确收益）。

---

## 2. 第一性原理瓶颈分析

把"GitHub 批量下载/增量更新/入库追踪"拆到本质，判断当前瓶颈：

| 本质诉求 | 现状 | 瓶颈/机会 |
|---|---|---|
| 快（批量、并行、省流量） | 并发 32、弱网降级、镜像、LFS | ① 大仓库失败重试仍偏串行；② 无"智能分片/断点续传下载"（仅 git 协议）；③ 无 CDN/代理池负载均衡 |
| 稳（不丢、不卡、可恢复） | 断点续传、锁、崩溃恢复、健康检查 | ① 长时间大批量无"看门狗续跑"（中断后需手动重跑）；② progress.json 会增长无治理（可加滚动/压缩） |
| 懂（数据可读、可分析、可汇报） | 统计中心、报表 CSV/MD/xlsx、健康分 | ① 健康分单值；② 无"多维评分卡/趋势/阈值告警"；③ 无导出到网盘/API |
| 小白友好/教学 | 帮助对话框、推荐项、置灰 tooltip | ① 无"黑匣子教学化"（小白不知原理/排错）；② 无新手任务引导（onboarding 已有但可深化） |
| 可扩展（agent 化/生态） | CLI、Webhook、技能雏形 | ① 技能未固化交付；② 无 MCP server 暴露能力；③ 无媒体/电商/办公横向能力 |

---

## 3. 优化迭代总纲（2-999 分层）

原则：**增量、小步、可回滚、证据驱动**；按"收益/成本/风险"排序；绝不为了先进而推翻。

### P0（小而稳，1–2 周，**建议先做**）
| 项 | 方案要点 | TDD/验收 |
|---|---|---|
| **P0-1 一键体检面板**（对标参考库 web-check） | 把 G48-8 网络诊断升级为"仓库/环境多维体检"：git 版本 / 远端可达 / TCP 443 / 代理 / 数据目录权限 / 磁盘空间 / 版本一致性 / health 检查；分级报告可导出 JSON/Markdown；GUI 面板 + CLI 共用内核 | 单测：每检查项分级正确、导出可读回；真实环境一次报告登记（入截图/台账） |
| **P0-2 Webhook 安全门禁与审计台账**（对标参考库 claude-ads） | 发送前幂等去重（同批次同 payload 去重）；审计落盘 `data/notify.log`（时间/URL 打码/结果）；设置页可查；失败重试 1 次；默认只读/显式授权 | mock HTTP 断言幂等/审计行/重试；磁盘日志无明文 token |
| **P0-3 CLI `--json` 机器可读输出**（对标参考库 zvec-grep） | `python -m gcm --cli --json [urls]` 输出结构化结果（每仓 status/action/path/msg + 汇总 + 退出码），保留文本进度；供 AI agent/脚本消费 | 真实 3 裸仓 E2E + JSON schema 断言 |

### P1（近期）
| 项 | 方案要点 | 验收 |
|---|---|---|
| **P1-1 多维评分卡** | repo_health 单值 → 评分卡（成功率/陈旧/冲突/大小/标签完整度/最近同步），统计中心展示明细与趋势，阈值可配 | 单测评分矩阵；UI 渲染 |
| **P1-2 黑匣子教学化** | 帮助对话框新增"原理/排错地图"页：命令背后的 git 机制、常见错误对照表、日志怎么读、瘦身建议 | 内容与真实实现对照（断言关键行）；离屏可开 |
| **P1-3 技能沉淀 v1** | 把"批量克隆/归档下载/报表导出/网络诊断"封装为仓库内 `skills/gcm-*`（SKILL.md + scripts + 验收），供 Claude Code/Codex 调用 | 技能被真实 agent 会话调用成功（记录证据） |
| **P1-4 长任务看门狗+续跑** | 中断后启动可检测未完成任务并提供一键续跑；progress.json 滚动/压缩治理 | 模拟中断恢复 E2E |

### P2（中期/扩展方向，逐个确认）
| 方向 | 思路（均以技能/插件挂载，不推翻主架构） |
|---|---|
| **agent 化/MCP** | 把克隆/更新/归档/报表/诊断暴露为 MCP server 或 agent 工具（对齐参考库 71 个 MCP 项目） |
| **媒体** | 视频脚本→成片流水线技能（参考 zack-d-films：脚本/TTS/画面/FFmpeg 组装）；gpt-image 提示词库 |
| **电商/营销** | 商品详情页生成工作台（参考 ai-product-page-generator）或 SEO 批量分析（参考 on-page-seo） |
| **办公** | PPT/文档生成（参考 2slides / webnovel-writer） |
| **性能/算法** | 智能并发调度（按仓库大小/失败率动态并发）、失败重试退避策略、大仓库增量分析（shallow→unshallow 智能切换） |
| **分发/体验** | Manual 文档站（参考 xl-converter）、现代压缩格式处理、插件系统（目录即注册，参考 AgentK） |

---

## 4. TDD 清单与执行顺序（DoD，逐节点）

每个优化项按以下**执行顺序**（先规划 → 分解 → TDD → 落地 → 验收 → 收尾）：

```
0. 读项目结构与既有测试 → 避免重复/冲突
1. 写方案 md（本指南落点）→ 拆任务节点（每个节点：输入/输出/验收）
2. TDD：先写失败测试（单元 → 集成 → E2E）
3. 实现最小可运行代码（禁止 TODO/省略号/散文式延迟）
4. 转绿：运行单个测试 → 相关回归 → 全量
5. 质量闸：coverage ≥ 84（门禁）、ruff 0、mypy 0、真实 git E2E 冒烟
6. 手动体验：运行 scripts/启动Git-clone-Max.bat（真实启动、点按钮、看反馈）；离屏截图登记
7. 收尾：清理临时文件/旧产物；更新 验收清单.md + workflow_status.md
8. 二次审查：自查 diff + 相邻行为；高风险项交独立 Critic 复审
```

**强制验证命令（每节点至少展示其一）**：
- 全量：`python -m coverage run --source=gcm -m unittest discover -s tests && python -m coverage report --fail-under=84`
- 质量：`python -m ruff check gcm && python -m mypy gcm`
- E2E：`python scripts/e2e_smoke.py`；打包冒烟：`python scripts/health_check.py --json dist/health_check.json`

---

## 5. QA 与真实体验路径（高级 QA 视角）

1. **跑启动器实测**：`scripts/启动Git-clone-Max.bat`（真实启动、遍历三 Tab、点关键按钮：开始下载/暂停/取消/清空/导出报表/归档下载/网络诊断/Webhook/语言切换/主题切换/托盘），记录卡顿/反馈缺失/报错。
2. **真实 git E2E**：本地裸仓 x3 批量克隆→增量更新→冲突保留→归档 zip 含文件→报表读回（复用 tests 现成工具，脚本 `scripts/e2e_smoke.py`）。
3. **打包链路**：PyInstaller 构建 → `health_check.py`（版本一致 + 离屏启动 + repos.db 生成）→ Inno 安装器编译 → 便携 zip 解压冒烟。
4. **三平台 CI**：Windows/Linux/macOS 矩阵 + E2E job 全绿（push 后核对 Actions runs）。
5. **小白路径**：首次启动向导→收件箱式引导→帮助对话框"原理/排错"→报错信息可读（含 redact 与可操作建议）。

---

## 6. 清理与维护（屎山治理）

- **升级后必清**：`build/`、`dist/` 内旧版本产物、`*.log`（regress_*/publish_*/build_*）、测试残留（如 z.png）、临时诊断脚本（run_*/fix_*）、`_*.json/_*.md` 扫描临时文件。
- **保留**：`计划书/参考扫描证据/`（扫描证据）、`docs/`、`dist/` 最新产物、`requirements*.txt` 锁定版本。
- **删除纪律**：只删"可再生产物/明确归属我的临时文件"；`data/`（用户数据）绝不删；删除前列出目标路径+理由，重大删除先经你确认。
- **文档权威化**：`db_structure.md`（表结构/索引/约束权威）与 `project_specs.md`（目标/任务/进度权威）每次变更同步，避免上下文丢失与漂移。

---

## 7. 运维与上下文持久化（自主续接协议）

- **预写式日志**：关键决策/数据变更写入 `planning/` 或 `docs/`（如本次 workflow_status.md），跨会话可续。
- **工作缓冲区**：长任务进度写 `计划书/workflow_status.md`（节点表），中断后可续跑。
- **自我修复协议**：每轮结束自查（测试/覆盖/ruff/mypy/E2E/产物健康）；发现漂移即回滚或修正。
- **及时校验**：任何"已实现"都以命令输出为准；禁止把"我以为有效"当成"已验证"。

---

## 8. 安全红线与决策门禁

1. **付费/外部操作**：默认只读；显式开启 + 显式填 URL/token 才发；审计落盘；不越权（呼应参考库 claude-ads 门禁思想）。
2. **凭据安全**：token/host_tokens/webhook token 加密落盘；日志/报错/Webhook payload 全链路 redact；严禁明文入库/入日志。
3. **数据保护**：不覆盖用户 data/、不强制删除用户数据；git 操作不 -f 推送、不重置、不清理脏工作区。
4. **代码门禁**：coverage ≥84（底盘 ≥82）、ruff 0、mypy 0、E2E 绿、实际产物健康检查通过，方可标记"完成"。
5. **范围门禁**：重大重构/横向扩展（P2 方向）需你确认后再动；本阶段严格执行"先文档→后 TDD→再落地"。

---

## 9. 近期执行建议（等你确认优先级）

1. P0-1 一键体检 → P0-2 Webhook 审计 → P0-3 CLI --json（我建议此顺序，1–2 周可闭环）。
2. 之后 P1-2 黑匣子教学化（小白友好）与 P1-3 技能沉淀（agent 化）最能体现你的产品愿景。
3. P2 各方向（媒体/电商/办公/MCP）按你的业务优先级逐项展开。

---

## 附录 A：里程碑任务清单（已交付核对，状态已回填）

> 以下为规划文档原里程碑清单，状态已按实际交付回填为 `[✅ 已落地]`（G45/G46/G47/G48/G49 全部交付并发版，详见 `验收清单.md`）。新增/未决项并入正文 §3 分层方案。

# Git-clone-Max 《下一步改进指南》（v8 迭代基线）

> **目标版本：v8.x · 旗舰版 Plus（本指南为 v7.3.1 → v8.0 迭代基线，可继续顺延 v8.x+）**
> **基线版本：v7.3.1（2026-09-18 M0 实测：590 测试全绿 / 覆盖 83% / G38 六回调已实装并发布）**
> **适用实施方：任意 AI 编码代理（Builder）**
> **配合文档：`计划书/规划总览.md`（目录/里程碑/验收规约）· `计划书/验收清单.md`（证据登记）**
>
> 本指南以 **2026-09-18 实测基线** 重新起算。已落地内容（v6.7 继承 + v7.0–v7.3 的 G33–G38）在 G32 统一登记，**禁止重复实施**；本轮实测新发现归入 **G50**，未落地与新增能力沿用 **G43 起** 编号。每条均给出：**问题现状（附代码依据）→ 改进方案 → 落地文件 → 验收标准 → 优先级**。
>
> 优先级标记：**P0** 必须（不发版缺陷） · **P1** 高优先（显著体验/能力） · **P2** 中（增强） · **P3** 可选。
> 状态标记：`[待办]` → 实施方完成后改为 `[✅ 已落地] + 一条证据`。

---

## 目录

- [G31 现状基线（2026-09-18 实证）与版本规划](#g31-现状基线2026-09-18-实证与版本规划)
- [G32 已落地登记（v6.7 继承 + G33–G38，禁止重复实施）](#g32-已落地登记v67-继承--g33g38禁止重复实施)
- [G43 性能与资源治理（长时挂机/大批量软肋）](#g43-性能与资源治理长时挂机大批量软肋)
- [G44 安全纵深加固](#g44-安全纵深加固)
- [G45 工程与发布体系（含 main_window 二次拆分）](#g45-工程与发布体系含-main_window-二次拆分)
- [G46 UI/UX 设计系统现代化（v8 体验主轴）](#g46-uiux-设计系统现代化v8-体验主轴)
- [G47 数据与洞察增强](#g47-数据与洞察增强)
- [G48 Git/网络能力深化](#g48-git网络能力深化)
- [G49 长期演进与生态](#g49-长期演进与生态)
- [G50 本轮实测新发现（2026-09-18 登记，M0 优先处置）](#g50-本轮实测新发现2026-09-18-登记m0-优先处置)
- [附录A 里程碑路线与门禁](#附录a-里程碑路线与门禁)
- [附录B TDD 执行规约（每条必守）](#附录b-tdd-执行规约每条必守)
- [附录C 关键代码定位表（2026-09-18 实测）](#附录c-关键代码定位表2026-09-18-实测)
- [附录D 当前已知风险与待验证项](#附录d-当前已知风险与待验证项)

---

## G31 现状基线（2026-09-18 实证）与版本规划

### 31.1 实证基线（2026-09-18 登记）

| 维度 | 实测现状 | 证据来源 |
|------|----------|----------|
| 版本 | `7.3.0`（`__changelog__` 覆盖 4.0.0 ~ 7.3.0 共 30 个版本）；**工作区含 G38 未提交改动（6 源文件 + 3 新测试文件）** | `gcm/__init__.py:2` / `git status` |
| 运行时 | 本机 Python 3.14.3 + PyQt6 6.11.0；CI Python 3.12 | `.venv` / `ci.yml` |
| 测试 | **实测 561 个测试全绿（coverage run 880.133s）；含 G38 专项 39 用例** | 本机实测 2026-09-18：`coverage run --source=gcm -m unittest discover -s tests` → `Ran 561 tests … OK` |
| 覆盖率 | **TOTAL 82%**（CI 门禁 `--fail-under=82` 持平，临界）；低点：single_instance 59 / statistics_dialog 63 / main_window 73 / manage_model 74 | `coverage report` 实测 2026-09-18 |
| 代码规模 | 5032 stmts 核心代码；**`main_window.py` 1948 行（G38 回调又 +58，仍为最大文件）** | 实测 2026-09-18 |
| 静态检查 | `ruff check gcm` 零 error；mypy 通过 | 本机实测 |
| 打包 | onefile exe 93.9MB + onedir portable.zip 184.3MB 双产物；v7.3.0 发布闭环已登记 | `dist/` / 发布闭环（2026-09-16） |
| 引擎 | SyncEngine（QThreadPool ≤32）去重/进度落盘/取消缓存/drain(8s)；`engine.py` 458 行 | `gcm/app/engine.py` |
| Git 服务 | `service.py` 935 行：clone/fetch/merge/treeless 降级/按 host 凭据注入（G38-1）/镜像前缀（G38-2）/远端预检（G38-3）/单分支浅克隆（G38-4）/depth1 保命（G38-5）/强制 IPv4（G38-6）/自定义主机（G38-7） | `gcm/git/service.py` |
| 数据库 | SQLite WAL + BEGIN IMMEDIATE；repos + sync_history + 迁移链（note 列 v2）；backup_to 一致性备份 | `gcm/db/repo_db.py`（519 行） |
| 设置 | 加密落盘 token + host_tokens 逐条目加密 + mirror_prefix/precheck/single_branch/force_ipv4/custom_hosts/quick_repos/geometry | `gcm/db/settings.py`（275 行） |
| 安全 | token DPAPI/Keyring/XOR 加密 · host 白名单 · redact 全链路 · single-instance · 内嵌凭据拒绝 | `gcm/db/settings.py` `gcm/util/redact.py` |
| 日志 | 结构化 app.log（RotatingFileHandler 5MB×3）+ error.log 兜底 + redact | `gcm/app/applog.py` |
| 测试隔离 | **⚠️ `tests/test_updater_retry.py::test_silent_check_failure_logged` 单独跑崩溃（exit -1073740791 / 0xC0000409），原因=未先行创建 QApplication 就构造 MainWindow；全量 discover 偶发同崩（顺序依赖侥幸通过）** | 本机实测 2026-09-18（HEAD 6d33f0a 同样复现，非 G38 回归） |
| 工作区 | **G38 逻辑层 + UI 层 + 专项测试已全部就绪，未提交未发版**；另有 3 个未跟踪测试文件 | `git status` 2026-09-18 |

### 31.2 v7.3.0 已具备的能力矩阵（供新条目定位，避免重复建设）

| 域 | 已有能力 | 尚未覆盖 |
|----|----------|----------|
| 多平台 | GitHub/GitLab/Gitee/Codeberg/Bitbucket/自建主机 + 短格式/子组/@tag/镜像前缀/自定义主机白名单 | 多账号同 host / SSH 指定 key |
| 克隆模式 | 满量 / 浅克隆(depth) / 单分支浅克隆 / treeless 降级 / depth1 保命 / 子模块 / 指定分支/标签 | 镜像克隆 --mirror / 归档下载 zip / LFS 开关 |
| 网络与认证 | 按 host 凭据注入（Bearer/PRIVATE-TOKEN/白名单）、host_tokens 逐条目加密、远端可达性预检、强制 HTTP/1.1、代理自动检测、下载限速 | 并发自适应 / 网络诊断 / 弱网预提示 / token 编辑器掩码 |
| 批量 | 去重/暂停单任务/取消缓存/进度周期落盘/任务清单持久化/批量打标签/导出所选 CSV | 批次流控（>64 惰性补投）/ 日志 250ms 合并 / 紧凑日志 / 批次总览条 |
| 数据 | 历史/统计中心/趋势图/用量/报表 CSV+MD/备注/远端检查 | 健康度评分 / 历史时间线 / xlsx / hover / 磁盘占用 / 多标签 AND-OR / 元数据导入导出 |
| UI/UX | 三主题/首启向导/状态符号/标准图标/动效/热键/深浅色跟随/设置搜索/窗口记忆 | 设计 token / 状态反馈 QSS / 焦点环 / 高对比主题 / 帮助对话框 / 批次总览 |
| 工程 | ruff/mypy/coverage 门禁/发布清单/双产物 sha256/单测 561 | main_window 二次拆分（>1300）/ CI 三平台矩阵 / 并发压力回归 / spec 入库 / 测试隔离修复 |

### 31.3 版本规划（v7.3.1 → v8.0）

| 版本 | 里程碑 | 聚焦 | 主线条目 |
|------|--------|------|----------|
| v7.3.1 | M0 | G38 收口发布 + 测试隔离修复 + 工程收尾 | G50 全部（其中 G50-1 测试隔离为 P0 前置） |
| v7.4.0 | M1 | 性能与资源治理 | G43 主条目 |
| v7.5.0 | M2 | 安全纵深 | G44 主条目 |
| v7.6.0 | M3 | 工程与结构（main_window 二次拆分 + 覆盖 85） | G45 主条目 |
| v7.7.0 | M4 | UI/UX 设计系统现代化 | G46 主条目 |
| v7.8.0 | M5 | 数据洞察 + Git 能力深化 | G47 + G48 主条目 |
| v7.9.0 | M6 | 长期演进 + 生态 | G49 主条目 |
| v8.0.0 | M7 | 收官验收 | 全模块回归 + Release 双产物 |

---

## G32 已落地登记（v6.7 继承 + G33–G38，禁止重复实施）

> 以下条目已有测试/文件证据。**实施方不得重复实现**，在验收清单登记"继承已落地"即可。

### 32.1 v6.7.0 继承（工程基线，G21/G22/G28/G29）

| 条目 | 落地证据 | 对应测试 |
|------|----------|----------|
| G21-1~4 暂停排序错行/进度落盘统一/更新检查重试/启动器退出码 | `main_window.py` `engine.py` `updater.py` `启动Git-clone-Max.bat` | `test_v7_fixes.py` `test_updater_retry.py` `test_launcher.py` |
| G22-1~7 关窗 drain 收敛/in_progress 生命周期/BEGIN IMMEDIATE/托盘 tooltip/app.log/failed 重试回填/重试文案 | `engine.py:422` `repo_db.py:110` `tray.py` `applog.py` | `test_v7_reliability.py` `test_tray_progress.py` `test_applog.py` |
| G28-1 redact 全链路打码（100% 覆盖） | `gcm/util/redact.py` | `test_redact.py` |
| G29-1/2/6/7 CI 门禁/ruff/mypy/模块摘要/发布清单 | `.github/workflows/ci.yml` `docs/RELEASE_CHECKLIST.md` | CI 配置 |

### 32.2 v7.0 已交付（G33 可靠性补全）

| 条目 | 落地证据 | 对应测试 |
|------|----------|----------|
| G33-1/2 导入灰标+仅显示未入库 / 二次导入不覆盖+覆盖开关 | `gcm/ui/local_repos_dialog.py` | `test_local_dialog.py`（24 OK） |
| G33-3 DB WAL checkpoint 一致性备份 `backup_to` | `gcm/db/repo_db.py:421` | `test_db_prune_backup.py`（8 OK） |
| G33-4 取消/超时读线程退出保障 | `gcm/git/service.py` `run_git_ui` | `test_git_thread_exit.py`（3 OK） |
| G33-5 sync_history 容量治理（每仓200+90天窗口/100次清理） | `gcm/db/repo_db.py:237` | `test_db_prune_backup.py` |
| G33-6 启动耗时自检+留痕 | `main_window.py` `_run_selftest` | `test_g33_startup.py`（3 OK） |
| G33-7/8 注释漂移修正 + main_window 拆分(settings_panel/log_buffer/download_tools) | `gcm/ui/settings_panel.py` 等 | `test_settings_scroll` 等 |

### 32.3 v7.1 已交付（G35 核心交互进阶）

| 条目 | 落地证据 | 对应测试 |
|------|----------|----------|
| G35-1 进度表右键重试/复制详情/打开目录 | `main_window.py` `_progress_table_menu` | `test_g35_ui.py`（12 OK） |
| G35-2 完成提示音开关 | `settings.finish_sound` | `test_g352_finish_sound_*` |
| G35-3 输入区拖拽 txt/目录 | `_repo_input_drop` | `test_g353_drop_*` |
| G35-4 任务清单持久化 data/lists | `gcm/db/lists.py`（97%） | `test_db_lists.py`（15 OK） |
| G35-5 详情页复制克隆命令(@tag) | `repo_detail_dialog.py` | `test_repo_detail.py`（8 OK） |
| G35-6 管理页 host 分布统计 | `manage_model.py` | `test_manage_ui.py` |
| G35-7 快捷填充可配置 quick_repos | `settings.py` | `test_g357_*` |
| G35-8 状态格 tooltip 拼接 | `main_window.py` `_on_worker_result` | `test_g358_status_tooltip` |
| G35-9 批量打标签+导出所选 CSV | `main_window.py` | `test_g359_*` |
| G35-10 GitHub Star 列表导入 | `gcm/app/gh_api.py`（100%） | `test_gh_api.py`（16 OK） |

### 32.4 v7.2 已交付（G36 UI/UX 升维）

| 条目 | 落地证据 | 对应测试 |
|------|----------|----------|
| G36-1 首启三步向导 | `gcm/ui/onboarding.py` | `test_onboarding.py`（13 OK） |
| G36-2 状态符号双通道 ✓✕⚠⊘→ | `main_window.py` status_map | `test_g362_*` |
| G36-3 QStyle.StandardPixmap 图标体系 | `gcm/ui/theme.py` `_std_icon` | `test_themes.py` |
| G36-4 窗口尺寸记忆 geometry | `settings._serialize/_restore` | `test_g364_geometry_*` |
| G36-5 空态 overlay | `main_window.py` | `test_g365_empty_overlay_*` |
| G36-6 完成动效背景淡出+开关 | `_animate_result_row` + `settings.animations` | `test_g366_*` |
| G36-7 设置页搜索过滤 | `settings_panel.py` | `test_settings_search.py`（6 OK） |
| G36-8 全局热键 Ctrl+Alt+S/U/M | `main_window.py` | `test_g368_hotkey_*` |
| G36-9 跟随系统深浅色 auto | `gcm/app/system_theme.py` | `test_follow_system_theme.py`（13 OK） |

### 32.5 v7.3 已交付（G37 数据能力纵深）

| 条目 | 落地证据 | 对应测试 |
|------|----------|----------|
| G37-1 30 天同步趋势 QPainter 柱状 | `statistics_dialog.py` `_TrendCanvas` | `test_g37_stats.py`（7 OK） |
| G37-2 详情页检查远端 ls-remote 对比 | `repo_detail_dialog.py` | `test_repo_detail.py`（14 OK） |
| G37-3 报表筛选导出（日期/host/状态） | `gcm/reports.py` `_fetch_rows` filters | `test_reports2.py`（14 OK） |
| G37-4 自动更新间隔 QTimer | `main_window.py` `_restart_auto_update_timer` | `test_g374_376.py` |
| G37-5 用量统计（克隆/更新/提交） | `repo_db.stats_usage` | `test_g37_stats.py` |
| G37-6 每仓库备注 note 迁移 0002 | `repo_db.py` `set_note` | `test_g374_376.py` |

### 32.6 G38 网络与同步前沿（✅ 2026-09-18 实测已完整实装，工作区未提交）

> **状态更新（2026-09-18）**：上一版指南登记的"半成品 100 errors"已消除并**已发布 v7.3.1**。实测：
> - `gcm/ui/main_window.py:777-812` **6 个保存回调已全部存在**（`_save_host_tokens`/`_save_mirror`/`_save_custom_hosts`/`_save_precheck`/`_save_single_branch`/`_save_force_ipv4`），与 `settings_panel.py:217-251` 的 `connect` 一一对应。
> - `gcm/db/settings.py:149-154` 六个字段已定义；host_tokens 逐条目加密已落地（:200-232）。
> - `tests/test_g381_host_tokens.py` / `test_g381_ui_callbacks.py` / `test_g38_network.py` 共 **39 用例全绿**。
> - 全量回归 **561 tests OK / TOTAL 82%**。
> **✅ 已收口**：G38 已于 2026-09-18 commit（86010f7/43eca62/55a773a）+ tag v7.3.1 + Release 双产物上传并独立下载复验 MATCH。

| 条目 | 落地证据（2026-09-18） | 状态 |
|------|----------|------|
| G38-1 按 host 凭据注入（github→Bearer / gitlab→PRIVATE-TOKEN / 未登记不发）+ host_tokens 逐条目加密落盘 | `service.py` `_token_for_host:481` `_env:422`；`settings.py:200-232` | ✅ 全链路已实装 |
| G38-2 镜像前缀 apply_mirror_prefix / _mirror_url（仅 HTTPS） | `url_lib.py` `service.py` `_mirror_url:495` | ✅ 全链路已实装 |
| G38-3 远端可达性预检 10s ls-remote（开关） | `service.py` `_precheck_reachable:510` | ✅ 全链路已实装 |
| G38-4 单分支浅克隆 --depth N --single-branch | `service.py` `_clone:676-677`（随设置开关） | ✅ 全链路已实装 |
| G38-5 降级链终档 depth1 保命模式 | `service.py` `_clone:727-745` | ✅ 已实装（无独立 UI，随弱网降级链触发） |
| G38-6 强制 HTTP/1.1 http.version=HTTP/1.1 | `service.py` `_env:467-469` | ✅ 全链路已实装 |
| G38-7 常用主机白名单 custom_hosts / is_supported_host | `url_lib.py` `_extra_hosts`；`settings.py:154` | ✅ 全链路已实装 |

> **✅ M0 验收已达成（2026-09-18）**：G38 commit + tag v7.3.1 + 双产物 sha256 独立下载复验 MATCH + README 能力矩阵同步 + spec 入库。此条目视为完成，**禁止重复实现**。
---

## G43 性能与资源治理（长时挂机/大批量软肋）

> 冷启动不是瓶颈（MainWindow 构造 ~0.1s）；真正的风险在**长时挂机**与**数百仓库大批次**。本模块全部条目要求"先测量后优化"，每条必须附 before/after 数据才算验收。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G43-1**（承接原G34-1） | P1 | `engine._emit_line`（`engine.py:142-146`）对每个 worker 每行 git 输出都发 Qt 信号（32 并发大仓克隆信号风暴）；UI 侧 `LogModel`（`theme.py:227-256`）已有 `maxlen=3000` 但为 **list 切片裁剪（O(n)）** 非 deque | ① `theme.py` LogModel 改 `deque(maxlen=3000)`（O(1) 裁剪）；② 引擎侧 git 输出行加 **250ms 合并窗口**（按 index 聚合并一次 emit 多行）；③ 批量 >100 时 UI 日志区自动切"紧凑模式"（仅状态变化行） | `gcm/ui/theme.py`（LogModel）`gcm/app/engine.py`（`_emit_line`）`main_window.py` | 32 并发大仓 UI 不卡（人工体验+计时）；`cProfile` 信号处理耗时占比下降有数据 |
| `[✅ 已落地]` **G43-2**（承接原G34-2） | P1 | `engine.launch`（`engine.py:275`）一次全量入队；单批次 500 仓库时 QThreadPool 队列同时持有 500 个 payload（specs/flags 在任务全结束才清） | **批次流控**：去重后 >64 只入队前 64，完成一个由 `_on_worker_done`（`engine.py:398`）从待发队列**惰性补投**；进度表仍显示全部行（PENDING 态） | `gcm/app/engine.py`（`launch`/`_on_worker_done`/`_release_tasks:409`） | E2E：600 仓库批次峰值内存较改造前下降（tracemalloc 采样）；全部完成且结果齐全 |
| `[✅ 已落地]` **G43-3**（承接原G34-3） | P2 | `sync_history.duration_ms` 已存但无利用维度 | 统计中心加「平均耗时 Top10 慢仓库」列表（帮助定位网络/大仓问题） | `gcm/ui/statistics_dialog.py` `repo_db.py`（新增 `stats_slow_repos`） | Top10 排序正确；空库占位文案 |
| `[✅ 已落地]` **G43-4**（承接原G34-4） | P3 | onefile exe 93.9MB（2026-09-18 实测）每次启动解包；portable 184.3MB 偏大 | 发布瘦身：PyInstaller `excludes` 排除多余 PyQt 翻译/插件/Qt 模块；README 登记双产物启动耗时对比 | `Git-clone-Max.spec` `scripts/publish_release.py` | zip 体积下降有数据；portable 启动 < 2s（实测登记） |
| `[✅ 已落地]` **G43-5**（新） | P2 | `_on_worker_progress_detail`（`main_window.py:1514`）已用 `_flush_detail_batch:1521` 批聚合，但进度信号仍逐条跨线程 | 统一进度信号批通道：engine 侧 `progress`/`progress_detail` 聚合为 100ms 心跳批量 emit（Qt 事件循环友好），UI 侧单次刷新 | `gcm/app/engine.py` `gcm/ui/main_window.py` | 30 并发进度刷新 UI 事件数下降 ≥50%（QTimer 计时） |
| `[✅ 已落地]` **G43-6**（新） | P3 | `main_window.py` 顶部 import 全量加载统计/报表/托盘模块，首窗 show 前有可优化空间 | 惰性 import：`show_statistics:634`/`export_report:639`/托盘初始化延迟到首次使用（`importlib` 或函数内 import） | `gcm/ui/main_window.py` | 启动→show 耗时下降有数据（复用 G33-6 自检通道） |
| `[✅ 已落地]` **G43-7**（新） | P3 | SQLite WAL 长期挂机 journal 增长；无自动 checkpoint 策略 | 引擎空闲（任务全结束）时 `PRAGMA wal_checkpoint(PASSIVE)` + `wal_autocheckpoint` 调优；设置页「数据库维护」按钮（含 VACUUM 需确认） | `gcm/db/repo_db.py` `main_window.py` | 长时运行后 WAL 文件回落（文件体积 before/after） |
| `[✅ 已落地]` **G43-8**（新） | P3 | 大批量 URL 粘贴解析 `parse_urls` 逐行正则 | 解析走编译好的正则缓存 + 无效行批量收集一次弹窗（已有分拣逻辑，补性能压测与并行化评估） | `gcm/app/url_lib.py` | 5000 行粘贴解析耗时 < 1s（实测）；无效行统计正确 |

---

## G44 安全纵深加固

> 既有安全资产必须保持回归：url_lib 层 `@` 凭据前置拒绝、`..` 段拒绝、ref 合法性校验、host 白名单（含 G38-7 custom_hosts）、token host 白名单注入、host_tokens 逐条目加密、DPAPI/Keyring/XOR 加密落盘、single-instance、`_safe_rmtree_partial` 不误删、redact 全链路。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G44-1**（承接原G39-1） | P1 | host_tokens（G38-1）已逐条目加密，但设置页 `edit_host_tokens`（`settings_panel.py:213`）为**明文可见多行框**；token 框 `edit_token` 未确认 echoMode | 设置页 host_tokens 编辑器改 **逐行密码框**（QLineEdit + echoMode=Password + "显示"眼睛切换）；`edit_token` 同样 Password + 眼睛；导出报表/统计**不得含任何 token 字段**（含 host_tokens） | `gcm/ui/settings_panel.py` `gcm/reports.py` | settings.json 中新字段为 `enc:v1:` 前缀密文；UI 单测：echoMode=Password；导出 CSV 无 token/host_tokens 列 |
| `[✅ 已落地]` **G44-2**（承接原G39-2） | P2 | `choose_target`（`main_window.py:1090`）无敏感目录校验 | 下载目录选择时若在 `%SystemRoot%`/`Program Files` 下 → 警告确认；`mkdir(parents=True)` 失败分类提示（权限/占用/盘符不存在） | `main_window.py` | 单测：选 C:\Windows\Temp 给警告文案；权限失败给出明确分类提示 |
| `[✅ 已落地]` **G44-3**（承接原G39-3） | P2 | 更新检查走 GitHub API 明文信任 latest tag；`publish_release.py` 已在 Release body 附 sha256 | `updater.check_latest` 已返回 body；更新提示 Dialog 展示"发布方公布 sha256"，用户手动核对（桌面轻量方案） | `gcm/app/updater.py` `main_window.py`（更新弹窗） | 弹窗含 sha256 行（mock Release body 断言） |
| `[✅ 已落地]` **G44-4**（承接原G39-4） | P3 | 日志含完整本地路径（用户名泄露面） | `redact()` 增加可选 `USERPROFILE` 打码（设置开关，默认关） | `gcm/util/redact.py` | 单测：开关开启后路径中用户名打码；关闭原样 |
| `[✅ 已落地]` **G44-5**（新） | P2 | 用户可能把含 token 的 URL 直接粘贴（`https://<token>@github.com/...` 或 `?access_token=`）——url_lib 已拒绝 `@` 前置但**无用户提示** | 解析层检测到 URL 内嵌凭据 → 输入区红框提示 + 弹窗"已剥离凭据再解析"（剥离后正常入队，不丢失任务） | `gcm/app/url_lib.py` `main_window.py` | 单测：粘贴带凭据 URL → 提示出现且入队 URL 无凭据；`redact` 日志不打码失败 |
| `[✅ 已落地]` **G44-6**（新） | P2 | 设置页/管理页对 settings 未做"仅本机可读"的 ACL 提示；data 目录默认 exe 同级可被同机他人读 | 数据目录权限自检：启动时提示"数据目录含加密凭据，建议置于用户目录"；提供"移动到 %APPDATA%"一键迁移（G49-2 前置） | `gcm/db/settings.py` `main_window.py` | 单测：目录非用户域时提示出现；迁移后旧路径数据不丢 |

---

## G45 工程与发布体系（含 main_window 二次拆分）

> 结构债优先级最高（P0 定义见 G45-6）；**任何 UI 改动必须评估 main_window 行数影响**，新逻辑优先独立模块。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G45-1**（承接原G40-1） | P0 | **2026-09-18 实测 TOTAL 82%**，与 CI 门禁 `--fail-under=82` **持平（临界，任何 omit 调整/重构都可能跌破）** | ① 保持门禁 ≥82%；② 本轮优先补覆盖低点（single_instance 59 / statistics_dialog 63 / main_window 73 / manage_model 74）；③ 达标后门禁提 **85%**，本地复现命令写 README | `.github/workflows/ci.yml` `tests/*` | CI 绿且门禁 ≥ 目标值；本地与 CI 覆盖率数字对齐有说明 |
| `[✅ 已落地]` **G45-2**（承接原G40-2） | P1 | 并发正确性只靠 E2E 单次跑；竞态回归靠人眼 | `tests/test_engine.py` 补参数化压力（unittest subTest）：随机 2-32 并发 × 3-20 仓库 × 随机 20% 失败注入，断言结果集合一致、无死锁（timeout 120s） | `tests/test_engine.py` | 随机种子固定可复现；CI 3 轮全绿 |
| `[✅ 已落地]` **G45-3**（承接原G40-3） | P1 | `__main__` 42 stmts 覆盖 42% / `single_instance` 121 stmts 覆盖 59% 两块最低（2026-09-18 实测） | `tests/test_main.py`：`get_data_dir` 三分支（env/frozen/源码）+ `main()` 冒烟（mock QApplication/SingleInstanceLock/MainWindow）；`single_instance` 补锁文件损坏/PID 复用分支 | 新 `tests/test_main.py` `tests/test_single_instance.py` | 两文件覆盖 ≥80%；全量绿；**覆盖回 82%+ 是 G45-1 的依赖** |
| `[✅ 已落地]` **G45-4**（承接原G40-4） | P2 | 发布脚本 body 模板停在旧版本描述 | `publish_release.py` Release body 模板读 `docs/RELEASE_BODY.md`（版本号占位符）；body 附 sha256（已落地） | `scripts/publish_release.py` 新 `docs/RELEASE_BODY.md` | dry-run 输出 body 预览；实发一次验证 |
| `[✅ 已落地]` **G45-5**（承接原G40-5） | P3 | `requirements-build.txt` 未锁版本（全 `>=`） | 锁定 PyInstaller/ruff/mypy/coverage/PyGithub 精确版本 | `requirements-build.txt` | `pip install -r` 可复现构建 |
| `[✅ 已落地]` **G45-6**（新·**P0 结构债**） | P1 | **`main_window.py` 1948 行**（2026-09-18 实测，G33-8 拆分后 v7.1–v7.3 又堆回 + G38 回调） | **只提取不重写**（同 G33-8 模式，行为零变化）：① 管理页构建 `_build_manage_tab`（~90 行）+ 管理页动作（`_load_db_into_grid`/`_refresh_manage`/`delete_selected`/`batch_tag_selected`/`export_selected_csv`/`_on_manage_double_clicked`）→ 新 `gcm/ui/manage_panel.py`（QWidget + 信号回传）；② 进度表逻辑（`_prepare_table`/`_add_table_row`/`_filter_progress_rows`/`_on_sort_changed`/`_progress_table_menu`/`_retry_table_row`/`_copy_row_detail`/`_open_row_dir`/`_on_worker_progress*`/`_on_worker_result`/`_animate_result_row`/`_update_empty_download`）→ 新 `gcm/ui/progress_table.py`（QWidget）；③ 主类仅保留编排与信号转发 | `main_window.py` → 新 `manage_panel.py`/`progress_table.py` | 全量回归绿；`main_window.py` ≤ 1300 行；覆盖率不降；控件名/信号兼容引用全保留 |
| `[✅ 已落地]` **G45-7**（新） | P2 | CI 仅 windows-latest + Python 3.12 单矩阵 | 补 **Linux + macOS** 矩阵（offscreen 跑全量；Linux 补 fcntl 分支）；新增"真实 git E2E 冒烟 job"（本地裸仓 clone/update/conflict） | `.github/workflows/ci.yml` | 三平台矩阵全绿；E2E job 独立红绿灯 |
| `[✅ 已落地]` **G45-8**（新） | P3 | `__changelog__` 与 tag 无自动一致性校验 | 发布脚本 dry-run 增加校验：`__version__` == tag；`__changelog__` 首行含该版本号；缺失则失败 | `scripts/publish_release.py` | dry-run 在版本不一致时报错 |
| `[✅ 已落地]` **G45-9**（新） | P3 | 打包产物健康检查靠人工 | 新 `scripts/health_check.py`：解包 exe → 版本资源断言 → 离屏启动冒烟（进程驻留+repos.db 创建）→ 输出 JSON 台账供发布登记 | 新 `scripts/health_check.py` | 脚本对当前产物通过；输出含 sha256+版本号 |

---

## G46 UI/UX 设计系统现代化（v8 体验主轴）

> 原则：**渐进增强，不做大重构**。G36 已建立首启向导/符号/图标/动效/主题/热键等基础；本模块把它们**统一为设计系统**，并补小白可达性与专业效率细节。每条须离屏可测（QTest）+ 至少一次真实窗口人工体验登记。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G46-1** | P1 | 三套主题色板（`theme.py` PALETTES）直接写 QSS 字符串，无设计 token 层；控件样式（圆角/间距/边框）散落 | 提炼 **设计 token 表**：颜色/圆角/间距/边框/阴影 → 一份 `theme.py` 内 token 字典 + `qss_for_theme()` 统一生成（三主题共用布局 token，仅换色板）；新增 light 与 deep 的 hover/disabled/pressed 状态色 | `gcm/ui/theme.py` | 三主题各状态色有定义；切换主题后圆角/间距一致；截图对比登记 |
| `[✅ 已落地]` **G46-2** | P2 | 主窗口为三 Tab（下载/管理/设置）QTabWidget，工具栏按钮较朴素 | 渐进式导航升级：① 统一工具栏（开始/取消/一键更新/统计/报表/打开目录）图标+文字+tooltip；② Tab 页签样式现代化（图标+文字）；③ 下载 Tab 输入区与操作按钮组加视觉分组容器 | `gcm/ui/main_window.py` `gcm/ui/theme.py` | 截图标定；三 Tab 图标化；无功能回归 |
| `[✅ 已落地]` **G46-3** | P1 | 按钮状态反馈不完整（hover/disabled 无统一 QSS）；`_reset_buttons`（`main_window.py:1687`）只切 enable | QSS 补齐 QPushButton/QSpinBox/QLineEdit/QComboBox 的 hover/focus/disabled/pressed 状态；按钮禁用态加说明 tooltip（"任务进行中不可用"） | `gcm/ui/theme.py` `main_window.py` | QTest：禁用按钮 click 不触发；截图 hover/disabled 可见 |
| `[✅ 已落地]` **G46-4** | P2 | 进度展示是表格行内进度条 + 状态符号；无批量总览 | 下载 Tab 顶部加**批次总览条**：已完成 x/总 y · 成功率 % · 平均速率（纯文本+迷你进度条），由 `_on_worker_result`（`main_window.py:1534`）累加更新 | `gcm/ui/main_window.py` | 离屏单测：结果到达后总览数字正确；真实 5 仓批次体验登记 |
| `[✅ 已落地]` **G46-5** | P2 | 中文字体未显式指定（回退系统默认）；字号缩放 0.8–1.6 已存在但无最小可读性保护 | QSS 字体栈：`"Microsoft YaHei UI","PingFang SC","Noto Sans CJK SC",sans-serif`；字号缩放下限钳制 0.9；设置页加"恢复默认字号"按钮 | `gcm/ui/theme.py` `settings_panel.py` | 字体栈在 Win10 生效；缩放至 0.8 时字号仍 ≥ 可读值 |
| `[✅ 已落地]` **G46-6**（承接G36-2 深化） | P2 | 符号双通道已落地；但键盘焦点环（focus outline）与屏幕阅读器可访问名未系统化 | ① 全局 QSS `:focus` 高亮环；② 关键控件 `setAccessibleName`/`setToolTip` 补齐；③ 表格键盘导航（↑↓ 选中行 + Enter 打开详情） | `gcm/ui/theme.py` `main_window.py` | Tab 遍历焦点可见；QTest 键盘选行+Enter 触发详情 |
| `[✅ 已落地]` **G46-7** | P3 | 主题已有 deep/light/nord + auto；无高对比/自定义强调色 | 新增 **HC（高对比）主题**（WCAG AAA 用色）+ 设置页强调色选择（3 档预设，写入 token 表） | `gcm/ui/theme.py` `settings_panel.py` | HC 对比度 ≥ 7:1（计算断言）；强调色切换即时生效 |
| `[✅ 已落地]` **G46-8** | P3 | 动效只有完成行淡出；无 reduced-motion 全局尊重 | 新增 `prefers_reduced_motion` 设置（默认关）：开时禁用所有 QVariantAnimation 与 hover 过渡（复用 `settings.animations` 上游，拆分"完成动效/界面过渡"两个开关或合并语义） | `main_window.py` `theme.py` | 开关开后无动画实例（离屏断言） |
| `[✅ 已落地]` **G46-9** | P2 | 托盘仅有显示/退出（`tray.py` 122 行）；无统计/更新入口 | 托盘菜单丰富：显示主窗 / 打开统计中心 / 检查更新 / 开机自启（勾选）/ 退出；双击托盘图标显示窗口 | `gcm/ui/tray.py` | 菜单动作离屏可 mock 断言；双击 handler 单测 |
| `[✅ 已落地]` **G46-10** | P2 | 设置页是分组滚动+搜索（G36-7 已落地）；分组卡片视觉较平 | 设置分组改**卡片式**（QGroupBox 样式：圆角/边框/分组标题）；"恢复默认"按钮（除 token 外全部回默认值） | `settings_panel.py` `theme.py` | 截图对比；恢复默认后 settings 回默认（token 保留） |
| `[✅ 已落地]` **G46-11** | P2 | 无内嵌帮助；小白不知 `@tag`/清单/快捷键/镜像语法 | 新"使用说明"对话框（`gcm/ui/help_dialog.py`）：语法速查（@tag / host=token / 镜像前缀 / 快捷键 / 拖拽 / 清单分享 / Star 导入）；入口：设置页按钮 + 菜单 | 新 `gcm/ui/help_dialog.py` `settings_panel.py` | 帮助内容与真实语法一致（对照实现断言关键行）；离屏可开 |
| `[✅ 已落地]` **G46-12** | P3 | 右键菜单语义分散（进度表有、管理表有、输入区有） | 统一右键菜单模式：禁用项置灰+说明 tooltip、菜单项图标化、ESC 关闭后焦点还原 | `main_window.py` `progress_table.py`（G45-6 后） | 三处菜单项一致性检查（离屏断言项集合） |

---

## G47 数据与洞察增强

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G47-1** | P1 | 管理页有过期高亮（is_stale）+ 收藏/标签/黑名单；无综合健康度 | 新增 **健康度评分**（0–5）：成功率（近 20 次）/陈旧天数/冲突计数加权 → 管理表"健康"列（⭐ 0–5 + 颜色）；统计中心加分布 | `gcm/db/repo_db.py`（`health_score`）`gcm/ui/manage_model.py` | 单测：构造历史 → 评分符合权重；空历史默认 3 分 |
| `[✅ 已落地]` **G47-2** | P1 | 详情页有统计数字但无历史时间线 | 详情页新增「同步历史」时间线列表（最近 20 次：时间/状态/动作/耗时/消息），复用 `db.history`（`repo_db.py:291`） | `gcm/ui/repo_detail_dialog.py` | 单测：history 数据 → 时间线渲染行数正确；空态占位 |
| `[✅ 已落地]` **G47-3** | P2 | 报表导出 CSV/MD；无 xlsx（Excel 原生） | `reports.py` 新增 xlsx 导出（`openpyxl` 可选依赖，缺失时提示装或用 CSV）；带自动列宽+冻结首行 | `gcm/reports.py` `requirements.txt` | 导出 xlsx 可被 openpyxl 读回且行数一致；无 openpyxl 时优雅提示 |
| `[✅ 已落地]` **G47-4** | P2 | 30 天趋势图静态柱状（`_TrendCanvas`，`statistics_dialog.py:29`）；无 hover/缩放 | ① hover 显示当日成功/失败/克隆数（QMouseMove + tooltip）；② 周期切换 7/30/90 天下拉；③ 支持导出 PNG | `gcm/ui/statistics_dialog.py` | 单测：hover 事件返回正确日数据；切换周期重算 |
| `[✅ 已落地]` **G47-5** | P2 | 管理页无磁盘占用；用户不知哪些仓库最占空间 | 管理表加「大小」列：懒加载 `du`（后台线程，QTimer 节流）→ 排序可用；显示 MB/GB 人类可读 | `gcm/ui/manage_model.py` `gcm/db/repo_db.py` | 单测：目录大小计算正确；排序正确；超大目录不卡 UI |
| `[✅ 已落地]` **G47-6** | P2 | 标签单值过滤（list_by_tag 竖线分隔）；无多标签 AND/OR | 管理页标签过滤升级：多选下拉（AND/OR 开关）→ `list_repos(tags=...)` 支持组合 | `gcm/db/repo_db.py` `manage_model.py` | 单测：AND/OR 组合命中正确 |
| `[✅ 已落地]` **G47-7** | P3 | 元数据无导入导出（重装/换机丢失标签/收藏/备注） | 新增「导出仓库元数据 JSON」（含 tags/favorite/note/excluded + repo 标识）与「导入合并」（按 owner/repo/host 匹配 upsert） | `gcm/db/repo_db.py` `gcm/ui/manage_panel.py` | 导出→清库→导入 → 元数据完整恢复 E2E |

---

## G48 Git/网络能力深化

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G48-1** | P1 | 并发数固定 1–32（设置）；网络抖动时无自适应 | **并发自适应**：连续 N 个网络类失败 → 自动降并发（/2，下限 1）并提示；恢复成功 K 次 → 回升（上限设置值）；状态栏显示当前并发 | `gcm/app/engine.py` `main_window.py` | 单测：注入失败序列 → 并发下降且提示；恢复序列回升 |
| `[✅ 已落地]` **G48-2** | P2 | 无 Git LFS 开关；treeless 克隆大 LFS 仓库时 blob:none 不含 LFS | 设置「Git LFS」开关 → clone/fetch 追加 `-c filter.lfs.required=false` + 提示 LFS 文件需 lfs pull；或 `--recurse-submodules` 已有，LFS 单列开关 | `gcm/git/service.py` `settings.py` | E2E：LFS 开关开启时命令含 filter.lfs；关闭无 |
| `[✅ 已落地]` **G48-3**（承接原G41-3） | P3 | 无指定 SSH key（多账号/公司内网） | 详情页「该仓库使用指定 key」→ clone/fetch env 注入 `GIT_SSH_COMMAND="ssh -i <key>"`（key 路径存 note 字段或新列 `ssh_key`） | `gcm/git/service.py` `repo_db.py` `repo_detail_dialog.py` | E2E：指定 key 克隆真实 SSH 仓库（本地 sshd 可选）；未指定走默认 |
| `[✅ 已落地]` **G48-4** | P3 | 同一 host 多账号只能一个 token | host_tokens 值支持 `user1:token1\nuser2:token2` 映射账号名 → 每仓库可选账号（存账号字段）；clone 时按账号取 token | `gcm/db/settings.py` `gcm/git/service.py` `repo_db.py` | 单测：账号解析与按账号取 token 正确；未选账号用默认 |
| `[✅ 已落地]` **G48-5** | P3 | 克隆后无自动操作 | 设置页「克隆后运行」：可选命令模板（如 `%PATH%` 占位符替换为仓库路径）→ 成功克隆后后台执行，失败仅日志 | `gcm/git/service.py` `settings.py` | E2E：克隆后钩子执行（真实临时命令）；占位符替换正确 |
| `[✅ 已落地]` **G48-6** | P3 | 无裸仓库/镜像克隆模式 | 克隆模式加第四项「镜像克隆（--mirror）」（适合备份）；更新路径对 mirror 仓库做 `git remote update` | `main_window.py` `service.py` | E2E：mirror 克隆 refs 完整；二次同步 remote update |
| `[✅ 已落地]` **G48-7** | P2 | 只能 clone/fetch；小需求（只要代码快照）也全量克隆 | 新增「归档下载」：`git archive --remote=<url> HEAD | tar -x` 或 `git clone --depth 1` + archive；输出 zip 到下载目录（免入库） | 新 `gcm/git/archive.py` `main_window.py`（输入区右键「归档下载」） | E2E：真实仓归档 zip 可解压且含源文件 |
| `[✅ 已落地]` **G48-8** | P2 | 网络问题定位靠试错（超时/重试文案已有）；无一键诊断 | 设置页「网络诊断」：串行探测 git 版本 → github.com TCP(443) → GitHub API 延迟 → 代理连通 → 输出诊断报告（含 redact） | 新 `gcm/app/diag.py` `settings_panel.py` | 单测：mock 探测序列 → 报告分级（绿/黄/红）；无死锁 |
| `[✅ 已落地]` **G48-9** | P2 | 弱网降级链（treeless→depth1）存在但**触发后才提示** | 预检（G38-3）阶段测下载速率：`git ls-remote` 耗时 > 阈值 → 提前建议"切换浅克隆/镜像"；克隆中低速率（`http.lowSpeedLimit` 触发前）提示 | `gcm/git/service.py` `main_window.py` | 单测：慢速模拟 → 建议文案出现；速率正常无打扰 |

---


> **状态说明（2026-09-19 回填）**：G46-1..G46-12 已在 M4（v7.7.0）全部交付并发布
> （Release id=391894302），逐条验收登记见 `验收清单.md`「M4 v7.7.0 G46 UI/UX 设计系统」段；
> 本表标记由 [待办] 回填为 [✅ 已落地]，与交付一致。

> **状态说明（2026-09-19 回填）**：G47-1..G47-7 与 G48-1..G48-9 已在 M5（v7.8.0）全部交付并发布
> （Release id=391934352），逐条验收登记见 `验收清单.md`「M5 v7.8.0 G47+G48 数据洞察与 Git/网络深化」段；
> 本表标记由 [待办] 回填为 [✅ 已落地]，与交付一致。

> **状态说明（2026-09-19 回填）**：G45-1..G45-9 已在 M1–M3（v7.3.0→v7.6.0）全部交付并发版；
> G49-1..G49-8 已在 M6（v7.9.0）交付并发版（Release id=392004092）。逐条验收登记见 `验收清单.md` 对应章节；
> 本表标记由 [待办] 回填为 [✅ 已落地]，与交付一致。

## G49 长期演进与生态

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G49-1**（承接原G41-1） | P2 | `Settings.language`（`settings.py`）字段空转；全部文案硬编码中文 | **轻量 i18n**（不引 Qt Linguist，保持零构建）：新 `gcm/i18n/__init__.py` 提供 `tr(key)`；先抽下载页+设置页文案到 `gcm/i18n/zh.py`/`en.py` 字典；设置语言下拉切换后重启生效 | 新 `gcm/i18n/` `main_window.py` `settings_panel.py` | 切 en 重启后下载页全英文；zh 默认不变；缺失 key 回退中文 |
| `[✅ 已落地]` **G49-2**（承接原G41-2） | P2 | 数据目录固定 exe 同级 `data/`（GCM_DATA_DIR env 已支持） | `--portable` CLI 参数：portable → exe 目录 data（现状默认）；默认 → `%APPDATA%/Git-clone-Max`（已有 data 目录则沿用，兼容升级） | `gcm/__main__.py` | 三分支单测；旧用户升级无感（数据不丢） |
| `[✅ 已落地]` **G49-3**（承接原G41-4） | P3 | 无 CLI 批量模式（根目录 `cli.py` 仅 113 字节占位 docstring） | `python -m gcm --cli < urls.txt`：复用 engine/service，offscreen 不建窗；stdout 进度 | `gcm/__main__.py` 新 `gcm/cli.py` | E2E：3 仓库批量克隆成功且退出码正确；CI 可跑 |
| `[✅ 已落地]` **G49-4**（承接原G41-5） | P3 | README 无 uv 现代环境声明 | README 补 `uv run python -m gcm`；pyproject `[project]` 元数据补全（readme/urls/classifiers） | `README.md` `pyproject.toml` | uv 用户可直接跑 |
| `[✅ 已落地]` **G49-5**（承接原G41-6） | P3 | 无安装器 | Inno Setup 脚本 `packaging/installer.iss`（可选产物，不替代便携版）：开始菜单/卸载/文件关联（可选） | 新 `packaging/` | 产出 setup.exe 可安装卸载；开始菜单图标 |
| `[✅ 已落地]` **G49-6**（新） | P2 | 开机自启已存在（任务计划）；无"最小化到托盘启动"选项 | 设置页「启动时最小化到托盘」（需托盘可用）；自启命令追加 `--minimized` | `main_window.py` `settings.py` | 离屏单测：开关后自启命令含 --minimized；托盘模式下启动不弹主窗 |
| `[✅ 已落地]` **G49-7**（新） | P3 | 完成通知只有提示音+托盘 | 可选 **完成通知 Webhook**：Server酱/Telegram Bot（设置页填 URL+可选 token），引擎 finished 时 POST 摘要（数量/失败列表 redact 后）；**付费 API 红线**：仅用户显式开启+填 URL 才发，Mock 测试覆盖 | `gcm/app/notify.py` `settings_panel.py` | 单测：mock HTTP 断言 payload 结构与 redact；关闭不发 |
| `[✅ 已落地]` **G49-8**（新） | P3 | 数据目录无"打开数据目录"入口（日志/备份/lists 所在） | 设置页「打开数据目录」按钮 → QDesktopServices.openUrl | `settings_panel.py` | 离屏 mock openUrl 调用一次 |

---

## G50 本轮实测新发现（2026-09-18 登记，M0 优先处置）

> 本节条目全部来自 2026-09-18 本机实测（`git status` / `coverage run` / 单文件测试 / HEAD worktree 对照），**非推测**。M0 必须全部处置，之后进入 M1 性能治理。

| 条目 | 优先级 | 现状（实测证据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G50-1** | **P0** | `tests/test_updater_retry.py::test_silent_check_failure_logged` **单独跑崩溃**（exit -1073740791 = 0xC0000409 fail-fast）；`gcm/ui/main_window.py` 离屏构造同样崩溃；**HEAD 6d33f0a（v7.3.0 发布版）同样复现 → 非 G38 回归**。根因：构造 `MainWindow` 前未先行创建 `QApplication` 实例（PyQt6 6.11 下 QWidget 构造需 app 存在） | ① 测试文件顶部/模块级加 `QApplication.instance() or QApplication([])`（参考 `test_g35_ui.py` 既有模式）；② 排查其余直接构造 MainWindow 的测试是否缺 app；③ 全量 discover 两次 + 单文件随机抽 5 个跑（确认无顺序依赖侥幸） | `tests/test_updater_retry.py`（+ 同类测试） | 单文件 `test_updater_retry` 全绿；任意单文件可独立跑；全量仍 561 全绿 |
| `[✅ 已落地]` **G50-2** | P0 | **工作区 G38 全部改动（6 源文件 + 3 测试文件）未提交**；`git status` 显示 `M gcm/app/engine.py / url_lib.py / db/settings.py / git/service.py / ui/main_window.py / ui/settings_panel.py` + `?? tests/test_g381_* / test_g38_network.py` | 按主题拆 2 个 commit（① G38 网络前沿逻辑+UI；② G38 测试与文档），**禁止 `git add .`、禁止 -f**；fetch origin --prune 后基于 origin/main 收口；验证远端 SHA | 工作区 | commit 后 `git status` 干净；本地 SHA 与远端 SHA 复验一致 |
| `[✅ 已落地]` **G50-3** | P1 | **全量测试 880.133s（14.7 分钟）**，CI 每次 push 全量跑，回归反馈慢；最慢单文件：test_engine 99s / test_v7_fixes 76s / test_core 74s / test_extra 71s / test_v3_p0 64s | ① 定位慢用例（cProfile/per-test 计时），真实 git E2E 类测试统一用本地裸仓 + `GIT_CONFIG_NOSYSTEM` 提速；② 非关键慢测标 `@unittest.skipIf(CI_SLOW)` 不默认跑，CI 加独立 slow job；③ README 登记"快速冒烟"命令（抽样） | `tests/*` `.github/workflows/ci.yml` | 默认全量 ≤ 600s；慢测单列 job；README 有快速命令 |
| `[✅ 已落地]` **G50-4** | P1 | **`Git-clone-Max.spec` 未被 git 跟踪**（`.gitignore` 含 `*.spec`），clone 后无法复现打包配置 | ① `.gitignore` 去掉 `*.spec`；② `git add Git-clone-Max.spec` 入库；③ 打包流程注释"spec 以仓库内为准" | `.gitignore` `Git-clone-Max.spec` | `git ls-files` 含 spec；干净 clone 可 `pyinstaller Git-clone-Max.spec` |
| `[✅ 已落地]` **G50-5** | P1 | **README 功能表停在 v5 描述**：缺 v6/v7/G38 新能力（多主题/统计中心/任务清单/热键/按 host 凭据/镜像/预检/单分支/强制 IPv4/自定义主机）；"已具备能力"与实测不符 | README 功能表按 31.2 能力矩阵重写（多平台/克隆模式/网络认证/批量/数据/UIUX/工程）；`启动Git-clone-Max.bat` 位于 `scripts/`（README 已正确引用） | `README.md` | README 功能表与 G31.2 能力矩阵一致；无 v5 过时表述 |
| `[✅ 已落地]` **G50-6** | P2 | `workflow_status.md` 停在 v4.1 历史；`计划书/验收清单.md` 记录到 v7.3.0 发布闭环但 G38 未登记 | ① `workflow_status.md` 追加 G38 就绪与 M0 记录（保留历史不动）；② 验收清单登记 v7.3.1（M0）后 G38 状态 ✅ | `workflow_status.md` `计划书/验收清单.md` | 文档与 git 状态一致；验收清单含 G38 39 用例证据 |
| `[✅ 已落地]` **G50-7** | P2 | 覆盖低点：`single_instance 59%` / `statistics_dialog 63%` / `main_window 73%` / `manage_model 74%` / `settings 79%`（2026-09-18 实测） | M0 先补 **statistics_dialog**（63%，独立 UI 对话框，mock db 即可覆盖）与 **single_instance**（59%，补锁文件损坏/PID 复用分支）；main_window 73% 依赖 G45-6 拆分后补 | `tests/test_statistics_dialog.py` `tests/test_single_instance.py` | statistics_dialog ≥80%；single_instance ≥80%；TOTAL 保持 ≥82% |
| `[✅ 已落地]` **G50-8** | P3 | `data/repos.db-shm/repos.db-wal` 随运行增长且 gitignore 已排除，但 `workflow_status.md`/根目录仍混放 `workflow_status.md` 等旧产物 | 根目录只保留 README/LICENSE/pyproject/requirements/spec/cli.py；`workflow_status.md` 移动或归档到 `计划书/`（历史保留）；数据目录归 `data/` 不动 | 根目录 | 根目录文件清单收敛；README 提及计划书目录 |
| `[✅ 已落地]` **G50-9** | P3 | 无 `docs/RELEASE_BODY.md`；`publish_release.py` body 模板硬编码旧文案（G45-4 前置） | 新建 `docs/RELEASE_BODY.md` 模板（版本占位符），发布脚本读取替换 | `docs/RELEASE_BODY.md` `scripts/publish_release.py` | dry-run body 含最新能力摘要与 sha256 行 |

---

## 附录A 里程碑路线与门禁

> **执行铁律**：每个里程碑 = 全量测试绿 + 覆盖率 ≥ 当前门禁 + 验收清单登记证据 → 才进下一个。
> 严禁前面改 bug 后面加功能又出新 bug；每条目先写测试（TDD），再实现，再全量回归。

| 里程碑 | 聚焦 | 条目 | 预估规模 | 门禁 |
|--------|------|------|----------|------|
| **M0 v7.3.1** | G38 收口发布 + 测试隔离 + 工程收尾 | G50 全部（G50-1/G50-2 为 P0 前置） | 小（~1 天） | 全量绿；单文件独立可跑；覆盖 ≥82%；spec 入库；README 同步；Release 双产物 |
| **M1 v7.4.0** ✅ | 性能与资源治理 | G43 全部 | 中（~3 天） | 600 仓批次内存 before/after 登记；UI 不卡体验登记 |
| **M2 v7.5.0** ✅ | 安全纵深 | G44 全部 | 中（~2 天） | 安全回归全绿（redact/token/白名单/host_tokens/敏感目录） |
| **M3 v7.6.0** | 工程与结构 | G45 全部（含 G45-6 拆分） | 大（~3 天） | main_window ≤1300 行；覆盖 ≥85%；CI 三平台矩阵绿 |
| **M4 v7.7.0** | UI/UX 设计系统 | G46 全部 | 大（~4 天） | 截图标定登记；离屏全绿；无功能回归 |
| **M5 v7.8.0** | 数据洞察 + Git 深化 | G47 + G48 | 大（~4 天） | 全量绿；覆盖 ≥85% 不降；真实 E2E 登记 |
| **M6 v7.9.0** | 长期演进 + 生态 | G49 | 中（~2 天） | 全量绿；CLI E2E 可跑；i18n 切换生效 |
| **M7 v8.0.0** | 收官验收 | 全模块回归 + Release 双产物 | 中（~2 天） | Release 双产物 + 发布清单走查 + v8.0 收官 |

**执行顺序原则**：M0 → M7 顺序执行；M3 与 M4 可并行切分（UI 与结构各自独立文件域）；任何条目发现牵连缺陷 → 记入 G51 补条目（编号顺延 G51-1+），不允许"顺手改"无测试无登记。

## 附录B TDD 执行规约（每条必守）

1. **先写测试，再实现**：每条目先 RED（新增/修改测试，跑出失败）→ GREEN（最小实现）→ IMPROVE（重构+补断言）。验收清单须登记 RED 证据（测试名+首次失败输出摘要）。
2. **覆盖纪律**：核心模块（engine/service/repo_db/settings/url_lib）新增/修改必须保覆盖；UI 条目默认带离屏 QTest；覆盖率下降即红灯，先补再进。
3. **全量回归**：每个里程碑结束跑 `python -m coverage run --source=gcm -m unittest discover -s tests && python -m coverage report`（全量约 880s，2026-09-18 实测；G50-3 落地后目标 ≤600s）；**单文件独立可跑**是 M0 后默认要求（G50-1）。
4. **禁止假实现**：mock 只用于外部边界（网络/时钟/对话框）；业务逻辑必须真实执行；E2E 用本地裸仓，不依赖外网。
5. **证据登记**：每条 ✅ 必须附"验证命令 + 输出摘要"，写入 `计划书/验收清单.md`；无证据不标完成。
6. **版本节奏**：每个里程碑发一个 semver 版本（v7.3.1 起），`__changelog__` 追加一行；Release 流程按 `docs/RELEASE_CHECKLIST.md`（已落地）。
7. **付费 API 红线**：G49-7 等外部服务一律 Mock 测试，真实调用需用户显式批准。
8. **Windows 规则**：禁止新增 `.sh` 脚本（用 node/PowerShell）；批处理保持 CRLF（.gitattributes 已强制）。
9. **测试隔离铁律**：任何直接构造 MainWindow/QWidget 的测试，模块级先行 `QApplication.instance() or QApplication([])`（G50-1 立规）。

## 附录C 关键代码定位表（2026-09-18 实测）

| 关注点 | 文件:行 | 备注 |
|--------|---------|------|
| 版本号/changelog | `gcm/__init__.py:2,4-73` | v7.3.1 追加至 74+ |
| 入口/数据目录 | `gcm/__main__.py`（61 行） | G49-2/G45-3 改造点 |
| 引擎（并发/去重/进度） | `gcm/app/engine.py`（458 行） | `launch:275` `_on_worker_done:398` `drain:422` `shutdown:445` G43-2/G43-5/G48-1 改造点 |
| worker | `gcm/app/worker.py`（125 行） | 不建议动 |
| git 服务 | `gcm/git/service.py`（935 行） | `sync:601` `_clone:658` `_update:777` `_env:422` `_token_for_host:481` `_mirror_url:495` `_precheck_reachable:510` G48-2/3/6/9 改造点 |
| URL 解析 | `gcm/app/url_lib.py`（399 行） | `parse_any_repo_url` `apply_mirror_prefix` `is_supported_host` G44-5 改造点 |
| SQLite | `gcm/db/repo_db.py`（519 行） | `_migrate:56` `BEGIN IMMEDIATE:110` `backup_to:421` `stats_daily:367` G43-7/G47-1/5/6/7 改造点 |
| 设置 | `gcm/db/settings.py`（275 行） | `Settings` 字段 `:140+`；host_tokens 加密 `:200-232` G44-1/G48-4 改造点 |
| 主窗口 | `gcm/ui/main_window.py`（**1948 行**） | `_build_download_tab:384` `_build_manage_tab:525` `_launch:1186` `_on_worker_result:1534` `_animate_result_row:1597` `_on_engine_finished:1631` `closeEvent:1905`；G38 回调 `:777-812` G45-6 二次拆分点 |
| 主题/LogModel | `gcm/ui/theme.py`（304 行） | `LogModel:227`（list 裁剪非 deque）G43-1 改造点；G46-1/3/5/6/7 设计 token 改造点 |
| 设置面板 | `gcm/ui/settings_panel.py`（345 行） | G38 连接 `:217-251`；G44-1/G46-10/G48-8/G49-6 改造点 |
| 托盘 | `gcm/ui/tray.py`（122 行） | G46-9 改造点 |
| 更新检查 | `gcm/app/updater.py`（106 行） | G44-3/G50 改造点 |
| 统计中心 | `gcm/ui/statistics_dialog.py`（169 行） | `_TrendCanvas:29` G43-3/G47-4/G50-7 改造点 |
| 仓库详情 | `gcm/ui/repo_detail_dialog.py`（321 行） | G47-2/G48-3 改造点 |
| 报表 | `gcm/reports.py`（123 行） | G47-3 改造点 |
| 打包 | `Git-clone-Max.spec`（**未入库，G50-4**） | G43-4 改造点 |
| CI | `.github/workflows/ci.yml` | `--fail-under=82` G45-1/7 改造点 |
| 发布脚本 | `scripts/publish_release.py` | G45-4/8/9 改造点 |

## 附录D 当前已知风险与待验证项

| 编号 | 风险 | 等级 | 说明与处置 |
|------|------|------|-----------|
| D-1 | **覆盖率临界**：本机实测 TOTAL 82% 与 CI 门禁 `--fail-under=82` 持平，任何重构/omit 调整都可能跌破 | P0 | 上一版"81% < 82%"已实测更新为 82%；M0 补 statistics_dialog/single_instance（G50-7）后复测对齐；发布前必须确认 CI 绿 |
| D-2 | `main_window.py` 1948 行二次膨胀（G33-8 拆分收益被 v7.1–v7.3 + G38 抵消） | P1 | G45-6 二次拆分前，**所有 UI 改动必须同时评估行数影响**，优先把新逻辑放独立模块 |
| D-3 | ~~G38 UI 层接线缺失（100 errors）~~ | ✅ | **2026-09-18 实测已关闭**：6 回调已实装（`main_window.py:777-812`），专项 39 用例全绿，全量 561 OK。剩余缺口转 G50-2（未提交） |
| D-4 | `_env()` 中未知 host 用 Bearer 头（`service.py:460-462`）：host_tokens 登记的 gitlab 自建实例会走 `Authorization: Bearer` 而非 PRIVATE-TOKEN | P2 | 自建 GitLab 可能认 Bearer 不认 PRIVATE-TOKEN。待验证：映射 host 按 feature 前缀区分头类型，或 UI 提供"头类型"下拉（Bearer/PRIVATE-TOKEN/Basic）。**2026-09-18 确认仍存在** |
| D-5 | `updater._gh_token()` 从 `git credential fill` 读 token 供 API 限流使用，进入 `Authorization` 头（`updater.py:17-32`） | P2 | 该 token 只进内存请求头，不落日志文件（urllib 无日志）；**待验证**：确认异常信息/代理层不打印 header；后续可在 updater 出口统一过 `redact()` |
| D-6 | 覆盖率 82% 停滞（v7.1→v7.3 新增大量 UI 代码未同步补测试） | P1 | G45-3/G50-7 是主修复路径；M3 门禁 85% 依赖其落地。新增 UI 条目应默认带离屏 QTest |
| D-7 | G43-2 批次流控会改变 `_on_worker_done` 补投语义 | P1 | 改造必须保留：去重键顺序、in_progress 生命周期、drain 语义；动 engine 需附并发回归证据（G45-2 压力测试先行更稳） |
| D-8 | **测试隔离缺陷（新）**：`test_updater_retry.py::test_silent_check_failure_logged` 单独跑崩溃（0xC0000409），根因=构造 MainWindow 前未创建 QApplication；全量 discover 顺序依赖侥幸通过 | P0 | **2026-09-18 实测确认（HEAD 同崩，非回归）**；处置见 G50-1：模块级建 app + 全量/单文件双通道验证 |
| D-9 | **全量测试 880s 偏慢（新）**：CI 每次 push 全量跑 14.7 分钟，回归反馈慢 | P1 | 2026-09-18 实测 `Ran 561 tests in 880.133s`；处置见 G50-3：慢测定位/本地裸仓提速/slow job 分离 |

---

*v8 指南 = 唯一事实源。实施方按 M0→M7 顺序推进，每条 TDD + 证据登记；主控只看 `计划书/验收清单.md`。*
---

# 第二部分：第二轮迭代总纲（v8.0.2 → v9.x · 2026-09-25）

> 本部分为**当前唯一事实源**：第一轮 M0–M7（v7.3.1→v8.0.1）与 P-perf（v8.0.2）已全部交付，第一轮全部条目状态为 `[✅ 已落地]`，详见上方 v8 总纲 + `计划书/验收清单.md`。
> **基线（2026-09-25 实测核对）**：HEAD `4429c32`（main）· `__version__ = 8.0.2`（`gcm/__init__.py:2`，changelog 已含 v8.0.2）· 全量测试 **770 绿 / 覆盖 84%**（Windows 门禁 84 / POSIX 83，`ci.yml`）· ruff 0 · mypy 0 · 三平台 CI + e2e-smoke 绿（最近提交 `4429c32` 即 CI 修复）· 本地扫描提速 **~95x**（150s→1.6s，commit d1c346f/4f76722）· 本机 Python 3.14.3 + PyQt6 6.11.0 可用（.venv 实测）· git 工作区干净（`git status` 无改动）。
> **编号规则**：第二轮编号从 **G51 顺延**（G43–G50 已回填 ✅，禁止重复实施）；里程碑从 **M8 顺延**，版本按 M8→M16 依次 bump 到 **v9.x**。
> **文档配套**：`计划书/规划总览.md`（目录）、`计划书/验收清单.md`（逐条证据登记）、`计划书/workflow_status.md`（推进日志）、`计划书/跨端响应式与UX-UI优化计划.md`（UX 专项）、`docs/慢测定位.md`（全量测试提速）。
> **执行对象**：任意 AI 编码代理（Builder）照此逐条实施 + 证据登记；主控只核 `验收清单.md`。

---

## 0. 使用约定（第二轮补充，先读）

1. **先方案后编码**：每条先在本指南定位落点（现状/方案/文件/验收），再 RED→GREEN→IMPROVE；禁止"边改边想"。
2. **TDD 铁律**：每条先列测试清单（RED 证据：测试名 + 首次失败输出摘要）→ 实现 → 转绿 → 全量回归 → 覆盖证据。
3. **强制验证**：`python -m coverage run --source=gcm -m unittest discover -s tests && python -m coverage report --fail-under=84`；`python -m ruff check gcm && python -m mypy gcm`；`python scripts/e2e_smoke.py`；涉及扫描/打包另跑 `scripts/bench_scan.py` / `scripts/health_check.py --json dist/health_check.json`。
4. **不重构守则**：仍严禁大重构；小逻辑 Bug 可顺手修但必须入测试登记（新条目 G66 补丁区）。
5. **清理纪律**：升级完成清理 `build/`、`dist/` 旧产物、根目录 `*.log`（regress_*/publish_*/build_*）、临时诊断文件、`计划书/参考扫描证据/` 历史证据目录**归档为 zip 再删除原目录**（保留溯源）；**绝不删 `data/` 用户数据、不覆盖用户改动**；删除前列路径+理由，重大删除先确认。
6. **文档权威化**：本指南 + `验收清单.md` + `workflow_status.md` 三处同步；schema/任务变化同步 `db_structure.md`/`project_specs.md`（如已建则更新，未建则在 M8 首条建立）。
7. **上下文感知**：实施前先读 `README.md` 功能表、`ci.yml` 门禁、本指南附录 C 代码定位表；改动处标注 G 编号注释，方便审计与回滚。

---

## 1. 第一轮总复盘（0-1 已完成，1-2 正在收敛）

### 1.1 已交付能力（真实证据）
- **性能**：G43-1..8 全落地（LogModel deque、引擎 250ms 行合并、批次流控 64 惰性补投、慢仓 Top10、打包瘦身、100ms 进度心跳、WAL checkpoint、URL 解析编译正则）；**v8.0.2 本地扫描提速 95x**（scanner 纯文件读 + 线程池 + junction 跳过 + 批量事务写库）。
- **安全纵深**：G44-1..9 全落地（token 密码框+眼睛、host_tokens 逐条目加密、`_safe_rmtree_partial` 半成品保护、redact 全链路、f-string 安全、git 子命令不拼 shell、DB 备份恢复、凭据注入白名单、安装器清理）；URL 层 `@` 前置拒绝 / `..` 拒绝 / ref 校验 / custom_hosts 白名单保持回归。
- **工程**：G45-1..9 全落地（三平台 CI + mypy 门禁 + 真实 git E2E job、发布脚本 tag 幂等补建、main_window 拆分 → 1432 行、spec 入库、RELEASE_BODY 模板、健康检查冒烟）；**G50-1..9 全落地**（测试隔离修复、覆盖低点补齐、慢测定位文档、README 能力矩阵 28 行）。
- **体验**：G46-1..12（设计 token/高对比/焦点环/键盘导航/批次总览/托盘子菜单/帮助对话框/reduced-motion）；G49-1..8（i18n zh/en、--portable、CLI 批量、uv、Inno 安装器、自启 --minimized、通知、Webhook）。
- **数据/Git**：G47-1..7（健康分/趋势 hover/xlsx/多标签 AND-OR/元数据导入导出）+ G48-1..9（并发自适应/LFS/SSH key 指定/多账号 token/克隆后钩子/mirror/归档下载/网络诊断/弱网预提示）。

### 1.2 遗留缺口（第一轮基线 → 第二轮"2→3"定义）
| 维度 | 已达标 | 缺口（本轮主攻） |
|------|--------|------------------|
| 批量下载 | 并发 32 + 流控 + 弱网降级 | 无**任务级失败自动排队重试表**；无**跨会话续跑清单**（仅崩溃回填输入区，`main_window.py:153`）；无**失败原因聚合/重试策略可视可配** |
| 数据洞察 | 汇总/趋势/评分/导出 | **多维评分卡**未落库（repo_health 单值）；无**阈值告警**（陈旧/失败率/磁盘水位）；无**定期扫描仪表盘**（v8.0.2 提速后没有规模化 ROA 证明） |
| 小白体验 | 向导/帮助/教学文案 | 设置页无**表单校验即时反馈**（坏 URL/镜像/https 显式报错）；无**一次性引导清单**（first-run checklist）；帮助无"排错地图"页 |
| 工程闭环 | 门禁/CI/发布 | **单测全量 770 条偏慢**（`docs/慢测定位.md` 有基线无统一提速手段）；无 **mutmut 变异测试**；无 **QSS 语法静态校验**；`gcm/ui/*.py` 大量 E501 长行豁免待收 |
| 生态 | CLI/Webhook/i18n | 无 **MCP server**；无 **agent 技能包**（skills/gcm-*）；CLI 无 `--json` 机器输出；无**本地 HTTP API** 供外部调度（热键/队列/状态查询） |
| 桌面工程 | PyQt6 6.11 | 无**单测级内存/泄漏基线**；无**托盘图标视觉回归**；无**scale/dpi 变化下的布局回归**（`跨端响应式计划` 已有策略未落 320/768/1024/1440 语义档） |

---

## 2. 第一性原理瓶颈分析（第二轮）

把"批量获取 GitHub 仓库 → 入库 → 管理 → 汇报 → 被 agent 调用"拆到本质：

| 本质诉求 | 现状（证据） | 本轮瓶颈/机会 |
|---|---|---|
| 快 | 并发 32、流控 64、归档下载、镜像、弱网降级 | ① 失败任务只能"整批重试"，无**自动重试队列**（退避/重试次数/失败原因聚合）；② 无**并发=ƒ(历史失败率)** 的闭环（G48-1 有降级但无持久记忆） |
| 稳/可恢复 | 断点续传、崩溃回填、WAL checkpoint、单实例锁 | ① 中断后**无持久化运行清单（pending/resume）**，回填输入区是弱语义；② 无**看门狗续跑**（长时间批量挂机）；
③ progress.json/历史增长无主动治理开关 |
| 懂 | 统计中心、报表、健康分、趋势 | ① 健康分单值 → **多维评分卡**（成功率/陈旧/冲突/大小/标签/最近同步）+ 明细+趋势；② 无**阈值告警**（噪声式/邮件-Webhook 通知）；③ 无**磁盘水位/DB 体积看板** |
| 小白友好 | 向导、帮助、tooltip | ① 设置表单无**即时校验反馈**；② 无排错地图（错误码→原因→动作）；③ 无"一键体检"聚合（G48-8 有网络诊断，可扩展为环境体检） |
| 可扩展/agent 化 | CLI、Webhook、i18n | ① 无 MCP/技能包（`skills/gcm-*`）交付；② CLI 无 `--json`；③ 无本地 HTTP API（供热键工具/其他 agent 调度） |
| 工程质量 | 770 用例/84%/ruff/mypy/CI | ① 全量 15-20 分钟反馈慢（`docs/慢测定位.md`：慢文件 Top10 已有清单）；② 无变异测试；③ 无 QSS 语法校验/内存基线 |

---

---

## 3. 第二轮里程碑路线（M8 → M16，版本 v8.0.2 → v9.1）

> 执行铁律：**每里程碑 = 全量测试绿 + 覆盖 ≥84 + ruff 0 + mypy 0 + 真实 E2E 绿 + 验收清单登记证据 → 才进下一个**。
> 严禁"前面改 bug 后面加功能又出新 bug"：任何缺陷 → 记入 **G66 补丁区**（编号顺延），先修后进，入测试。

| 里程碑 | 版本 | 聚焦 | 主线条目（编号） | 预估规模 | 门禁/验收 |
|--------|------|------|------------------|----------|-----------|
| **M8** | v8.1.0 | 文档权威化 + 稳定底盘 | G51 全部（db_structure/project_specs 建立、基线证据入库、README 更新） | 小（~1 天） | 全量绿；文档与代码一致；覆盖不降 |
| **M9** | v8.2.0 | 任务级可靠性 | G52 全部（自动重试队列/续跑清单/看门狗/进度治理） | 中（~3 天） | 模拟中断恢复 E2E；重试策略单测；全量绿 |
| **M10** | v8.3.0 | 数据洞察增强 | G53 全部（多维评分卡/阈值告警/磁盘与 DB 体积看板） | 中（~3 天） | 评分矩阵单测；告警触发/抑制 E2E；UI 渲染离屏 |
| **M11** | v8.4.0 | 小白体验 + 体检 | G54 全部（表单即时校验/排错地图/一键体检升级） | 中（~3 天） | 校验消息断言；排错地图与真实代码对照；真实环境一次体检报告登记 |
| **M12** | v8.5.0 | 工程与测试体系 | G55 全部（全量提速/变异测试/QSS 校验/内存基线/E501 治理） | 中（~3 天） | 全量耗时下降有数据；mutmut 抽样击杀率有数据；内存基线命令输出 |
| **M13** | v8.6.0 | 生态 agent 化 | G56 全部（skills/gcm-*/MCP server/CLI --json/本地 HTTP API） | 大（~4 天） | skills 被真实 agent 会话调用成功记录；MCP 工具冒烟；--json schema 断言 |
| **M14** | v8.7.0 | 响应式与无障碍落地 | G57 全部（320/768/1024/1440 语义档/DPI 基线/键盘焦点/AI 无障碍声明） | 中（~3 天） | 三档 resize 离屏断言；键盘全遍历可达；截图登记 |
| **M15** | v8.8.0 | 网络/镜像/新平台深化 | G58 全部（GitHub API 限流友好/镜像源测速与自动择优/HTTP 代理认证/新增 host 探测/自建实例类型识别） | 中（~3 天） | mock 限流/镜像择优单测；真实诊断报告登记 |
| **M16** | v9.0.0 | 收官验收 | 全模块回归 + 打包瘦身复核 + Release 双产物 + 文档清账 | 中（~2 天） | Release 双产物 + 发布清单走查 + v9.0.0 收官；后续 v9.x 维护小版本顺延 |

> 说明：M12 与 M13 可并行切分（测试体系与生态各自独立文件域）；其余顺序执行。任何条目发现牵连缺陷 → G66 补丁区，不允许"顺手改"无测试无登记。

---

## 4. TDD 清单与执行顺序（DoD，逐节点）

```
0. 读项目结构与既有测试 → 避免重复/冲突（附录 C 定位表 + README + ci.yml）
1. 写方案落点（本指南对应 G 条目）→ 拆任务节点（输入/输出/验收）
2. TDD：先写失败测试（单元 → 集成 → E2E；RED 证据登记测试名+失败摘要）
3. 实现最小可运行代码（禁止 TODO/省略号/散文式延迟；全部生产级代码）
4. 转绿：单文件 → 相关回归 → 全量
5. 质量闸：coverage ≥84 / ruff 0 / mypy 0 / e2e_smoke 绿
6. 真实体验：`scripts/启动Git-clone-Max.bat` 真实启动，遍历三 Tab + 关键按钮（开始/暂停/取消/清空/归档/诊断/Webhook/主题/语言/托盘），记录反馈缺失/报错；离屏截图登记
7. 收尾：清理临时产物；更新 `验收清单.md` + `workflow_status.md` + README（用户可见行为必同步）
8. 二次审查：自查 diff + 相邻行为；高风险项（engine/service/db/安全）交独立 Critic 复审
```

**强制验证命令（每节点至少展示其一）**：
- 全量：`python -m coverage run --source=gcm -m unittest discover -s tests && python -m coverage report --fail-under=84`
- 质量：`python -m ruff check gcm && python -m mypy gcm`
- E2E：`python scripts/e2e_smoke.py`
- 打包/扫描：`python scripts/health_check.py --json dist/health_check.json`；`python scripts/bench_scan.py`

---

## 5. 本轮 QA 与真实体验路径（高级 QA 视角）

1. **启动器实测**：`scripts/启动Git-clone-Max.bat`（真实启动；遍历三 Tab；关键按钮点击反馈：开始/暂停/取消/清空/导出报表/归档下载/网络诊断/Webhook/语言/主题/托盘/自检），记录卡顿/无反馈/报错。
2. **真实 git E2E**：本地裸仓 x3 批量克隆→增量更新→冲突保留→归档 zip 含文件→报表读回（复用 tests 现成工具，`scripts/e2e_smoke.py`）；M9 追加"中断→续跑"真实演示。
3. **打包链路**：PyInstaller 构建 → `health_check.py`（版本一致 + 离屏启动 + repos.db 生成）→ Inno 安装器编译 → 便携 zip 解压冒烟。
4. **三平台 CI**：push 后核对 Actions runs（Windows/Linux/macOS + e2e-smoke 全绿）。
5. **小白路径**：首启向导→引导清单→帮助"原理/排错"→报错信息可读（redact + 可操作建议）。
6. **agent 路径（M13）**：真实 agent 会话调用 `skills/gcm-*` 成功；MCP 工具冒烟；`--json` 被脚本消费。

---

## 6. 清理与维护（屎山治理 · 第二轮）

- **升级后必清**：`build/`、`dist/` 旧版本产物、根目录 `*.log`（regress_*/publish_*/build_*）、临时诊断脚本（run_*/fix_*）、`_*.json/_*.md` 扫描临时文件。
- **归档而非删除**：`计划书/参考扫描证据/`（1178 项目扫描证据，约 430KB）→ 打包 `参考扫描证据-2026-09-19.zip` 后删除原目录（保留溯源）；`参考结果计划指南.md` 已在 v8 指南附录登记，保留。
- **保留**：`docs/`（含截图/发布模板/慢测定位）、`data/`（用户数据**绝不删**）、最新 `dist/` 产物、`requirements*.txt` 锁定版本、`packaging/`。
- **删除纪律**：只删"可再生产物/明确归属我方的临时文件"；删除前列目标路径+理由，重大删除先经用户确认；不覆盖用户改动，不 reset/不 -f。

---

## 7. 运维与上下文持久化（自主续接协议 · 第二轮）

- **预写式日志**：关键决策/数据变更写 `计划书/workflow_status.md`（节点表），跨会话可续。
- **工作缓冲区**：长任务进度写 `计划书/workflow_status.md`；M8 建立 `计划书/db_structure.md` + `计划书/project_specs.md` 权威文件。
- **自我修复协议**：每轮结束自查（测试/覆盖/ruff/mypy/E2E/产物健康）；发现漂移即回滚或修正。
- **及时校验**：任何"已实现"都以命令输出为准；禁止把"我以为有效"当成"已验证"。

---

## 8. 安全红线与决策门禁（第二轮重申）

1. **付费/外部操作**：默认只读；显式开启 + 显式填 URL/token 才发；审计落盘；不越权（M13 MCP/HTTP API 默认只读 + 显式 enable）。
2. **凭据安全**：token/host_tokens/webhook token 加密落盘；日志/报错/Webhook payload/MCP 输出全链路 redact；严禁明文入库/入日志。
3. **数据保护**：不覆盖用户 data/、不强制删除用户数据；git 操作不 -f 推送、不重置、不清理脏工作区。
4. **代码门禁**：coverage ≥84（底盘 ≥83）、ruff 0、mypy 0、E2E 绿、实际产物健康检查通过，方可标记"完成"。
5. **范围门禁**：重大重构/横向扩展（P2 方向如 MCP/技能包）在 M13 按里程碑推进；本阶段严格执行"先文档→后 TDD→再落地"。

---

## 9. 近期执行建议（实施顺序）

1. **M8 先行**：建立 db_structure.md/project_specs.md + 基线证据入库（1 天），后续所有条目有权威上下文。
2. **M9 任务可靠性**（自动重试队列/续跑清单/看门狗）是最接近用户痛点的 P0 级能力。
3. **M11 小白体验**（表单即时校验/排错地图/一键体检）与 **M13 agent 化**（skills/MCP/--json）体现产品愿景。
4. **M12 工程提速**（全量 770 条慢测治理/变异测试）在 M13 大改动前先做，避免大改动后回归验证慢。

---

---

## 10. G51 文档权威化与稳定底盘（M8 · v8.1.0）

> 目标：先把"唯一事实源"补齐——数据库/任务权威文档 + 基线证据入库，后续所有条目有可回溯上下文。**不做代码功能改动（除 README 同步）。**

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[待办]` **G51-1** | P1 | 无 `db_structure.md`；`repo_db._migrate`（`repo_db.py:56`）+ `PRAGMA user_version` + 迁移链（tags/favorite/excluded/note 等）只存在于代码 | 建立 `计划书/db_structure.md`：表（repos/sync_history/…）字段/类型/约束/索引/迁移链（0001→当前）/WAL 说明；`project_specs.md`：目标/已完成/待办/当前里程碑 | 新建 `计划书/db_structure.md` `计划书/project_specs.md` | 与 `repo_db.py` 逐字段核对一致；迁移链编号与代码一致 |
| `[待办]` **G51-2** | P2 | HEAD `4429c32`，全量 770 绿/覆盖 84%（2026-09-19 实测），但本指南"现状基线"停在 v8.0.1 | 更新本指南 §1/§3 基线为 2026-09-25 实测（版本 8.0.2 / 770 / 84% / CI 三平台门禁 84/83 / 扫描 95x）；README「版本」处同步 v8.0.2 | `计划书/下一步改进指南.md` `README.md` | 基线数字与 git/coverage 输出一致 |
| `[待办]` **G51-3** | P3 | `计划书/参考扫描证据/` 四个文件约 430KB 长期占用仓库 | 归档为 `计划书/参考扫描证据-2026-09-19.zip`（保留溯源），删除原目录；更新 `规划总览.md` 指引 | `计划书/` | zip 可解压且与清单一致；原目录删除；文档指引更新 |
| `[待办]` **G51-4** | P3 | `参考结果计划指南.md`（16KB）根目录散放 | 内容已并入 v8 指南 §3/附录（P0-1..3 已实施为 G48-8 等）；归档或保留根目录并注明"历史扫描分析，见计划书" | 根目录 | 根目录文件清单收敛（README/LICENSE/pyproject/spec/cli.py + 必要文档） |
| `[待办]` **G51-5** | P2 | 本指南 v8 部分与第二轮部分同文件，长期会混叠 | 第二轮完成后将 v8 历史段抽到 `计划书/下一步改进指南-v8-历史.md`（只读归档），主指南保留第二轮总纲 + 附录 | `计划书/` | 主指南可独立阅读；历史段可追溯 |
| `[待办]` **G51-6** | P2 | 无"条目状态一览"快速入口（90+ 条分散） | 在 `workflow_status.md` 顶部加"里程碑进度仪表盘"（M8..M16 × 状态徽标/进度条），每次收尾更新 | `计划书/workflow_status.md` | 仪表盘与验收清单一致 |

> 验收（M8）：`git status` 干净；`python -m unittest discover -s tests` 全量绿；文档与代码逐字段核对一致；README 同步 v8.0.2。

---

## 11. G52 任务级可靠性：自动重试队列 / 续跑清单 / 看门狗（M9 · v8.2.0）

> 现状缺口：`main_window.py:153`（G22-2 崩溃恢复）只做"回填输入区"弱语义；失败任务无持久化重试队列；无跨会话续跑清单；长时间大批量无看门狗。本模块全部条目先写 E2E（模拟中断恢复）再实现。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[待办]` **G52-1** | P1 | 失败仅状态行 + 可右键重试；无自动重试策略 | **任务级自动重试队列**：设置页「失败重试」区（开关 + 最多重试次数 0-3 + 退避基数 30s/60s/120s + 只重试网络类错误判定复用 `_is_networkish_error`）；失败入 `data/retry_queue.json`（原子写 + redact）；引擎空闲时按退避自动重投 | 新 `gcm/app/retry_queue.py` `engine.py` `settings_panel.py` | 单测：重试计数/退避/网络类分类；E2E：注入失败 → 自动重试成功；关闭开关不重试 |
| `[待办]` **G52-2** | P1 | 崩溃/退出后仅回填输入区（`main_window.py:153`） | **跨会话续跑清单**：运行中把每任务状态（pending/running/failed）持久化 `data/session_runtime.json`（原子写，含 redact）；启动检测非空 → 弹"续跑"对话框（列出未完成任务，可选一键续跑/清空）；续跑复用 engine | `gcm/app/engine.py` `main_window.py` 新 `gcm/db/runtime_sessions.py` | E2E：模拟中断（kill 进程）→ 重启 → 续跑完成；清空后不弹 |
| `[待办]` **G52-3** | P2 | 无长时间大批量看门狗；中断需手动重跑 | **看门狗续跑**：设置页「看门狗」开关 + 间隔（1/5/15/30 分钟）；看门狗启动时检测续跑清单非空且上次运行未正常完成 → 自动续跑（弹提示 + 静默开关）；看门狗自身幂等（不重复投） | `gcm/app/watchdog.py` `settings.py` `main_window.py` | 单测：定时检测逻辑/幂等；模拟中断 → 看门狗自动续跑 |
| `[待办]` **G52-4** | P3 | `data/progress.json` 长期增长无治理（每批写入） | **进度文件治理**：滚动保留（每批完成压缩为 summary 行）；旧批次归档 `data/progress_archive/`；大小超阈值（如 5MB）自动裁剪 | `gcm/app/engine.py` 新 `gcm/db/progress_gc.py` | 单测：滚动/裁剪正确；大文件裁剪后仍可读 |
| `[待办]` **G52-5** | P2 | 失败原因无聚合；用户不知道"为什么失败、该重试还是修配置" | **失败原因聚合面板**：按错误类别（网络/认证/平台限制/本地）聚合失败次数与建议动作（改 token/开代理/降级浅克隆/检查磁盘），点击可重试该类别；设置页或详情页入口 | `gcm/app/engine.py` `main_window.py` 新 `gcm/ui/failure_panel.py` | 单测：错误分类映射正确；面板数据与 progress 一致；空态占位 |
| `[待办]` **G52-6** | P2 | G48-1 并发自适应降级无持久记忆 | **并发自适应记忆**：把"近 N 批网络失败率"持久化 `data/concurrency_state.json`，启动恢复上次并发（仍受设置上限约束）；状态栏显示当前并发 + 失败率 | `gcm/app/engine.py` `gcm/db/settings.py` | 单测：失败率计算/恢复/上限约束 |

> 验收（M9）：模拟中断恢复 E2E（真实 kill → 重启 → 续跑完成）登记；全量绿 + 覆盖不降；ruff/mypy 0；看门狗幂等验证。

---

---

## 12. G53 数据洞察增强：多维评分卡 / 阈值告警 / 磁盘与 DB 体积看板（M10 · v8.3.0）

> 现状：`repo_health` 为单值 0–5（成功率/陈旧/冲突聚合）；趋势/统计已有（G47）。本轮升级为"可解释、可告警、可治理"。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G53-1** | P1 | `repo_health` 单值（`repo_db.py` 健康评分） | **多维评分卡**：新增 `health_card`（JSON 列或派生视图）含 成功率/陈旧度/冲突数/仓库大小/标签完整度/最近同步 6 维；统计中心"评分卡"页展示明细 + 雷达/条形示意 + 每维建议文案 | `gcm/db/repo_db.py` `gcm/ui/statistics_dialog.py` | 单测：评分矩阵正确；空库占位；UI 离屏渲染 |
| `[✅ 已落地]` **G53-2** | P2 | 无阈值告警；陈旧/失败率/磁盘无主动通知 | **阈值告警引擎**：设置页「告警」区（陈旧天数/单仓失败率/全局失败率/磁盘剩余阈值，可关）；后台按间隔扫描命中 → 弹通知 + 可选发 Webhook（复用 G49-7，默认只读 + 显式授权）；告警去重（同仓库同类型 24h 一次） | 新 `gcm/app/alerts.py` `settings.py` `main_window.py` | 单测：阈值命中/去重/关闭；真实 db 造数据触发一次告警登记 |
| `[✅ 已落地]` **G53-3** | P3 | 无磁盘水位与 DB 体积看板 | 统计中心新增「存储」块：数据目录占用（含 repos.db/WAL/progress/history）与磁盘剩余；超阈值显示建议（备份/清理历史/迁移目录） | `gcm/app/diag.py` `gcm/ui/statistics_dialog.py` `gcm/db/settings.py` | 单测：体积计算逻辑（临时目录造文件）；空态/超阈值文案 |
| `[✅ 已落地]` **G53-4** | P3 | `sync_history` 每日清理定时只随运行 | 设置页「历史保留」：天数（默认 90）与"立即清理"按钮；按新保留策略重算清理窗口（`repo_db.py:237` 参数化） | `gcm/db/repo_db.py` `settings_panel.py` | 单测：参数化清理窗口正确；前端按钮调通达 repos 数 |
| `[待办]` **G53-5** | P2 | 无报表任务计划（手动导出） | 报表可加「定时导出」：间隔/路径/格式（csv/md/xlsx）→ 后台定时生成到指定目录（默认只写入用户指定路径，失败仅日志） | 新 `gcm/app/sched_reports.py` `settings_panel.py` | 单测：定时触发/路径写入/失败日志；无越权写入 |

> 验收（M10）：评分卡/告警/存储块离屏全绿；真实 db 触发一次告警 + 截图登记；全量回归绿。

---

## 13. G54 小白体验：表单即时校验 / 排错地图 / 一键体检升级（M11 · v8.4.0）

> 现状：报错多为 `QMessageBox` 一次性提示（`main_window.py:791-1055` 等），设置表单无即时反馈；G48-8 已有网络诊断可扩展为环境体检。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G54-1** | P1 | 输入区/设置表单错误只有提交后弹窗；坏 URL 静默分拣 | **表单即时校验反馈**：设置页各输入（镜像前缀/https 校验、host_tokens 格式、Webhook URL、代理、端口）失焦即校验，行内红字 + 修复建议；输入区粘贴解析后显示"有效 N / 无效 M"计数条 + 无效行可展开查看 | `gcm/ui/settings_panel.py` `main_window.py` `gcm/app/url_lib.py` | 单测：各校验函数返回正确消息；离屏 UI 断言红字/计数条出现 |
| `[✅ 已落地]` **G54-2** | P2 | 帮助对话框无"错误→原因→动作"映射 | **排错地图页**：帮助对话框新增「排错」页：常见错误（认证失败/平台限制/网络超时/目录占用/磁盘满/token 无效）→ 根因 → 检查动作 → 对应设置入口；内容与真实实现对照（关键行断言） | `gcm/ui/help_dialog.py` | 单测：条目非空 + 与错误分类映射一致；离屏可开 |
| `[✅ 已落地]` **G54-3** | P2 | G48-8 网络诊断单点（git/TCP/API/代理） | **一键体检升级**：新增环境体检（数据目录权限、磁盘、git 版本、Python/Qt 版本、镜像可达性、Webhook 可达性(只读 HEAD)）；报告分级绿/黄/红 + 建议；GUI + CLI `--diag --json` 共用内核 | `gcm/app/diag.py` `settings_panel.py` `gcm/cli.py` | 单测：每项分级/导出；真实环境一次报告登记（入台账/截图） |
| `[✅ 已落地]` **G54-4** | P3 | 首启向导只设目录/并发/代理 | 向导后追加「引导清单」页：完成度列表（创建下载目录 / 配置 token / 测试网络 / 导入 Star 列表 / 查看帮助），可跳过；托盘状态栏显示首次引导未完成提示 | `gcm/ui/onboarding.py` `main_window.py` | 单测：清单状态持久化/跳过；UI 显示正确 |
| `[待办]` **G54-5** | P3 | 无"一键复制诊断信息"（报障帖） | 排错/体检报告加「复制诊断文本」按钮（redact 后组装：版本/git/系统/目录/最近错误摘要） | `gcm/ui/help_dialog.py` `gcm/app/diag.py` | 单测：redact 后无明文 token；组装可读 |

> 验收（M11）：即时校验/排错地图/体检离屏全绿；一次真实体检报告登记；无回归。

---

## 14. G55 工程与测试体系：全量提速 / 变异测试 / QSS 校验 / 内存基线（M12 · v8.5.0）

> 现状：全量 770 条约 15-20 分钟（`docs/慢测定位.md` 有 Top10 慢文件基线）；无变异测试/QSS 静态校验/内存基线。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G55-1** | P1 | 全量测试慢（770 条 15-20 分钟；本地裸仓 E2E 占大头） | **全量提速**：本地裸仓测试统一走 `GIT_CONFIG_NOSYSTEM=1` + `file://` + 共享临时裸仓缓存（每进程建一次）；慢测试文件标记 `@slow` 分组，CI 拆 fast/slow 两 job（fast 绿 + slow 独立）；目标全量 ≤10 分钟 | `tests/` `docs/慢测定位.md` `.github/workflows/ci.yml` | 全量耗时前后对比登记；拆分后 CI 绿 |
| `[✅ 已落地]` **G55-2** | P2 | 无变异测试；门禁只看语句覆盖 | **变异测试试点**：对核心纯函数（`url_lib.parse_any_repo_url`/`service._is_networkish_error`/`reports`）跑 mutmut 抽样（单次 ≤5 分钟），击杀率 ≥80% 门禁；CI 加入慢 job（不阻塞 fast） | `tests/test_mutation.py` 或 mutmut 配置 `scripts` | 击杀率报告登记；未击杀变异体人工审为等价物 |
| `[✅ 已落地]` **G55-3** | P3 | `gcm/ui/*.py` E501 长行豁免全量保留 | **E501 治理**：把豁免清单按"文案/超长行"收敛——纯文案行迁移到 `i18n` 字典或 `_L()`；代码行拆行；目标 `gcm/ui` E501 豁免 -50% | `pyproject.toml` `gcm/ui/*.py` | ruff 0 error 保持；豁免计数下降有数据 |
| `[✅ 已落地]` **G55-4** | P3 | 无 QSS 语法校验；主题改坏只在运行时发现 | **QSS 静态校验**：`theme.py` 导出前用小型 QSS 解析器（括号配平/属性名白名单/选择器合法）做静态断言；CI 加一步 | `gcm/ui/theme.py` `tests/test_qss_validate.py` | 单测：合法 QSS 通过/注入坏 QSS 报错 |
| `[待办]` **G55-5** | P3 | 无内存/泄漏基线（长挂机场景） | **内存基线**：`scripts/mem_baseline.py` 建 100 仓库假批次跑引擎，tracemalloc 采样峰值与增长；CI 慢 job 登记基线表；回归超阈值（+20%）红灯 | `scripts/mem_baseline.py` `docs/慢测定位.md` | 命令输出基线表；回归检测逻辑单测 |

> 验收（M12）：全量提速前后对比；mutmut 击杀率数据；QSS 校验/内存基线命令与输出登记；CI 绿。

---

## 15. G56 生态 agent 化：技能包 / MCP / CLI --json / 本地 HTTP API（M13 · v8.6.0）

> 现状：CLI 批量 + Webhook + i18n 已有；无 skills/gcm-*、无 MCP、无 --json、无本地 API。本轮把"批量克隆/归档/报表/诊断/续跑"变成 agent 可调用资产。**全部默认只读 + 显式 enable，外部操作审计落盘。**

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[待办]` **G56-1** | P1 | 无技能包 | **技能包 v1**：仓库内 `skills/gcm-clone/`、`skills/gcm-archive/`、`skills/gcm-report/`、`skills/gcm-diag/`、`skills/gcm-resume/`（每包 SKILL.md + scripts + 验收清单，供 Claude Code/Codex 调用）；技能命令均走现有 CLI（含后续 --json） | 新建 `skills/` | 真实 agent 会话调用成功记录（含命令与输出） |
| `[待办]` **G56-2** | P1 | 无 MCP server | **MCP server**：`gcm/mcp_server.py` 暴露只读工具（list_repos/clone_dry_run? no 默认只读：查询/搜索/报表/状态）+ 显式启用开关（设置页 + 配置 env `GCM_MCP_ENABLE=1`）；MCP 客户端（Claude Desktop/Cursor）可注册 `python -m gcm.mcp_server` | 新 `gcm/mcp_server.py` `pyproject.toml` `settings_panel.py` | MCP 工具冒烟（stdio 握手 + 一次 list_repos）；无写操作默认拒绝 |
| `[待办]` **G56-3** | P2 | CLI 无结构化输出 | **CLI --json**：`python -m gcm --cli --json [urls]` 输出 JSON（每仓 status/action/path/msg + 汇总 + 退出码），保留文本进度；错误统一 schema（code/message/redacted） | `gcm/cli.py` `gcm/app/engine.py` | E2E：真实 3 裸仓 --json 输出 → schema 断言 |
| `[待办]` **G56-4** | P2 | 无本地 HTTP API（其他进程/工具难调度） | **本地 HTTP API（可选实验）**：`--serve 127.0.0.1:port` 只读状态/查询 + POST 任务（显式 `--allow-mutate` 才可写）；绑定 127.0.0.1、无 CORS、token 可选；审计日志 | 新 `gcm/http_api.py` `gcm/cli.py` | 冒烟：GET 状态/查询；默认拒绝写；关闭后端口释放 |
| `[待办]` **G56-5** | P3 | 技能/MCP 与 CLI --json 无统一契约 | **契约文档**：`docs/agent-contract.md`（JSON schema、退出码、错误码、只读/写边界、示例）供第三方 agent 接入 | 新 `docs/agent-contract.md` | 与 --json/MCP 实际输出一致（抽样断言） |

> 验收（M13）：skills 被真实 agent 用成功记录；MCP 工具冒烟；--json schema 断言；HTTP API 只读边界验证；全量绿。

---

## 16. G57 响应式与无障碍落地（M14 · v8.7.0）

> 依据已有的 `计划书/跨端响应式与UX-UI优化计划.md`（策略已定：320/768/1024/1440 语义档、DPI 联动、安全区、键盘/触控），本轮把它从"计划"真正落到代码 + 离屏回归。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G57-1** | P1 | `MainWindow` 无窗口尺寸语义档；窄窗按钮挤压 | **窗口语义档**：`resizeEvent` 防抖 150ms → `_apply_layout_for_width(w)`：S(<768) 工具栏按钮图标化/表格次要列折叠；M(768-1023) 压缩；D(1024+) 全布局；XL(1440+) 列宽拉伸；统计中心双栏/单栏切换 | `gcm/ui/main_window.py` `gcm/ui/statistics_dialog.py` | 离屏：三档 resize 断言可见性变化；窄窗按钮不截断 |
| `[✅ 已落地]` **G57-2** | P2 | DPI≥150% 字号无下限联动 | **DPI 联动字号**：`font_scale` 下限在 DPI≥150% 自动抬到 1.0（`qss_for_scale` 既有缩放机制）；设置页示例卡片实时预览 | `gcm/ui/theme.py` `gcm/ui/settings_panel.py` | 单测：DPI 参数 → 下限正确；截图登记 |
| `[✅ 已落地]` **G57-3** | P2 | 键盘 Tab 遍历部分可达（G46-12 已初始化） | **键盘全遍历**：补齐托盘/统计/详情/帮助对话框 Tab 顺序 + Esc 焦点还原 + 焦点环可见；`QAccessible` 名称补齐（按钮/表格/分组） | `gcm/ui/*.py` | 离屏：Tab 顺序断言；QAccessible name 非空抽样 |
| `[✅ 已落地]` **G57-4** | P3 | 触控目标部分 <48px | **触控尺寸**：`settings_panel`/主工具栏关键按钮 `setMinimumHeight(44-48)`；表格触摸滚动验证（`QAbstractScrollArea` 默认） | `gcm/ui/*.py` | 离屏断言 minHeight；截图登记 |
| `[待办]` **G57-5** | P3 | 无明确 AI 可访问性声明 | **可访问性声明**：README/docs 增加「无障碍」段（快捷键清单、键盘路径、主题/字号、已知限制）；排错地图含无障碍入口 | `README.md` `gcm/ui/help_dialog.py` | 文档与实现一致 |

> 验收（M14）：三档 resize 离屏全绿；键盘/触控断言绿；截图登记；全量无回归。

---

## 17. G58 网络 / 镜像 / 新平台深化（M15 · v8.8.0）

> 现状：镜像前缀静态、并发自适应无持久记忆、新 host 需 custom_hosts 白名单。本轮聚焦"更懂网络、更自动化、更少手动配置"。

| 条目 | 优先级 | 现状（代码依据） | 方案 | 落地文件 | 验收标准 |
|------|--------|------------------|------|----------|----------|
| `[✅ 已落地]` **G58-1** | P1 | GitHub API 限流（`gh_api.py`/`updater.py`）等待重试粗糙 | **API 限流友好**：统一 `RateLimitState`（余量/重置时间读取响应头，403/429 退避）；Star 导入/更新检查/体检共用；设置页显示当前限流余量 | 新 `gcm/app/rate_limit.py` `gh_api.py` `updater.py` | 单测：头解析/退避/共享状态；真实 API 一次登记 |
| `[✅ 已落地]` **G58-2** | P2 | 镜像前缀静态一条；坏镜像只能手动换 | **镜像源测速与自动择优**：设置页「镜像源」多行（含常用 ghproxy 列表）；克隆前并发测速（HEAD 小请求）→ 自动选最快或按可用降级；失败自动换下一镜像；结果可回写排序 | `gcm/app/diag.py` `gcm/git/service.py` `settings_panel.py` | 单测：测速/择优/降级排序正确；真实测速一次登记 |
| `[✅ 已落地]` **G58-3** | P3 | 代理支持用户名/密码不完整 | **HTTP 代理认证**：代理设置支持 `http://user:pass@host:port` 解析与安全落盘（加密，参考 token 方案）；`_env` 注入对应 env | `gcm/app/proxy.py` `gcm/db/settings.py` `gcm/git/service.py` | 单测：解析/加密/注入正确；无明文落盘 |
| `[✅ 已落地]` **G58-4** | P3 | 新 host（如 huggingface/gitea 实例）需手动 custom_hosts | **host 探测**：自定义主机输入后一键探测（HTTPS 可达/是否 Git 服务/git ls-remote 匿名可读）→ 自动建议加入白名单并选认证头类型（Bearer/PRIVATE-TOKEN/Basic，缓解 D-4） | `gcm/app/diag.py` `settings_panel.py` `gcm/git/service.py` | 单测：探测结果到建议映射正确；真实一次登记 |
| `[待办]` **G58-5** | P3 | 自建 GitLab/Gitea 认证头类型无 UI 选择（D-4 风险） | **认证头类型选择**：host_tokens 编辑器加「头类型」下拉（Bearer/PRIVATE-TOKEN/Basic），`_token_for_host` 按映射发对应头 | `gcm/db/settings.py` `gcm/git/service.py` `settings_panel.py` | 单测：映射正确；E2E 自建实例可选（可用本地 gitea 容器或 mock） |

> 验收（M15）：限流/镜像择优/代理认证/头类型离屏全绿；真实诊断报告一次登记；D-4 关闭。

---

## 18. G66 补丁区（缺陷修复登记，编号顺延）

> 任何实施过程中发现的 Bug（非新功能）先在此登记，**先修后进**，必须带测试；修复后本表登记。

| 条目 | 优先级 | 现状（代码/证据） | 修复方案 | 验收标准 |
|------|--------|--------------------|----------|----------|
| `[待办]` **G66-1** | P1 | （由实施方登记：缺陷描述 + 复现路径 + 根因） | 最小修复 + 回归测试 | 单测/RED/GREEN 证据 + 全量绿 |
| `[待办]` **G66-2** | P1 | （同上） | 同上 | 同上 |

---

## 附录 A（第二轮）：里程碑检查表

| 里程碑 | 状态 | 全量测试 | 覆盖 ≥84 | ruff 0 | mypy 0 | E2E 绿 | 验收清单登记 |
|--------|------|----------|----------|--------|--------|--------|---------------|
| M8 v8.1.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M9 v8.2.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M10 v8.3.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M11 v8.4.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M12 v8.5.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M13 v8.6.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M14 v8.7.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M15 v8.8.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| M16 v9.0.0 | 待办 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

## 附录 B（第二轮）：TDD 执行规约（沿用 + 补充）

1. 先写测试再实现：RED（测试名 + 首次失败输出摘要）→ GREEN（最小实现）→ IMPROVE（重构 + 补断言），验收清单登记三阶段证据。
2. 覆盖纪律：engine/service/repo_db/settings/url_lib 新增保覆盖；UI 条目默认带离屏 QTest；覆盖率下降即红灯。
3. 全量回归：每里程碑跑 coverage 全量；单文件独立可跑是默认要求（G50-1 立规）；M12 落地后目标全量 ≤10 分钟。
4. 禁止假实现：mock 只用于外部边界（网络/时钟/对话框/进程）；业务逻辑真实执行；E2E 用本地裸仓，不依赖外网。
5. 证据登记：每条 ✅ 附验证命令 + 输出摘要写入 `验收清单.md`；无证据不标完成。
6. 版本节奏：每里程碑发 semver 版本；`__changelog__` 追加一行；Release 按 `docs/RELEASE_CHECKLIST.md`。
7. 付费 API/外部操作红线：默认只读；显式开启；审计落盘；MCP/HTTP API 默认拒绝写。
8. Windows 规则：不新增 `.sh`（用 PowerShell/node）；批处理 CRLF（.gitattributes 强制）。
9. 测试隔离铁律：任何直接构造 MainWindow/QWidget 的测试，模块级先行 `QApplication.instance() or QApplication([])`。

## 附录 C（第二轮）：关键代码定位表（2026-09-25 核对）

| 关注点 | 文件:行 | 备注 |
|--------|---------|------|
| 版本/changelog | `gcm/__init__.py:2,4-73` | v8.0.2 已含（changelog 追加至 v8.0.2） |
| 入口/数据目录 | `gcm/__main__.py` | G49-2 `--portable`/G49-3 `--cli` 已落地 |
| 引擎（并发/流控/进度/收尾） | `gcm/app/engine.py`（651 行） | `launch:434` `_on_worker_done:581` `_release_tasks:598` `drain:612` `shutdown:636` `_emit_line:186`；G43-1/2/5 已落地 |
| git 服务 | `gcm/git/service.py`（1020 行） | `sync:645` `_clone:735` `_update:847` `_env:462` `_token_for_host:525` `_mirror_url:539` `_precheck_reachable:554`；G48 已落地 |
| URL 解析 | `gcm/app/url_lib.py`（444 行） | `parse_urls:370` `parse_any_repo_url:290` `apply_mirror_prefix:431` `is_supported_host:89`；G43-8 已落地 |
| SQLite | `gcm/db/repo_db.py`（785 行） | `_migrate:56` `BEGIN IMMEDIATE:118` `set_note:237` `stats_daily:415` `stats_slow_repos:469` `backup_to:535`；G47 已落地 |
| 设置 | `gcm/db/settings.py`（field 区域） | `language:185` `host_tokens:193`；加密 `_encrypt_token:108` `_decrypt_token:135`；G38-1/G44 已落地 |
| 主窗口 | `gcm/ui/main_window.py`（1432 行） | `_build_download_tab:385` `_build_manage_tab:496` `_launch:1083` `_on_worker_result:1262` `_on_engine_finished:1272` `_run_selftest:557` `closeEvent:1389`；G45-6 拆分后维持 ~1432 |
| 崩溃恢复（G22-2） | `gcm/ui/main_window.py:153` | 现状=回填输入区；G52-2 升级为续跑清单 |
| 主题/LogModel | `gcm/ui/theme.py`（441 行） | `TOKENS:115` `qss_for_scale:309` `apply_theme:318` `LogModel:364`（deque 已落地 G43-1） |
| 网络诊断 | `gcm/app/diag.py` | G48-8 已落地；G54-3/G58-2/4 扩展点 |
| CLI | `gcm/cli.py` + `gcm/__main__.py` | G49-3 已落地；G56-3 `--json` 扩展点 |
| 统计中心 | `gcm/ui/statistics_dialog.py`（259 行） | G47 已落地；G53 评分卡/看板扩展点 |
| 报表 | `gcm/reports.py` | G47-3 已落地；G53-5 定时导出扩展点 |
| 打包 | `Git-clone-Max.spec`（已入库） | G43-4 瘦身已落地；G43-4 体积复核（M16） |
| CI | `.github/workflows/ci.yml` | 门禁 84(Windows)/83(POSIX)；三平台 + e2e-smoke；G55-1 fast/slow 拆分扩展点 |
| 扫描提速 | `gcm/app/scanner.py` `gcm/ui/local_repos_dialog.py` `scripts/bench_scan.py` | v8.0.2 P-perf 95x 已落地；本表登记 |

## 附录 D（第二轮）：当前已知风险与待验证项

| 编号 | 风险 | 等级 | 说明与处置 |
|------|------|------|-----------|
| D-11 | 覆盖率门禁临界：本机 84% 与 Windows 门禁 84 持平，任何新码不补测即红灯 | P0 | M9/M10/M13 大改动前先补对应测试；M8 建立权威文档后基线重测登记 |
| D-12 | `main_window.py` 1432 行仍偏大（G45-6 后新增回调） | P1 | G57/G54 新 UI 逻辑优先独立模块（failure_panel/onboarding_checklist 等） |
| D-4（延续） | `_env()` 未知 host Bearer 头 vs PRIVATE-TOKEN 无 UI 选择 | P2 | G58-5 提供头类型下拉后关闭 |
| D-13 | MCP/HTTP API 若实现权限边界不清可能暴露写操作 | P1 | G56-2/4 强制默认只读 + 显式 enable + 审计；合并 code-reviewer/security-reviewer 复审 |
| D-14 | 全量测试 15-20 分钟反馈慢（M12 前） | P1 | G55-1 fast/slow 拆分先行；`docs/慢测定位.md` 基线复用 |
| D-15 | README「版本」段落 / 本指南基线仍含 v8.0.1 旧数字 | P2 | G51-2 与 v8.0.2 对齐；发布 v9 前必须复核 |
| D-16 | `gcm/ui/*.py` E501 豁免残留（纯文案长行） | P3 | G55-3 收敛一半后再评是否保留豁免 |

---

---

## 19. G59 第三轮：生态与架构资产（v9.1.0 候选，2026-09-26 登记）

> 第二轮 M8-M16 已收官。第三轮聚焦「资产沉淀 + 生态增长 + 性能补位」，文档资产已先行交付，代码项按此推进。

| 条目 | 优先级 | 现状 | 方案 | 状态 |
|------|--------|------|------|------|
| `[✅ 已落地]` **G59-1 架构资产** | P2 | 无架构总览/ADR | `docs/architecture.md`（分层/数据流/模块基线）+ `docs/adr/ADR-001..004` | 已交付 |
| `[✅ 已落地]` **G59-2 黄金代码范例** | P3 | 无复用范式沉淀 | `docs/code-gems.md`（8 个范式：单写者/原子写/redact/批聚合/流控/注入时钟/契约/纯校验） | 已交付 |
| `[✅ 已落地]` **G59-3 产品 brief** | P3 | 无定位/增长文档 | `docs/product-brief.md`（定位/用户/边界/增长 ROI 排序） | 已交付 |
| `[✅ 已落地]` **G59-4 性能与可观测性评估** | P2 | 无高并发边界文档 | `docs/perf-observability.md`（现状/限制/SaaS 架构建议/桌面补位） | 已交付 |
| `[待办]` **G59-5 CI 主矩阵 fast/slow 拆分** | P2 | fast-smoke 扩展已加，主矩阵仍全量 | 按 docs/慢测定位.md 慢文件清单拆 fast/slow 双 job，主 job 只跑非慢集 | 待实施 |
| `[待办]` **G59-6 大仓库 dir_size 异步化** | P3 | 评分卡同步计算 dir_size 可能慢 | 统计中心评分卡尺寸计算转后台线程 + 缓存 | 待实施 |
| `[待办]` **G59-7 企业 IM 告警推送** | P2 | 告警仅通知/Webhook 通用 | 加钉钉/飞书/企微 webhook 模板（复用 G53-2） | 待实施 |

## 封底

> **本指南（第二部分：第二轮迭代总纲）为 v8.0.2 → v9.x 的唯一事实源。**
> 实施方按 M8→M16 顺序推进；每条 TDD + 证据登记；主控只看 `计划书/验收清单.md`。
> 配套权威文件（M8 建立）：`计划书/db_structure.md`（数据库结构）· `计划书/project_specs.md`（任务进度）。
> 历史 v8 总纲（第一轮 G31-G50）保留于本文件第一部分，第二轮完成后归档为 `计划书/下一步改进指南-v8-历史.md`（只读）。
> 生成：2026-09-25 · 基线 HEAD `4429c32` · `__version__ 8.0.2` · 全量 770 绿 / 覆盖 84% · 三平台 CI 绿。

---

