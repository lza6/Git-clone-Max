# Git-clone-Max 《下一步改进指南》

> **目标版本：v8.x · 旗舰版（本指南为 v7.0 → v8.0 迭代基线）**
> **基线版本：v6.7.0（2026-09-15 本机真实运行实测）**
> **适用实施方：任意 AI 编码代理（Builder）**
> **配合文档：`计划书/规划总览.md`（目录/里程碑/验收规约）· `计划书/验收清单.md`（证据登记）**
>
> 全指南 60+ 项改进，按模块 G31–G40 分组，编号稳定、逐条可验收。每条均给出：**问题现状（附代码依据）→ 改进方案 → 落地文件 → 验收标准 → 优先级**。
>
> 优先级标记：**P0** 必须（不发版缺陷） · **P1** 高优先（显著体验/能力） · **P2** 中（增强） · **P3** 可选。
> 状态标记：`[待办]` → 实施方完成后改为 `[✅ 已落地] + 一条证据`。

---

## 目录

- [G31 现状基线（实测）与版本规划](#g31-现状基线实测与版本规划)
- [G32 已落地登记（v6.7 继承，禁止重复实施）](#g32-已落地登记v67-继承禁止重复实施)
- [G33 可靠性补全（小步快速见效）](#g33-可靠性补全小步快速见效)
- [G34 性能与资源治理](#g34-性能与资源治理)
- [G35 核心交互进阶（批处理工作台极致化）](#g35-核心交互进阶批处理工作台极致化)
- [G36 UI/UX 升维（小白友好与现代化）](#g36-uiux-升维小白友好与现代化)
- [G37 数据能力纵深（洞察与决策）](#g37-数据能力纵深洞察与决策)
- [G38 网络与同步前沿](#g38-网络与同步前沿)
- [G39 安全纵深加固](#g39-安全纵深加固)
- [G40 工程与发布体系](#g40-工程与发布体系)
- [G41 长期演进（i18n / Portable / 生态）](#g41-长期演进i18n--portable--生态)
- [附录A 里程碑路线与门禁](#附录a-里程碑路线与门禁)
- [附录B TDD 执行规约（每条必守）](#附录b-tdd-执行规约每条必守)
- [附录C 关键代码定位表（v6.7.0 实测行号）](#附录c-关键代码定位表v670-实测行号)

---

## G31 现状基线（实测）与版本规划

### 31.1 实证基线（2026-09-15 本机真实运行）

| 维度 | 实测现状 | 证据来源 |
|------|----------|----------|
| 版本 | `6.7.0`（`__changelog__` 覆盖 4.0.0 ~ 6.7.0 共 22 个版本） | `gcm/__init__.py:2` |
| 运行时 | Python 3.14.3 + PyQt6 6.11.0 | 本机 `.venv` 实测 |
| 测试 | **360 用例全绿**（96.7s） | `python -m unittest discover -s tests` → `Ran 360 tests OK` |
| 覆盖率 | **TOTAL 81%**；弱项：`__main__` 42%、`single_instance` 59%、`main_window` 75%、`manage_model` 73%、`service` 80%、`settings` 78% | `coverage report` 实测（本机） |
| 代码规模 | 3710 stmts 核心代码；`main_window.py` 1514 行（最大文件） | `wc -l` 实测 |
| 静态检查 | `ruff check gcm` → **All checks passed!**（零 error）；mypy 配置通过 | 本机实测 |
| 打包 | onefile exe 98.3MB + onedir portable zip 196MB 双产物 | `dist/` 实测 |
| 引擎 | SyncEngine（QThreadPool ≤32）去重/进度周期落盘/取消缓存/drain(8s) 优雅收敛 | `gcm/app/engine.py`（444 行） |
| 数据库 | SQLite WAL + `BEGIN IMMEDIATE` 显式写事务；repos + sync_history 表 | `gcm/db/repo_db.py:86,103` |
| 安全 | token DPAPI/Keyring/XOR 加密落盘 · host 白名单注入 · `redact()` 全链路打码 · single-instance | `gcm/db/settings.py` `gcm/util/redact.py` |
| 日志 | 结构化 `app.log`（RotatingFileHandler 5MB×3）+ error.log 崩溃兜底 | `gcm/app/applog.py` |

### 31.2 版本规划（v7.x → v8.0）

| 版本 | 聚焦 | 说明 |
|------|------|------|
| v7.0 | M1 可靠性补全 + M2 性能 | G33 + G34 主条目 |
| v7.1 | M3 交互进阶 | G35 主条目 |
| v7.2 | M4 体验升维 | G36 + G37 主条目 |
| v7.3 | M5 网络与安全 | G38 + G39 |
| v7.4 | M6 工程与发布 | G40 + G41 主条目 |
| v8.0 | 收官验收 | 全模块回归 + 发布清单走查 + Release 双产物 |

---

## G32 已落地登记（v6.7 继承，禁止重复实施）

> 以下条目在 v6.7.0 已真实落地并有测试/文件证据。**实施方不得重复实现**，只需在验收清单登记"继承已落地"。

| 条目 | 优先级 | 落地证据（代码/文件） | 对应测试 |
|------|--------|----------------------|----------|
| G21-1 暂停排序错行修复 | P0 | `main_window.py:1182-1194` 行内存 engine index（`UserRole+1`） | `tests/test_v7_fixes.py:73,95` |
| G21-2 进度落盘双路径统一 | P0 | `engine.py` `_mark_done` 单路径 | `tests/test_v7_fixes.py:188` |
| G21-3 更新检查单次重试+留痕 | P0 | `updater.py:50-86` sleep(2) 重试；`main_window.py:849` app.log 留痕 | `tests/test_updater_retry.py` |
| G21-4 启动器退出码透传 | P0 | `scripts/启动Git-clone-Max.bat` 末尾 `exit /b %EXIT_CODE%` | `tests/test_launcher.py` |
| G22-1 closeEvent drain 收敛 | P0 | `main_window.py:1473-1494` `engine.drain(8000)` | `tests/test_v7_fixes.py:148,166` |
| G22-2 in_progress 全生命周期 | P0 | `engine.py:126-137,321,373`；`main_window.py:156-178` 崩溃恢复预填 | `tests/test_v7_fixes.py:188,232,250` |
| G22-3 BEGIN IMMEDIATE 写事务 | P1 | `repo_db.py:103`；`settings.py` WAL | `tests/test_v7_reliability.py:38,63` |
| G22-4 托盘 tooltip 进度 | P1 | `tray.py:57` `update_counts` + 300ms 节流 | `tests/test_tray_progress.py` |
| G22-5 结构化 app.log | P1 | `gcm/app/applog.py` RotatingFileHandler 5MB×3 | `tests/test_applog.py` |
| G22-6 failed 子集 + 启动重试回填 | P1 | `engine.py:373-379`；`main_window.py:165-177` | `tests/test_v7_fixes.py:211` |
| G22-7 重试文案含次数/原因/秒数 | P2 | `service.py` `run_git_ui` 超时/网络重试文案 | `tests/test_v7_reliability.py:91` |
| G28-1 redact 全链路打码 | P1 | `gcm/util/redact.py`（12 stmts 100% 覆盖） | `tests/test_redact.py` |
| G23-7 onefile+onedir 双产物 | P3 | `Git-clone-Max.spec` COLLECT；`dist/` 含 exe + portable.zip | `scripts/publish_release.py:33-56` |
| G29-1 CI 覆盖率门禁 | P0 | `.github/workflows/ci.yml:45` `--fail-under=82` | CI 配置 |
| G29-2 ruff/mypy 静态检查 | P1 | `pyproject.toml` `[tool.ruff]`/`[tool.mypy]`；CI lint 步骤 | 本机 `ruff check gcm` 零 error |
| G29-6 CI 模块级覆盖率 summary | P2 | `.github/workflows/ci.yml:56-72`（json→markdown） | CI 配置 |
| G29-7 发布清单 | P2 | `docs/RELEASE_CHECKLIST.md`（前置/打包/冒烟/发布/复验） | 文档 |

> 注：`singleShot` 行尾注释漂移（`main_window.py:155` 把 G22-2 注释挂在 `_check_update_silent` 行尾）——实施方顺手修正，属 G33-8。

---

## G33 可靠性补全（小步快速见效）

> 全部 P2，单条 ≤ 1 小时，不碰 engine/worker/repo_db 闭环核心，纯增量。

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G33-1** | P2 | `local_repos_dialog` 构建行时不查 DB，已入库仓库仍可选 | 构建行时查 `db.get_repo(owner,repo,host)`；已有 → 行禁用 + "已入库"后缀；工具栏加"仅显示未入库"checkbox | `gcm/ui/local_repos_dialog.py`（230 行） | 单测：已存在仓库灰标；过滤后可见行数正确 |
| `[待办]` **G33-2** | P2 | `import_selected` 二次导入覆盖原 local_path 无保护 | `import_selected` 前查 `get_repo`，存在则跳过除非勾选"覆盖路径"；补回归测试锁行为 | `local_repos_dialog.py` | 回归测试：二次导入不覆盖原 local_path |
| `[待办]` **G33-3** | P2 | 无"备份数据库"入口，用户数据无手动快照通道 | 管理页工具栏「备份数据库」：`repos.db → backups/repos-<ts>.db`（shutil.copy2 前先 `wal_checkpoint(TRUNCATE)` 保证一致） | `main_window.py`（管理页工具栏）`repo_db.py`（`backup_to` 方法） | 备份文件可被 `Database()` 打开且 count 一致 |
| `[待办]` **G33-4** | P2 | `run_git_ui` 取消路径：读线程 join 无兜底核查；`_kill_tree` 后读线程可能滞留 | 核查取消/超时路径：kill 后读线程 `join(2)`，超时置 daemon 不再等待；补"取消路径读线程退出"单测 | `gcm/git/service.py:290-300`（`pump.join(timeout=5)` 处） | 单测：取消时读线程 2s 内退出；句柄无泄漏（进程数恢复） |
| `[待办]` **G33-5** | P2 | `sync_history` 无限增长（每次同步 1 行，挂机一年十万行级），`stats_overview` 全表扫描变慢 | `add_sync_history` 后异步清理：保留每仓库最近 200 条 + 全局 90 天窗口（事务内 DELETE）；触发频率每 100 次同步执行 1 次 | `gcm/db/repo_db.py`（`_maybe_prune_history`） | 单测：写入 300 条旧记录 + 清理后只剩窗口内；`stats_overview` 耗时回归 |
| `[待办]` **G33-6** | P2 | 启动耗时未产品化：无"启动性能"自检输出 | `_run_selftest`（`main_window.py:645`）增加启动耗时测量：PyQt 导入→窗口 show 耗时写 app.log，异常（>3s）时提示 | `main_window.py` `gcm/app/applog.py` | selftest 输出含启动耗时；>3s 有提示 |
| `[待办]` **G33-7** | P2 | `settings.py:131` 注释漂移："并行线程数 1..16" 与实际 32 不符 | 注释修正为 `1..32`；全局扫查同类注释漂移（`main_window.py:155` 单行尾注释错挂） | `gcm/db/settings.py:131` `main_window.py:155` | 无"1..16"残留；注释与实际一致 |
| `[待办]` **G33-8** | P2 | `main_window.py` 1514 行单类；维护热点（G21-1 即实证） | **只做提取不做重写**：`_build_settings_tab`（~190 行）提取为 `gcm/ui/settings_panel.py`（QWidget + 信号回传）；`_emit_log/_flush_log_batch` 提取为 `gcm/ui/log_buffer.py`；行为零变化 | `main_window.py` → 新 `settings_panel.py`/`log_buffer.py` | 全量回归绿；`main_window.py` ≤ 1300 行；覆盖率不降 |

---

## G34 性能与资源治理（长时运行软肋）

> 冷启动实测不是瓶颈（MainWindow 构造仅 0.10s）；真正的风险在**长时挂机**与**大批量任务**。
> 本模块全部条目要求"先测量后优化"，每条必须附 before/after 数据才算验收。

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G34-1** | P1 | 日志双通道无上限协同：UI 环形缓冲 3000 条（`theme.py:187` LogModel 列表裁剪 O(n)）+ 引擎 `_emit_line` 对每个 worker 每行 git 输出都发 Qt 信号（32 并发大仓克隆信号风暴） | ① `LogModel._entries` 改 `deque(maxlen=3000)`（O(1) 裁剪）；② 引擎侧 git 输出行加 250ms 合并窗口（按 index 聚合一次 emit 多行）；③ 批量 >100 时 UI 日志区自动切"紧凑模式"（仅状态变化行） | `gcm/ui/theme.py`（LogModel）`gcm/app/engine.py`（`_emit_line` 节流）`main_window.py` | 32 并发大仓 UI 不卡（人工体验 + 计时）；`cProfile` 信号处理耗时占比下降有数据 |
| `[待办]` **G34-2** | P1 | `_retire_task` 只保留 32 个退役 worker 引用，但 specs/flags 在任务全部结束才清；单批次 500 仓库时 QThreadPool 队列同时持有 500 个 payload | 新增**批次流控**：`launch()` 去重后 >64 只入队前 64，完成一个由 `_on_worker_done` 从待发队列补位（惰性补投）；进度表仍显示全部 500 行（PENDING 态） | `gcm/app/engine.py`（`launch`/`_on_worker_done`） | E2E：600 仓库批次峰值内存较改造前下降（tracemalloc 采样）；全部完成且结果齐全 |
| `[待办]` **G34-3** | P2 | `sync_history` 无 duration 维度利用（duration_ms 已存） | 统计中心加「平均耗时 Top10 慢仓库」列表（帮助定位网络/大仓问题） | `gcm/ui/statistics_dialog.py`（72 行）`repo_db.py`（`stats_slow_repos`） | Top10 排序正确；空库占位文案 |
| `[待办]` **G34-4** | P3 | exe 单文件 ~98MB 每次启动解包；portable 196MB 偏大 | 发布体系优化：onedir 便携版压缩参数调优（upx=True 已开）+ 排除冗余 PyQt 翻译/插件（`excludes` 列表）；README 登记两产物启动耗时对比 | `Git-clone-Max.spec` `scripts/publish_release.py` | zip 体积下降有数据；portable 启动 < 2s（人工实测登记） |

---

## G35 核心交互进阶（把批处理工作台做到极致）

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G35-1** | P1 | 失败仓库重跑需重新粘贴；G22-6 只做"启动时"提示，运行中失败无法就地重试 | 进度表状态列右键菜单：「重试此仓库」（FAILED/CANCELLED 行可用，经 `_launch` 统一调度单仓库重跑）；「复制错误详情」「打开所在目录」 | `main_window.py`（`setContextMenuPolicy(CustomContextMenu)` + `customContextMenuRequested`） | 单测：FAILED 行右键重试后该行状态回到 RUNNING→SUCCESS |
| `[待办]` **G35-2** | P1 | 结果反馈纯视觉，无提示音/系统通知闭环；挂机用户不知"跑完了"（托盘 finished 已桥接但无声音） | 引擎 finished 时：设置页「完成提示音」开关（默认开）→ `QApplication.beep()`；托盘通知保留（已落地） | `main_window.py`（`_on_engine_finished:1351` 处）`db/settings.py` | E2E：mock 开关关闭后 beep 不调用；开启后调用 |
| `[待办]` **G35-3** | P1 | 输入区只支持粘贴；批量场景（从文件管理器拖一堆 URL 文件/文件夹）无通道 | 输入区 `setAcceptDrops(True)`：拖入 `.txt/.csv` → 读入每行地址；拖入文件夹 → scanner 扫描导入 | `main_window.py`（`dropEvent`/`dragEnterEvent`） | 单测：拖入 txt 内容并入输入框；拖入目录触发扫描对话框 |
| `[待办]` **G35-4** | P1 | 无「任务清单」持久化（savelist 从未实现，grep 确认） | 新 `gcm/db/lists.py`（JSON 存 `data/lists/*.json`）：保存当前输入区为命名清单、清单下拉一键载入、可删除；文件可分享 | 新 `gcm/db/lists.py` `main_window.py` | 保存/加载/删除/分享 E2E；清单格式文档化在 README |
| `[待办]` **G35-5** | P2 | `@tag` 只在 URL 语法级支持；详情页无克隆命令复制 | 详情页「复制克隆命令」按钮：`git clone -b <ref> <url>`（有 ref 时带 `-b`，否则纯 clone） | `gcm/ui/repo_detail_dialog.py`（194 行） | 复制内容含 `-b` 与正确 ref |
| `[待办]` **G35-6** | P2 | 管理页统计行无 host 分布（G01-7 遗留） | 管理页顶部统计标签追加 `github: n · gitlab: m · gitee: k`（来自 `list_repos()` 内存聚合，不新增 SQL） | `gcm/ui/manage_model.py`（211 行） | UI 测试断言统计文案含 host 计数 |
| `[待办]` **G35-7** | P2 | 快捷填充是 6 个写死的 GitHub 仓库按钮（`main_window.py:250-257`） | 抽到 settings.json `quick_repos`（默认现值），设置页可编辑列表 | `main_window.py` `gcm/db/settings.py` | 修改设置后按钮变化持久化 |
| `[待办]` **G35-8** | P2 | 详情列已设 tooltip（`main_window.py:1301`），但状态格无 tooltip | 状态格 tooltip = message+detail 拼接 | `main_window.py`（`_on_worker_result`） | 单测：tooltip 含 detail 全文 |
| `[待办]` **G35-9** | P3 | 无批量操作 | 管理页多选批量：更新所选/删除所选/打标签/导出所选（CSV）；一键更新尊重 excluded（已有） | `main_window.py`（管理页）`repo_db.py` | 批量动作 E2E 全通过 |
| `[待办]` **G35-10** | P3 | 无「GitHub Star 列表导入」 | 新 `gcm/app/gh_api.py`：`GET /users/{u}/starred?per_page=100`（Link header 分页，匿名 60 次/h；token 则 5000）；设置页入口 | 新 `gcm/app/gh_api.py` `main_window.py` | 单测 mock 分页两页 30 仓库全部入输入区 |

---

## G36 UI/UX 升维（小白友好与现代化）

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G36-1** | P1 | 首次运行零引导：小白面对三 Tab 无从下手 | 新 `gcm/ui/onboarding.py`：首次启动（settings 无 `first_run_done`）显示 3 步向导 Dialog（选下载目录→选模式→完成）；跳过也写标志 | 新 `gcm/ui/onboarding.py` `main_window.py`（show 后判断） | 单测：首次显示/完成后不再显示；跳过也写标志 |
| `[待办]` **G36-2** | P1 | 状态仅文字颜色区分（`_on_worker_result` status_map 仅前景色），色弱用户不可达 | 状态列追加符号后缀：成功 ✓ 失败 ✕ 冲突 ⚠ 取消 ⊘ 跳过 →（颜色+形状双通道）；QSS 对比度 ≥ 4.5:1 | `main_window.py:1265-1273`（status_map）`theme.py` | 单测断言状态文本含符号 |
| `[待办]` **G36-3** | P1 | 按钮 emoji 图标（▶⏹📂📄）跨平台渲染不一致；无统一图标体系；无 exe 图标 | QStyle.StandardPixmap 为主（零资源）+ 自绘关键图标；托盘正式图标 `assets/icon.ico`（spec `icon=` 参数）；emoji 作回退 | `gcm/ui/theme.py`（icon helper）`Git-clone-Max.spec` | Windows 资源管理器 exe 显示图标；窗口/托盘图标一致 |
| `[待办]` **G36-4** | P2 | 主窗口 1240×840 固定（`main_window.py:192`），小屏笔记本溢出 | 改 1280×800 最小 + 记忆上次窗口尺寸（settings.geometry）；`showMaximized` 状态记忆 | `main_window.py` `db/settings.py` | 重启恢复上次尺寸；1080p 无滚动条 |
| `[待办]` **G36-5** | P2 | 无空状态引导：进度表空时白板、管理页空时白板 | QTableWidget 空态 overlay QLabel："粘贴仓库地址开始 ↓ / 管理页导入本地仓库 →"；主题联动 | `main_window.py`（resizeEvent 定位 overlay） | 空态显示、有数据隐藏；主题切换颜色正确 |
| `[待办]` **G36-6** | P2 | 进度条无完成动效；结果出现瞬变（grep 无 QPropertyAnimation） | 完成行 QPropertyAnimation 背景色 1.2s 消隐（成功绿/失败红）；设置页"动效"开关（默认开，offscreen/低配自动关） | `main_window.py`（`_on_worker_result`）`db/settings.py` | 离屏可测：动画结束状态值正确；关开关后无动画实例 |
| `[待办]` **G36-7** | P2 | 设置页 20+ 控件平铺（已分组滚动），无搜索定位 | 设置页顶部过滤框：输入"代理"仅网络分组可见（QGroupBox 标题匹配 + 行内匹配双层） | `gcm/ui/settings_panel.py`（G33-8 提取后在此实现） | 单测：输入"代理"仅网络分组可见 |
| `[待办]` **G36-8** | P3 | 无全局热键（仅 Ctrl+=/-/0 字号已有） | QShortcut：Ctrl+Alt+S 开始/取消、Ctrl+Alt+U 一键更新、Ctrl+Alt+M 显示窗口；设置页开关 | `main_window.py` | 单测：QTest 触发快捷键对应动作 |
| `[待办]` **G36-9** | P3 | 无「跟随系统深浅色」 | Windows 注册表 `HKCU\...\Themes\Personalize AppsUseLightTheme` 读取 + `QEvent.ApplicationPaletteChange` 监听自动切 deep/light | `gcm/ui/theme.py` `main_window.py` | 单测 mock 注册表值切换主题 |

---

## G37 数据能力纵深（洞察与决策）

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G37-1** | P1 | 统计中心只有全局聚合数字（`statistics_dialog.py` 72 行），无趋势 | 统计中心加「最近 30 天同步趋势」：`sync_history` 按日聚合成功/失败双序列，QPainter 自绘 30 根柱（纯 Qt 无图表库依赖） | `gcm/ui/statistics_dialog.py` `repo_db.py`（`stats_daily(days=30)`） | 空库占位文案；30 天聚合数字与 SQL 手查一致 |
| `[待办]` **G37-2** | P1 | 仓库详情统计有次数/成功失败冲突/平均耗时，但无「与远端差异」实时化 | 详情页加「检查远端」按钮：单仓库 `git ls-remote origin` 后台线程比较 head_sha vs remote_sha → ahead/behind 展示（复用 `_divergence_info`） | `gcm/ui/repo_detail_dialog.py` | E2E：真实裸仓库远端多 1 提交 → 显示 behind 1 |
| `[待办]` **G37-3** | P2 | 报表导出是全量快照（`reports.py` 40 行），无"增量/筛选导出" | 报表对话框加筛选：日期范围 + host + 状态多选，按筛选出 CSV/MD | `gcm/reports.py` `main_window.py` | 筛选导出行数与 SQL 一致 |
| `[待办]` **G37-4** | P2 | 无「同步计划任务」；watch 模式一直未做 | 设置页「自动更新间隔（分钟，0=关）」：QTimer 触发 `update_all`（仅 engine 非 busy 时）；下次执行时间在状态栏可见 | `main_window.py` `db/settings.py` | E2E：mock QTimer 触发后 update_all 被调用；busy 时跳过并顺延 |
| `[待办]` **G37-5** | P3 | 「用量统计」未落地 | `stats_overview` 已有数据，统计中心顶部补"累计克隆 X 次 · 累计更新 Y 次 · 累计新增提交 Z" | `gcm/ui/statistics_dialog.py` | 数字与 SQL 一致 |
| `[待办]` **G37-6** | P3 | 无每仓库备注 | repos 表加 `note TEXT DEFAULT ''`（沿用 user_version 迁移链）；详情页可编辑 | `gcm/db/repo_db.py`（迁移 0002）`repo_detail_dialog.py` | 迁移不丢数据（旧库升级 E2E）；备注持久化 |

---

## G38 网络与同步前沿

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G38-1** | P1 | token 只发 github.com（F1 加固正确但保守）：GitLab/Gitee 私有仓库无法认证 | **按 host 映射凭据**：settings 新增 `host_tokens: dict[str,str]`（每 host 一个 token，独立加密复用 `_encrypt_token`）；`GitService._env`（`service.py:379-407`）按 `_current_host` 查表注入 `http.extraHeader`（GitLab 用 `PRIVATE-TOKEN` 头）；安全回归：host 表外 host 仍不发任何凭据 | `gcm/db/settings.py`（新字段+加密）`gcm/git/service.py`（`_env` 查表） | 单测：github→Bearer、gitlab→PRIVATE-TOKEN、未知 host→无凭据；加密落盘复验 |
| `[待办]` **G38-2** | P2 | 无 `GIT_MIRROR` 镜像能力 | settings `mirror_prefix: dict[host,prefix]`；clone/fetch URL 前缀拼接（仅 HTTPS）；设置页每 host 一行可编辑 | `gcm/app/url_lib.py`（镜像 URL 组装纯函数）`gcm/git/service.py` | 单测：`https://github.com/a/b` → `https://ghproxy.com/https://github.com/a/b`；SSH 不拼 |
| `[待办]` **G38-3** | P2 | 无远端可达性预检：断网时用户等满超时才知道 | clone/fetch 前 `git ls-remote --exit-code <url> HEAD`（timeout 10s，独立小步）；不可达 → 立即失败并提示"远端不可达（10s 预检），检查网络/代理" | `gcm/git/service.py`（sync 入口） | E2E：无效 host 10s 内明确失败；正常网络预检通过不打扰 |
| `[待办]` **G38-4** | P2 | 浅克隆模式只有 depth；无 single-branch | 克隆模式第三项「单分支浅克隆」：`--depth N --single-branch`；增量更新路径兼容（shallow fetch 带 single-branch 语义） | `main_window.py`（mode_combo:276-285）`gcm/git/service.py` | E2E：单分支浅克隆工作区完整、refs 只含目标分支 |
| `[待办]` **G38-5** | P2 | 弱网降级只到 treeless（`service.py:576-590`）；超大仓仍慢 | 降级链补终档：treeless 失败 → `--depth 1 --single-branch`（保命模式）+ 明确提示"完整历史未拉取，可用设置→unshallow 补全" | `gcm/git/service.py`（`_clone`） | E2E：模拟两级失败最终 depth1 成功；提示文案出现 |
| `[待办]` **G38-6** | P3 | 无强制 IPv4（IPv6 兼容问题常见） | 设置「强制 IPv4」→ `_env` 注入 `git -c http.version=HTTP/1.1` | `gcm/git/service.py` `db/settings.py` | 单测断言 env 注入；E2E 正常仓不劣化 |
| `[待办]` **G38-7** | P3 | 自建主机（企业 GitLab）每次要敲完整 URL | 设置页「常用主机」列表（host + 可选 token 关联 G38-1）；`mygit.example.com/a/b` 无协议时按白名单外 host 直接解析为 HTTPS URL | `gcm/app/url_lib.py` `db/settings.py` | 单测：自建 host 短格式解析 + token 按 host 注入 |

---

## G39 安全纵深加固

> 既有安全资产（必须保持回归）：url_lib 层 `@` 凭据前置拒绝、`..` 段拒绝、ref 合法性校验、host 白名单、token host 白名单注入、DPAPI/Keyring/XOR 加密落盘、single-instance、`_safe_rmtree_partial` 不误删、redact 全链路。

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G39-1** | P1 | `host_tokens`（G38-1）新增后密钥面扩大：settings.json 一份密文可被同 Windows 用户进程解密 | host_tokens 每条独立加密（复用现有 backend 链 `settings.py:16-24`）；设置页 token 输入框 `echoMode=Password` + "显示"眼睛按钮；导出报表/统计不含 token 字段 | `gcm/db/settings.py` `main_window.py`（设置页 UI） | settings.json 中新字段为 `enc:v1:` 前缀密文；UI 单测 echoMode |
| `[待办]` **G39-2** | P2 | `choose_target`（`main_window.py:918-925`）无敏感目录校验：用户选到 C:\Windows 后写文件失败或污染 | 下载目录校验：选择时若在 `%SystemRoot%`/`Program Files` 下 → 警告确认；`mkdir(parents=True)` 失败分类提示 | `main_window.py` | 单测：选 C:\Windows\Temp 之类给警告文案 |
| `[待办]` **G39-3** | P2 | 更新检查走 GitHub API 明文信任 latest tag；无完整性概念（publish_release.py 已附 sha256 于 body） | `updater.check_latest` 返回 Release body；更新提示 Dialog 展示"发布方公布 sha256"，用户手动核对（桌面工具轻量方案） | `gcm/app/updater.py`（提示文案）`main_window.py`（更新弹窗） | Release body 含 sha256 行（已落地）；更新弹窗显示该值 |
| `[待办]` **G39-4** | P3 | 日志含完整本地路径（用户名泄露面） | redact 增加可选 `USERPROFILE` 打码（设置开关，默认关） | `gcm/util/redact.py` | 单测：开关开启后路径中用户名打码 |

---

## G40 工程与发布体系

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G40-1** | P0 | CI 覆盖率门禁 82%（`ci.yml:45`），冲刺目标 85% | 待 G33/G34/G36 提升真实覆盖后，`--fail-under` 提到 85；本地复现命令写 README | `.github/workflows/ci.yml` | CI 在 <85% 时红 |
| `[待办]` **G40-2** | P1 | 并发正确性只靠 E2E 一次一次跑；竞态回归靠人眼 | `tests/test_engine.py` 补参数化压力（unittest subTest，不引 hypothesis）：随机 2-32 并发 × 3-20 仓库 × 随机 20% 失败注入，断言结果集合一致、无死锁（timeout 120s） | `tests/test_engine.py` | 随机种子固定可复现；CI 3 轮全绿 |
| `[待办]` **G40-3** | P1 | `__main__` 42% / `single_instance` 59% 覆盖率最低两块 | `tests/test_main.py`：`get_data_dir` 三分支（env/frozen/源码）+ `main()` 冒烟（mock QApplication/SingleInstanceLock/MainWindow）；`single_instance` 补锁文件损坏/PID 复用分支 | 新 `tests/test_main.py` `tests/test_single_instance.py` | 两文件覆盖 ≥80%；全量绿 |
| `[待办]` **G40-4** | P2 | 发布脚本 body 模板停在旧版本描述；无独立模板文件 | `publish_release.py` Release body 模板读 `docs/RELEASE_BODY.md`（版本号占位符）；body 附 sha256（已落地） | `scripts/publish_release.py` 新 `docs/RELEASE_BODY.md` | dry-run 输出 body 预览；实发一次验证 |
| `[待办]` **G40-5** | P3 | `requirements-build.txt` 未锁版本（全是 `>=`） | 锁定 `PyInstaller==x.y.z`（当前实际 6.x）+ ruff/mypy/coverage/PyGithub 精确版本 | `requirements-build.txt` | `pip install -r` 可复现构建 |

---

## G41 长期演进（i18n / Portable / 生态）

| 条目 | 优先级 | 现状 | 方案 | 落地文件 | 验收标准 |
|------|--------|------|------|----------|----------|
| `[待办]` **G41-1** | P2 | `Settings.language`（`settings.py:141`）字段空转（"i18n 备用"）；全部文案硬编码中文 | **轻量 i18n**（不引 Qt Linguist，保持零构建步骤）：新 `gcm/i18n/__init__.py` 提供 `tr(key)`；先抽 download tab 与设置页文案到 `gcm/i18n/zh.py`/`en.py` 字典；设置语言下拉切换后重启生效 | 新 `gcm/i18n/` `main_window.py` | 切 en 后重启主界面 download tab 全英文；zh 默认不变 |
| `[待办]` **G41-2** | P2 | 数据目录固定 exe 同级 `data/`（`get_data_dir` 现支持 `GCM_DATA_DIR` env 覆盖）；绿色/安装用户混 | `--portable` CLI 参数：数据目录 = exe 目录（现状默认）vs 默认 `%APPDATA%/Git-clone-Max`（变更前兼容：已有 data 目录则继续用） | `gcm/__main__.py`（10-27） | 三分支单测；旧用户升级无感 |
| `[待办]` **G41-3** | P3 | 无多账号 SSH | 详情页"该仓库使用指定 key"→ clone env 注入 `GIT_SSH_COMMAND="ssh -i <key>"`（路径存 note 字段或新列） | `gcm/git/service.py` `repo_db.py` | E2E：指定 key 克隆真实 SSH 仓库（本地 sshd 可选） |
| `[待办]` **G41-4** | P3 | 无 CLI 模式（无 GUI 环境跑批量） | `python -m gcm --cli < urls.txt`：复用 engine/service，QT_QPA_PLATFORM=offscreen 不建窗；输出进度到 stdout | `gcm/__main__.py`（新增分支）新 `gcm/cli.py` | E2E：3 仓库批量克隆成功且退出码正确；CI 可跑 |
| `[待办]` **G41-5** | P3 | 无 uv 等现代环境声明（README 无 uv 说明） | README 补 uv 一键运行（`uv run python -m gcm`）；可选 `pyproject [project]` 元数据补全 | `README.md` `pyproject.toml` | uv 用户可直接跑 |
| `[待办]` **G41-6** | P3 | 无安装器 | Inno Setup 脚本 `packaging/installer.iss`（可选发布产物，不替代便携版） | 新 `packaging/` | 产出 setup.exe 可安装卸载；开始菜单图标 |

---

## 附录A 里程碑路线与门禁

> **执行铁律**：每个里程碑 = 全量测试绿 + 覆盖率 ≥ 当前门禁 + 验收清单登记证据 → 才进下一个。
> 严禁前面改 bug 后面加功能又出新 bug；每条目先写测试（TDD），再实现，再全量回归。

| 里程碑 | 聚焦 | 条目 | 预估规模 | 门禁 |
|--------|------|------|----------|------|
| **M1 可靠性补全** | G33 全部 + G34-1/2 | P2 为主，小步 | 中（~2 天） | 360→380+ 全绿；覆盖率 ≥82%；`main_window` 不降 |
| **M2 性能与批量化** | G34-3/4 + G35-1~4 | P1 为主 | 中（~3 天） | 600 仓库批次 E2E；内存 before/after 登记 |
| **M3 交互进阶** | G35-5~10 + G36-1/2/3 | 中 | ~3 天 | 400+ 用例；覆盖率 ≥84% |
| **M4 体验升维** | G36 剩余 + G37 全部 | 大 | ~4 天 | 420+ 用例；覆盖率 ≥85%；CI 门禁 85% 生效 |
| **M5 网络与安全纵深** | G38 + G39 | 中 | ~3 天 | 安全回归全绿（redact/token/白名单/host_tokens） |
| **M6 工程与发布** | G40 + G41-1/2 | 中 | ~2 天 | Release 双产物 + 发布清单走查 + v8.0 收官 |

**执行顺序原则**：M1 → M6 顺序执行；M3 可与 M2 并行切分；任何条目发现牵连缺陷 → 记入 G33 补条目（编号顺延 G33-9+），不允许"顺手改"无测试无登记。

## 附录B TDD 执行规约（每条必守）

1. **RED**：先写失败测试（引用本指南条目号，如 `def test_g35_1_retry_failed_row`）。
2. **GREEN**：最小实现让测试通过，不改无关代码。
3. **IMPROVE**：全量回归 `python -m unittest discover -s tests` + `coverage report`，覆盖率不低于进入前。
4. **证据登记**：`计划书/验收清单.md` 新增 v7.x 节，每条格式：`| G35-1 | ✅ | 要点 | 验证命令与输出摘要 | 覆盖率 |`。
5. **禁止事项**：禁止省略号注释、TODO 占位实现、"应该可以跑"式结论；禁止在未运行验证命令时声称完成；禁止重写 engine/worker/repo_db 闭环（动则必须附并发回归证据）。
6. **版本节奏**：每个里程碑发一个 semver 版本（v7.0.0 起），`__changelog__` 追加一行；Release 流程按 `docs/RELEASE_CHECKLIST.md`（已落地）。

## 附录C 关键代码定位表（v6.7.0 实测行号）

| 关注点 | 文件:行 | 备注 |
|--------|---------|------|
| 版本号/changelog | `gcm/__init__.py:2,4-58` | v7 追加至 59+ |
| 入口/数据目录 | `gcm/__main__.py:10-27` | G41-2 改造点 |
| 引擎（并发/去重/进度） | `gcm/app/engine.py`（444 行） | `launch:261` `drain:408` `shutdown:431` G34-2 改造点 |
| worker | `gcm/app/worker.py`（121 行） | 不建议动 |
| git 服务 | `gcm/git/service.py`（781 行） | `sync:484` `_clone:528` `_update:623` `_env:379` `run_git_ui:213` |
| URL 解析 | `gcm/app/url_lib.py`（342 行） | `parse_any_repo_url:205` `parse_urls:284` G38-2 改造点 |
| SQLite | `gcm/db/repo_db.py`（399 行） | WAL:86 `BEGIN IMMEDIATE:103` G33-3/5 改造点 |
| 设置 | `gcm/db/settings.py`（240 行） | `Settings:129-149` 加密工具 `:16-24` G38-1 改造点 |
| URL 历史 | `gcm/db/history.py`（70 行） | G35-4 参考其模式 |
| 主窗口 | `gcm/ui/main_window.py`（1514 行） | `_build_download_tab:196` `_build_settings_tab:375` `start_all:942` `update_all:994` `_launch:1028` `pause_selected:1204` `_on_worker_result:1260` `_on_engine_finished:1314` `closeEvent:1473` |
| 主题/LogModel | `gcm/ui/theme.py`（264 行） | `LogModel:187`（deque 改造点 G34-1）|
| 托盘 | `gcm/ui/tray.py`（122 行） | `update_counts:57` `notify:114` |
| 更新检查 | `gcm/app/updater.py`（106 行） | `check_latest:50` G39-3 改造点 |
| 打包 | `Git-clone-Max.spec` | G36-3（icon）G34-4（excludes）改造点 |
| CI | `.github/workflows/ci.yml` | `--fail-under=82:45` G40-1 改造点 |
| 发布脚本 | `scripts/publish_release.py` | G40-4 改造点 |
| 统计中心 | `gcm/ui/statistics_dialog.py`（72 行） | G37-1/5 改造点 |
| 仓库详情 | `gcm/ui/repo_detail_dialog.py`（194 行） | G35-5/G37-2 改造点 |
| 本地导入 | `gcm/ui/local_repos_dialog.py`（230 行） | G33-1/2 改造点 |
| 旧指南归档 | 本文件（v6.7.0 基线版） | v6.7 已落地内容以 `G32` 登记 + `计划书/验收清单.md` 证据链为准 |

---

*v7.0 指南 = 唯一事实源。实施方按 M1→M6 顺序推进，每条 TDD + 证据登记；主控只看 `计划书/验收清单.md`。*
