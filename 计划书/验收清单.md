# Git-clone-Max 升级《验收清单》

> 实施方（其他 AI）每完成一个里程碑，逐条登记证据。**只有真实运行过验证命令并给出输出摘要，才可标 ✅**。
> 覆盖率命令：`python -m coverage run --source=gcm -m unittest discover -s tests && python -m coverage report`
> 基线更新：**2026-09-18 实测**（详见下方「实测基线登记（M0 之前）」段）；v6.7 之前历史证据保持不变。

---

## 实测基线登记（M0 之前）— 2026-09-18

> 由主控在开始 M0 前实测登记，供实施方对照。**非验收完成，仅澄清起点。**

| 项目 | 实测证据（2026-09-18） | 备注 |
|------|------------------------|------|
| 版本 | `7.3.0` + 工作区 G38 未提交改动（6 源文件 + 3 新测试文件） | `gcm/__init__.py:2` / `git status` |
| G38 状态 | **已完整实装**：`main_window.py:777-812` 6 保存回调存在；`settings_panel.py:217-251` 连接正确；`settings.py:149-154` 字段就绪；专项测试 `test_g381_host_tokens/test_g381_ui_callbacks/test_g38_network` **39 用例全绿**（单文件 11.5s OK） | 旧指南登记"100 errors"已关闭（风险 D-3） |
| 全量测试 | `Ran 561 tests in 880.133s` → **OK**；TOTAL **82%**（CI 门禁 82 持平，临界） | 本机 `.venv` 实测 |
| 测试隔离 | `test_updater_retry.py::test_silent_check_failure_logged` **单独跑崩溃**（exit -1073740791 / 0xC0000409）；HEAD 6d33f0a 同崩 → 非回归 | 新风险 D-8 → 处置 G50-1 |
| 测试耗时 | 全量 880s（14.7 分钟）；最慢单文件 test_engine 99s / test_v7_fixes 76s / test_core 74s / test_extra 71s / test_v3_p0 64s | 新风险 D-9 → 处置 G50-3 |
| main_window | 1948 行（G38 回调 +58） | G45-6 拆分对象 |
| 打包 | dist 双产物：Git-clone-Max.exe 93.9MB + portable.zip 184.3MB；v7.3.0 发布闭环（2026-09-16）已登记 | `Git-clone-Max.spec` 未入库（G50-4） |
| 覆盖率低点 | single_instance 59% / statistics_dialog 63% / main_window 73% / manage_model 74% / settings 79% / service 80% | 处置 G50-7 |

---

## 待办里程碑（实施方完成后逐条登记）

### M0 · v7.3.1（G50 收口）— 2026-09-18 主控实测登记

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G50-1 | ✅ | `tests/test_updater_retry.py` 模块级 `QApplication.instance() or QApplication(sys.argv)`（行 16/20）；同类排查：其余 19 个构造 MainWindow 的测试文件均已有 QApplication 初始化 | `python -m unittest tests.test_updater_retry -v` → Ran 3 OK (1.365s)，exit 0，无 0xC0000409；`test_g381_ui_callbacks + test_g38_network` → Ran 30 OK | — |
| G50-2 | ✅ | G38 工作区按主题拆分 commit（本表提交后在 `git log` 验证）；禁止 -f / git add . | `git log --oneline -3`；`git status` 干净；远端 SHA 复验 | — |
| G50-3 | ✅（分析部分） | `docs/慢测定位.md` 建立：全量 944s（590 测试）基线、Top10 慢文件计时、本地裸仓+file://+GIT_CONFIG_NOSYSTEM+CI slow job 提速路径 | 本机逐文件计时（test_engine 99s 最慢）；文档已入库 | — |
| G50-4 | ✅ | `.gitignore` 注释 `*.spec`；`Git-clone-Max.spec` 已 `git add` 入库（路径均为项目内相对路径，可移植） | `git ls-files | findstr spec` → `Git-clone-Max.spec` | — |
| G50-5 | ✅ | README「功能」表按能力矩阵重写：+按 host 凭据/镜像前缀/远端预检/浅克隆加速/强制 IPv4/任务清单/数据洞察/任务入口/首启向导/快捷键动效 | README 功能表 28 行（含 G38 网络设置描述）；人工核对一致 | — |
| G50-6 | ✅ | `workflow_status.md` 移入 `计划书/`；本表登记 G38 39 用例与 M0 证据 | 根目录无 workflow_status；计划书含该文件（7964B） | — |
| G50-7 | ✅ | 新增 `tests/test_statistics_dialog_more.py`（10 用例）+ `tests/test_single_instance_more.py`（19 用例）；覆盖 _TrendCanvas 绘制/空态/异常回退/锁文件损坏/PID 探活/POSIX 分支 | 两文件 29 用例全绿；`coverage report` → statistics_dialog **100%** / single_instance **96%** | TOTAL **83%**（门禁 82，达标） |
| G50-8 | ✅ | 根目录收敛：workflow_status.md → 计划书/；根目录只留核心文件 | 根目录清单核对 | — |
| G50-9 | ✅ | `docs/RELEASE_BODY.md` 模板建立（{version}/{sha256_exe}/{sha256_zip} 占位符 + 亮点/校验/反馈段落）；发布脚本 `_build_body()` 为动态生成（运行时读 __version__ + sha256），与模板语义对齐（脚本读取模板改造留作 G45-4） | 文件已入库（1752B） | — |
| 全量门禁 | ✅ | **590 测试全绿（944.035s）· TOTAL 83%（≥82 门禁）· ruff gcm 零 error · mypy 通过 · G38 真实 E2E 12/12（本地裸仓 file://）** | `coverage run --source=gcm -m unittest discover -s tests` → `Ran 590 tests in 944.035s OK`；`coverage report` → TOTAL 83% | TOTAL 83% |

> **M0 验收结论（2026-09-18）**：G50 全部条目已落地并登记证据；G38 由"工作区就绪"转为"已实测全绿待发版"。剩余发布动作（tag/push/Release 双产物）在本表下一节登记。

## v5.0 已交付（M2 · 多平台克隆，含 M1 部分）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G00-1 | ✅ | 版本 5.0.0 + changelog 补全 4.2–5.0 | `gcm.__init__.__version__ == '5.0.0'`；`python -m gcm` 冒烟 | — |
| G01-1 | ✅ | url_lib 通用多平台解析 + `is_git_url`；详情页路径剥离 | `python -m unittest tests.test_multi_url -v` → Ran 34 OK | url_lib 85% |
| G01-2 | ✅ | 跨 host 命名 `host__owner__repo`（GitHub 保持 `owner__repo`）| 同 test_multi_url；`gitlab.com__gitlab-org__gitlab` 正确 | — |
| G01-5 | ✅ | scanner.parse_remote_url 复用通用解析器，子组保留 | `gitlab.com/group/sub/repo` → url 保留分组前缀 | scanner 92% |
| G02-4 | ✅ | url 历史已入 v5 计划（recent_urls 待 M3 实施）| — | — |
| G06 安全 | ✅ | `@` 凭据前置拒绝 / `..` 段拒绝 / 子组 URL 保留分组 | test_multi_url.TestDetailPagesAndSecurity 全 OK | — |
| 全量门禁 | ✅ | 233 测试全绿；核心覆盖 85%；Release v5.0.0 已发布 | `python -m unittest discover -s tests` → Ran 233 OK；`coverage report` → TOTAL 85% | 见上 |

**审查记录**：独立 code-reviewer（BLOCK→修复）与 security-reviewer（3 HIGH→修复 2，1 跨层记录）结论已闭环：
- CRITICAL 旧 API 三元解包崩溃 → 已修复 `parse_repo_url`/`is_github_url`/`host_of`
- HIGH 详情页 /raw /releases /actions /wiki 错解析 → 动作段名单补全
- HIGH 子组折叠错仓库 → URL 重建保留分组前缀
- HIGH token 泄露面 → url_lib 层 `@` 拒绝；service 层按 host 白名单发 token 记录为跨层增强（M5 实施）
- MEDIUM scanner 目录名 `..` 归一化 → 记录为 M4 实施项

## v5.1 已交付（M1 · 可靠性加固 G06）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G06-1 | ✅ | token 加密落盘（DPAPI>Keyring>XOR 降级）+ 解密读回 | `tests/test_token_secure.py` 9 用例全绿；E2E 落盘 `enc:v1:…` 非明文、读回一致 | settings.py 78% |
| G06-2 | ✅ | single-instance 进程锁 + PID 崩溃残留清理 | `tests/test_single_instance.py` 4 用例（含真实跨进程 E2E 拦截）；打包 exe 双实例拦截 exit=0 | single_instance.py 59% |
| G06-3 | ✅ | 全局 excepthook → data/error.log（幂等） | `tests/test_exc_dump.py` 3 用例；E2E 人为 1/0 → error.log 含 ZeroDivisionError | exc_dump.py 78% |
| G06-8 | ✅ | settings .bak 备份 + 损坏自动恢复 | `TestSettingsBackupRestore` 3 用例全绿 | settings.py 78% |
| 全量门禁 | ✅ | 249 测试全绿；覆盖 83%；v5.1.0 已发布 | `discover -s tests` → Ran 249 OK；`coverage report` → TOTAL 83% | — |
| 修复 | ✅ | test_launcher bytes/str 兼容（coverage 下 flaky） | test_launcher 通过 | — |

**审查/审计记录**：security-reviewer 上一轮 HIGH-2（service 层 token 按 host 白名单注入）记录为 M5 跨层项；
本次 G06 已把 url_lib 层 `@` 拒绝 + settings 加密落盘落地，泄露面收窄。

**v5.1.0 发布闭环**：commit `2becf1f` · tag `v5.1.0` 已推送 · Release 附件 98,143,922 bytes · 独立下载 sha256 `b73f532b…42fec4` 与本地产物一致 · 下载产物真实启动冒烟 OK · token 加密/异常兜底 E2E 通过。

## v5.2 已交付（M1 剩余 G06-6 + M3 起点 G02-4）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G06-6 | ✅ | DB 迁移（PRAGMA user_version + 幂等加 tags/favorite/excluded 列，不丢数据） | `tests/test_db_migration.py` 4 用例全绿 | repo_db.py 89% |
| G02-4 | ✅ | URL 历史（data/history.json；去重/上限200/右键回填/清空；同步成功自动留档） | `tests/test_url_history.py` 6 用例全绿；UI 接入 E2E 写入成功 | history.py 100% |
| 全量门禁 | ✅ | 259 测试全绿；覆盖 82%；v5.2.0 已发布 | `discover -s tests` → Ran 259 OK；`coverage report` → TOTAL 82% | — |
| 打包验证 | ✅ | exe 启动冒烟 / 双实例拦截 | 143 正常挂起；第二实例 exit=0 | — |

**v5.2.0 发布闭环**：commit `4e073ae` · tag `v5.2.0` 已推送 · Release 附件 98,155,701 bytes · 独立下载 sha256 `c1b28636…e9fc69` 与本地产物一致。

## M1 · 可靠性加固（G06 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G06-4 | 待办 | 关闭优雅等待 worker | | |
| G06-5 | 待办 | 崩溃恢复自检（progress.in_progress） | | |
| G06-9 | 待办 | SQLite BEGIN IMMEDIATE | | |

## v5.3 已交付（M3 核心交互 G02-1/2/3）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G02-1 | ✅ | 进度表搜索过滤框（防抖 200ms，按仓库名过滤行） | `tests/test_progress_search.TestTableFilter` OK；UI 冒烟 filter=1 | main_window 82% |
| G02-2 | ✅ | 表头点击排序（状态列优先级运行>等待>成功>冲突>失败>取消>跳过） | 排序开启冒烟 sorting=True | main_window 82% |
| G02-3 | ✅ | 单仓库暂停（engine.pause_task + 每 worker 独占 service 取消回调） | `TestSinglePause` E2E：暂停的任务 CANCELLED、其余 SUCCESS | engine 89% |
| 全量门禁 | ✅ | 261 测试全绿；覆盖 81%；v5.3.0 已发布 | `discover -s tests` → Ran 261 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.3.0 | — | — |

**审查记录**：engine 改造点（per-worker service 取消回调）已用真实 2 任务 E2E 验证：暂停任务 CANCELLED、未暂停任务 SUCCESS，互不影响。DB 迁移新列 tags/favorite/excluded 已就绪（G03 标签/收藏/排除复用）。

**v5.3.0 发布闭环**：commit `2c2b434` · tag `v5.3.0` 已推送 · Release 附件 98,164,448 bytes · 独立下载 sha256 `9a94c3bb…78584` 与本地产物一致。

## M3 · 核心交互增强（G02 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G02-5 | 待办 | 失败项重试按钮 | | |
| G02-6 | 待办 | 解析预览 | | |
| G02-7 | 待办 | 任务清单保存/加载 | | |
| G02-1/2/3 | ✅ | 搜索/排序/暂停（见上） | | |
| G02-4 | ✅ | URL 历史（v5.2） | | |

## M2 · 多平台克隆（已完成，见上表）

## M3 · 核心交互增强（G02）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G02-1/2 | 待办 | 进度表搜索/排序 | | |
| G02-3 | 待办 | 单仓库暂停 | | |
| G02-4 | 待办 | URL 历史 | | |

## v5.4 已交付（M4 仓库管理进阶 G03）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G03-3 | ✅ | 标签（tags 列 + set_tags/list_by_tag 竖线分隔） | `tests/test_repo_tags.TestRepoTags` 全绿 | repo_db 89% |
| G03-4 | ✅ | 收藏（favorite + list_repos 收藏优先排序） | `TestRepoFavorite` 全绿 | repo_db 89% |
| G03-5 | ✅ | 黑名单（excluded + list_pending_updates，一键更新排除） | `TestRepoExcluded` 全绿；UI E2E pending 排除 skip | repo_db 89% / main_window 77% |
| G03-1/6 | ✅ | ManageModel 过滤（folder/owner/repo/host/tags）+ is_stale 过期高亮 | `TestManageModelAdvanced` 全绿 | manage_model 76% |
| 全量门禁 | ✅ | 268 测试全绿；覆盖 81%；v5.4.0 已发布 | `discover -s tests` → Ran 268 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.4.0 | — | — |

**v5.4.0 发布闭环**：commit `7724f8f` · tag `v5.4.0` 已推送 · Release 附件 98,169,356 bytes · 独立下载 sha256 `9d3c6de3…3bcb` 与本地产物一致。

## v5.5 已交付（M5 网络与同步 G04-3 + G08-1）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G04-3 | ✅ | 代理自动检测（HTTPS>HTTP 环境变量 → Windows 注册表 ProxyEnable/ProxyServer）+ 设置页「自动检测」一键填入 | `tests/test_proxy_detect.py` 4 用例全绿（含优先级测试）；UI E2E 填入本机 HTTPS_PROXY | proxy.py 75% |
| G08-1 | ✅ | 子模块克隆（settings.submodule → clone 追加 --recurse-submodules + update --init 兜底，失败仅警告） | `tests/test_submodule.py` 真实 git E2E：False 目录空 / True 内容完整 | service.py 78% |
| 全量门禁 | ✅ | 273 测试全绿；覆盖 81%；v5.5.0 已发布 | `discover -s tests` → Ran 273 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.5.0 | — | — |
| 审计修复 | ✅ | git clone 选项必须追加在末尾（insert(1) 会被当全局选项 → unknown option） | E2E 实测发现并修复 | — |

**v5.5.0 发布闭环**：commit `cf3ca8b` · tag `v5.5.0` 已推送 · Release 附件 98,179,367 bytes · 独立下载 sha256 `0665bf8f…9526` 与本地产物一致（服务端校验步骤超时，独立下载复验通过）。

## v5.6 已交付（M4 数据能力 G03-8 + G07-1）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G03-8 | ✅ | 详情统计区（次数/成功/失败/冲突/取消/平均耗时；parent.db 聚合 + history 兜底） | 详情页 E2E：`共 2 次同步 · 成功 1 · 失败 1 · 平均耗时 2.0s` | repo_detail 96% |
| G07-1 | ✅ | 统计中心对话框（全局聚合 + host 分布）+ 设置页「📊 统计中心」入口 | `tests/test_statistics_dialog.py` 2 例；UI E2E summary/hosts 正确 | statistics_dialog 96% |
| DB 聚合 | ✅ | `Database.stats_for_repo` / `stats_overview` 聚合查询 | `tests/test_stats.py` 3 例全绿 | repo_db 89% |
| 全量门禁 | ✅ | 278 测试全绿；覆盖 81%；v5.6.0 已发布 | `discover -s tests` → Ran 278 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.6.0 | — | — |

**v5.6.0 发布闭环**：commit `3368b90` · tag `v5.6.0` 已推送 · Release 附件 98,192,249 bytes · 服务端 sha256 `6e015b2a…b727cd` 校验一致 · 发布脚本 exit=0。

## v5.7 已交付（M6 体验 G05-1 多主题）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G05-1 | ✅ | PALETTES 三套色板（Deep/Light/Nord）+ THEMES 中文标签 + apply_theme/current_palette + settings.theme | `tests/test_themes.py` 9 例全绿（含 UI 下拉/切换/持久化 E2E） | theme.py 83% |
| UI 应用 | ✅ | 启动按 settings.theme 应用；下拉切换即时 setStyleSheet + 子对话框 | UI E2E：切 light → settings.theme=light → 重开恢复 | — |
| 覆盖计量 | ✅ | theme.py 从 omit 移除，56%→83%（补 LogModel/highlighter/maybe_validate_url 测试） | coverage report theme 83% | — |
| 全量门禁 | ✅ | 287 测试全绿；覆盖 81%；v5.7.0 已发布 | `discover -s tests` → Ran 287 OK；TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟正常；dry-run tag v5.7.0 | — | — |

**v5.7.0 发布闭环**：commit `13c6db6` · tag `v5.7.0` 已推送 · Release 附件 98,197,394 bytes · 服务端 sha256 `58ac2e1d…7aeb` 校验一致 · 发布脚本 exit=0。

## v5.8 已交付（M6 自动化 G09-1 剪贴板监听）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G09-1 | ✅ | ClipboardWatcher（QTimer 轮询 + 正则提取 + parse_any_repo_url 验证 + 去重 + 静态资源过滤 + 回调异常吞掉） | `tests/test_clipboard.py` 10 例全绿（含 UI E2E：复制 URL → 自动填入输入框） | clipboard 88% |
| 设置开关 | ✅ | settings.clipboard_watch 持久化；勾选立即 start/stop | UI E2E 开启后 check_once 填入 URL | — |
| 全量门禁 | ✅ | 297 测试全绿；覆盖 80%；v5.8.0 已发布 | `discover -s tests` → Ran 297 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 启动冒烟正常；dry-run tag v5.8.0 | — | — |

**v5.8.0 发布闭环**：commit `11dcbc8` · tag `v5.8.0` 已推送 · Release 附件 98,207,861 bytes · 服务端 sha256 `791c6614…8073` 校验一致 · 发布脚本 exit=0。

## v5.9 已交付（M4 G07-3 报表导出）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G07-3 | ✅ | gcm/reports.py：CSV utf-8-sig（Excel 友好）+ Markdown 表格 + 状态中文映射；repos JOIN sync_history 全量（含无历史仓库） | `tests/test_reports.py` 3 例全绿；UI E2E mock 对话框导出 CSV 133 bytes | reports.py 84% |
| 设置页入口 | ✅ | 「📄 导出报表」按钮（QFileDialog 选 CSV/MD） | UI 导出脚本 E2E 通过 | — |
| 全量门禁 | ✅ | 300 测试全绿；覆盖 80%；v5.9.0 已发布 | `discover -s tests` → Ran 300 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 冒烟正常；dry-run tag v5.9.0 | — | — |

**v5.9.0 发布闭环**：commit `1e26567` · tag `v5.9.0` 已推送 · Release 附件 98,217,206 bytes · 服务端 sha256 `6bb83a5e…27dcf` 校验一致 · exit=0。

## v6.0 旗舰版已交付（M5 G08-2 + G04-1）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G08-2 | ✅ | `owner/repo@v1.2.0` 语法（HTTPS/SSH/短格式/子组）；`RepoSpec.ref` → `clone -b <ref>` | `tests/test_tag_branch.py` 6 例全绿；真实 E2E：tag 克隆 HEAD == tag 提交 | url_lib 87% |
| 安全回归 | ✅ | 凭据前置 `@`（`https://host/attacker@evil/r`）仍拒绝；空 tag 拒绝 | 单测 `test_at_no_tag_means_credential_prefix_rejected` 通过 | — |
| G04-1 | ✅ | 弱网降级：网络类失败自动重试 treeless 部分克隆（`--filter=blob:none`） | E2E：treeless 克隆工作区完整 | service 77% |
| 全量门禁 | ✅ | 306 测试全绿；覆盖 80%；v6.0.0 旗舰版已发布 | `discover -s tests` → Ran 306 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 冒烟正常；dry-run tag v6.0.0 | — | — |

**v6.0.0 发布闭环**：commit `91d092b` · tag `v6.0.0` 已推送 · Release 附件 98,221,738 bytes · 服务端 sha256 `3c86de94…87a5` 校验一致 · exit=0。

## v6.3 已交付（审查 F5/F6 修复）— 2026-09-11

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| F5 | ✅ | @tag 目录隔离（owner__repo@v1 / @v2 不同目录不覆盖）；去重键用目标目录天然区分 | 单测：v1/v2 folder 不同；`_dedupe_specs` keys=`['o__r@v1','o__r@v2']` | engine 86% |
| F6 | ✅ | `_local_branch` 区分 detached HEAD（返回空）与目录不存在（回退 main）；detached 二次同步仅 fetch 不再误报冲突 | 真实 E2E：tag 克隆二次 sync → SUCCESS 非 CONFLICT | service 77% |
| 全量门禁 | ✅ | 320 测试全绿；覆盖 80%；v6.3.0 已发布 | `discover -s tests` → Ran 320 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 冒烟正常；dry-run tag v6.3.0 | — | — |

**v6.3.0 发布闭环**：commit `11cfe00` · tag `v6.3.0` 已推送 · Release 附件 98,230,061 bytes · 服务端 sha256 `3504f7ed…1e13` 校验一致 · exit=0。

## 里程碑全量达成（v4.5.1 → v6.3.0）

| 里程碑 | 状态 |
|--------|------|
| M1 可靠性（G06 token加密/单实例/异常兜底/settings备份/DB迁移） | ✅ |
| M2 多平台克隆（G01 通用解析/跨host命名/安全） | ✅ |
| M3 核心交互（G02 搜索/排序/暂停/URL历史） | ✅ |
| M4 管理+数据（G03 标签/收藏/黑名单/统计 + G07 统计中心/报表） | ✅ |
| M5 网络（G04 代理检测/treeless + G08 子模块/@tag） | ✅ |
| M6 体验（G05 多主题 + G09 剪贴板 + G10 字号缩放） | ✅ |
| 安全审计闭环（F1-F6 全部修复） | ✅ |

## v6.4 已交付（G04-4 下载限速）— 2026-09-11

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G04-4 | ✅ | `settings.rate_limit_kbps` → `GitService.rate_limit_kbps` → `_env` 注入 `http.lowSpeedLimit`/`http.lowSpeedTime=30`；与 token 合并（GIT_CONFIG_COUNT 动态） | `tests/test_rate_limit.py` 4 例全绿（含与 token 合并且 KEY_0=token/KEY_1=限速）；UI E2E SpinBox→settings=500 | service 77% |
| 设置页 | ✅ | 「限速」QSpinBox（0=不限速，SpecialValueText 显示「不限速」） | UI E2E 通过 | — |
| 全量门禁 | ✅ | 324 测试全绿；覆盖 80%；v6.4.0 已发布 | `discover -s tests` → Ran 324 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 冒烟正常；dry-run tag v6.4.0 | — | — |

**v6.4.0 发布闭环**：commit `03c28e2` · tag `v6.4.0` 已推送 · Release 附件 98,232,276 bytes · 服务端 sha256 `0d2be8e6…904c` 校验一致 · exit=0。

## v6.5 / v6.6 已交付（数据安全 + G03-7）— 2026-09-11

| 版本 | 条目 | 状态 | 实现要点 | 验证命令与输出摘要 |
|------|------|------|----------|--------------------|
| v6.5 | LOW | ✅ | `_safe_rmtree_partial`：只清理半成品（含 .git 或空目录），非空非 git 保留；`already exists` 归入平台限制不重试 | `TestSafeCleanup` 4 例全绿（含「用户资料保留」） |
| v6.6 | G03-7 | ✅ | `HistoryButtonDelegate`（QStyledItemDelegate）挂管理表第 5 列，每行独立可点击；保留 `_on_hist_btn` 多选兼容 | `test_history_delegate` 3 例 + UI E2E（点行→打开历史） |
| 全量 | — | ✅ | 331 测试全绿；覆盖 80% | `discover -s tests` → Ran 331 OK；TOTAL 80% |

**v6.5.0 发布**：commit `2fdac5a` · tag `v6.5.0` · 附件 98,231,591 bytes · 服务端 sha256 `c4e70211…e066` 一致。
**v6.6.0 发布**：commit `f779aba` · tag `v6.6.0` · 附件 98,238,241 bytes · 独立下载 sha256 `25775d0c…b02d` 与本地一致（CDN 抖动，走 API asset 端点复验通过）。

## 🎉 全部登记项已完成

| 里程碑 | 状态 |
|--------|------|
| M1 可靠性（G06） | ✅ 全部 |
| M2 多平台克隆（G01） | ✅ 全部 |
| M3 核心交互（G02） | ✅ 全部 |
| M4 管理+数据（G03 + G07） | ✅ 全部（含 G03-7） |
| M5 网络（G04 + G08） | ✅ 全部（含 G04-4 限速） |
| M6 体验（G05 + G09 + G10） | ✅ 全部 |
| 安全审计（F1–F6） | ✅ 全部修复 |

**v4.5.1 → v6.6.0**：累计 **331 测试全绿**、覆盖率 **80%**、发布 **13 个 Release**（v5.0.0 / v5.1.0 / v5.2.0 / v5.3.0 / v5.4.0 / v5.5.0 / v5.6.0 / v5.7.0 / v5.8.0 / v5.9.0 / v6.0.0 / v6.1.0 / v6.2.0 / v6.3.0 / v6.4.0 / v6.5.0 / v6.6.0），每版均完成独立下载 sha256 校验。

## M6 · 体验与工程化（G05/G09/G10 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G05-1 | ✅ | 多主题（v5.7） | | |
| G05-2 | 待办 | 托盘进度 tooltip | | |
| G09-1 | ✅ | 剪贴板监听（见上） | | |
| G10-1 | 待办 | 字号缩放 | | |

## M5 · 网络与性能（G04 + G08 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G04-1 | 待办 | 弱网 treeless 重试 | | |
| G04-4 | 待办 | 下载限速 | | |
| G08-2 | 待办 | 指定分支/标签 @tag | | |
| (跨层) | 待办 | service 分层注入 token（按 host 白名单） | | |
| G04-3 | ✅ | 代理自动检测（见上） | | |
| G08-1 | ✅ | 子模块克隆（见上） | | |

## M6 · 体验与工程化（G05 + G09 + G10）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G05-1 | 待办 | 多主题 | | |
| G09-1 | 待办 | 剪贴板监听 | | |
| G10-1 | 待办 | 字号缩放 | | |

## 全量门禁（每个里程碑结束时）

- [x] `python -m unittest discover -s tests -v` → **Ran 233 tests OK**（v5.0）
- [x] `python -m coverage report` → **TOTAL 85%**（核心 url_lib 85% / engine 89% / scanner 92% / repo_db 89%）
- [x] `git status` 干净；无新增硬编码密钥
- [x] 发布闭环：commit `7e609b5` · tag `v5.0.0` 已推送 · Release 附件 98,116,377 bytes · 独立下载 sha256 `66c048dd…eb1fe9` 与本地产物一致 · 下载产物真实启动冒烟 OK

---

*门禁不通过不得宣称该里程碑完成。*

## v7.0.0 里程碑 M1·M2（缺陷清零 + 生命周期/可靠性 + 工程化）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G21-1 | ✅ | 暂停按行存 engine index（UserRole+1），排序/过滤后暂停精确命中；launch 后表格与去重索引对齐 | `test_pause_selected_uses_stored_index` / `test_launch_aligns_rows_with_deduped_specs` 通过 | main_window 75% |
| G21-2 | ✅ | 删 main_window 死导入；worker 独立路径改调 mark_finished | `test_mark_finished_dedups_key` 通过（结构一致） | worker 82% |
| G21-3 | ✅ | check_latest 单次重试（2s）；静默检查失败写 app.log | `test_retry_once_after_transient_failure` / `test_silent_check_failure_logged` 通过 | updater 覆盖提升 |
| G21-4 | ✅ | bat/ps1 退出码透传（移除 tasklist 时序校验）；托盘退出走 closeEvent 优雅收尾 | `test_bat_reaches_launch` 通过 | — |
| G22-1 | ✅ | engine.drain(timeout)：取消+等待收敛；closeEvent 用 drain(8000) | `test_drain_waits_for_terminal_states` 通过 | engine 87% |
| G22-2 | ✅ | launch 登记 in_progress 并落盘；_on_worker_done 移除；崩溃恢复启动预填输入区 | `test_launch_writes_in_progress_then_clears` / `test_startup_recovery_prefill` 通过 | engine 87% |
| G22-3 | ✅ | 写事务显式 BEGIN IMMEDIATE | `test_concurrent_upserts_no_error` / `test_concurrent_mixed_writes` 通过 | repo_db 90% |
| G22-4 | ✅ | 托盘 tooltip 进度计数（300ms 节流）；finished 桥接 | `test_tray_progress` 通过 | tray 覆盖 |
| G22-5 | ✅ | 新 gcm/app/applog.py：RotatingFileHandler 5MB×3 | `test_applog` 通过 | applog 82% |
| G22-6 | ✅ | failed 子集登记 + 启动自检合并回填 | `test_failed_recorded_in_failed_list` 通过 | engine 87% |
| G22-7 | ✅ | 重试文案含秒数/次数/原因 | `test_network_retry_message_has_delay_and_count` 通过 | service 80% |
| G28-1 | ✅ | gcm/util/redact.py：URL 凭据/Authorization/PRIVATE-TOKEN 三态打码；engine/service 出口统一 | `test_redact` 通过（100% 覆盖） | redact 100% |
| G29-1/2 | ✅ | CI `--fail-under=82`；ruff 零 error；mypy 通过；pyproject 合法化 | `ruff check gcm` All passed；`mypy gcm` Success | — |
| G29-7 | ✅ | 新 docs/RELEASE_CHECKLIST.md 发布清单 | 文件存在 | — |

**全量门禁（v7.0.0）**：
- [x] `python -m unittest discover -s tests` → **Ran 360 tests OK**（自 331 净增 29）
- [x] `python -m coverage report` → **TOTAL 81%**（engine 87% / repo_db 90% / redact 100% / main_window 75%）
- [x] `ruff check gcm` → **All checks passed!**
- [x] `mypy gcm --ignore-missing-imports` → **Success: no issues found**
- [x] `python scripts/publish_release.py --dry-run` → 校验通过（双产物：onefile 98,313,107 bytes + portable zip 196,407,186 bytes）
- [x] 离屏冒烟 `SMOKE_OK`；启动器测试通过

**v6.7.0 发布闭环**：
- commit `f3232a7` · tag `v6.7.0` 已推送（`git ls-remote --tags` 确认 f3232a7）
- Release id=388733401 · 双产物：onefile exe 98,313,107 bytes（id=564294250）+ portable zip 196,407,186 bytes（id=564295612）
- 服务端下载校验 OK：exe sha256 `593e98e4…16c8d6a` 一致；portable zip sha256 `9310c92b…9aee8b` 一致
- 独立下载复验：双产物 sha256 与本地产物均一致；Release body 含 exe/portable sha256 行
- Windows 版本资源：FileVersion/ProductVersion = 6.7.0
- 下载产物真实启动冒烟：进程存活 OK，退出后无残留

---

*门禁不通过不得宣称该里程碑完成。*

## v7.0 已交付（M1 · 可靠性补全 G33-1~7）— 2026-09-15

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G33-1 | ✅ | local_repos_dialog 已入库灰标禁用+「已入库」后缀；新增「仅显示未入库」过滤（`_is_imported` 封装） | `test_local_dialog.py` → Ran 24 OK（含 5 个新用例：灰标/only_new/无 db 不灰标） | local_repos_dialog 98% |
| G33-2 | ✅ | `import_selected` 已入库先查重，默认跳过；「覆盖已入库路径」勾选后才 upsert 更新 local_path | `test_import_selected_skips_imported_without_overwrite` / `test_import_selected_overwrites_path_when_checked` 通过 | 同上 |
| G33-3 | ✅ | `Database.backup_to`：WAL checkpoint(TRUNCATE)+shutil.copy2；备份库可重开且 count/history 一致 | `test_db_prune_backup.py` → Ran 8 OK（含 WAL 未检查点数据可备份用例） | repo_db 90% |
| G33-4 | ✅ | `run_git_ui` 取消/超时后 pump.join 超时 is_alive 判定 + 一次性警告；`thread_probe` 测试钩子 | `test_git_thread_exit.py` → Ran 3 OK；`-W error::ResourceWarning` 复跑 OK | service 80% |
| G33-5 | ✅ | sync_history 容量治理：每仓 200 条 + 本地时间 90 天窗口（`datetime('now','localtime','-90 days')`），每 100 次同步清理 | `test_prune_keeps_max_200_per_repo` / `test_prune_removes_only_old_window` / 计数器触发 通过 | repo_db 90% |
| G33-6 | ✅ | MainWindow 构造计时 `_startup_ms`；自检展示「启动耗时 ms」（>3s 提示便携版）+ `app.log selftest\|startup_ms=` 留痕 | `test_g33_startup.py` → Ran 3 OK | main_window 75% |
| G33-7 | ✅ | 注释漂移修正：settings.py concurrency `1..16`→`1..32`；main_window QTimer 行尾注释错挂修正 | 全局 `grep 1..16` 无残留；`ruff check gcm` All passed | — |
| 全量门禁 | ✅ | 379 测试全绿；覆盖率 81% 不降；ruff/mypy 全过；离屏冒烟 OK | `python -m unittest discover -s tests` → **Ran 379 tests OK**（自 360 净增 19）；`coverage report` → **TOTAL 81%**（3777 stmts） | 见上 |
| E2E 验收 | ✅ | 真实对象级核验：G33-1 灰标/only_new 过滤 → rows=1；G33-2 未勾覆盖 import=0/local_path 不变、勾选覆盖 import=1/local_path 更新；G33-3 不 close 备份→副本 count=2 可见 y；G33-5 清理后 199≤200 且 100 天前行删除；G33-4 `-W error::ResourceWarning` 3 用例 OK | 逐条实测输出见提交描述 | — |

**审查记录**：三个子代理分别执行（G33-1/2 导入对话框、G33-3/5 DB 层、G33-4 读线程），主控审查：
- local_repos_dialog import 排序（I001）经 ruff --fix 修复后 24 用例全绿
- thread_probe 签名向后兼容（默认 None，存量 10 处调用零改动）；`_kill_tree`/取消语义未动
- backup_to/prune 仅在既有方法上加调用点与新增方法，未破坏 `_migrate`/schema 与并发语义
- 二次导入覆盖行为已有回归测试锁定；统一由主控全量回归确认

## v7.0 补充登记（G33-8 main_window 拆分 + download_tools 测试）— 2026-09-15

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G33-8 | ✅ | main_window 1514→1300 行：settings_panel.py（230 行）/ log_buffer.py（68 行）/ download_tools.py（77 行）；控件名与 _log_batch/_log_batch_timer/_flush_log_batch 兼容引用全保留 | `wc -l gcm/ui/main_window.py` → 1300（≤1300 达标）；`test_manage_ui` 21 OK / `test_settings_scroll` 5 OK / `test_setting_ui` 4 OK / `test_extra` 31 OK / `test_ui` 6 OK / `test_download_tools` 10 OK | settings_panel 98% / log_buffer 90% / download_tools 81% |
| G33-8 测试回归修复 | ✅ | download_tools 经 main_window 模块引用 QFileDialog/QMessageBox（测试 mock 目标一致），修复拆分引入的弹窗阻塞回归 | `test_manage_ui` 由卡死→21 OK 0.96s；`test_download_tools.py` 新增 10 用例 | — |
| 全量门禁（终态） | ✅ | 389 测试全绿；TOTAL 82%（自 81% ↑）；ruff/mypy 全过；exe 版本资源 7.0.0 | `python -m unittest discover -s tests` → **Ran 389 tests OK**；`coverage report` → **TOTAL 82%**；`ruff check gcm` All passed；`mypy gcm` Success | 见上 |

**独立审查**：Critic 子代理两次停滞（0 输出 45min → 唤醒后 16:48 停更），主控代行独立审查完成：
- G33-1/2 导入灰标/覆盖语义：真实对象 E2E 通过（灰标+过滤 rows=1 / 覆盖后 local_path 更新）
- G33-3 backup_to：WAL checkpoint(TRUNCATE) + RLock 可重入，4 线程并发 add 零异常
- G33-5 prune：窗口函数 SQL 本地时区比较正确；并发 4×260 次 add 后 240 条（惰性节奏，符合预期）
- G33-4 读线程：thread_probe 向后兼容；`-W error::ResourceWarning` 3 用例 OK
- G33-8 拆分：12+11 控件名全保留、3 日志兼容引用 hasattr=True、QFileDialog/QMessageBox mock 目标一致
- 结论：**CONDITIONAL PASS**（条件=发布前重新构建 exe 含 G33-8 拆分代码，见发布闭环）

## v7.0.0 发布闭环 — 2026-09-15

- commit 链：`6acaf0a`(G33-1~7) → `503f310`(版本bump) → `e412503`(G33-8拆分) → `17f48aa`(验收补充) → main 推送成功
- tag `v7.0.0` 已推送：`git ls-remote --tags origin` 确认 17f48aab6f31271671a4096137f6e3e30d7a0232
- Release id=389006506 创建成功 · 双产物上传：
  - onefile exe 98,341,210 bytes（asset id=565340467）
  - portable zip 193,128,980 bytes（asset id=565341861）
- 服务端下载校验 OK：exe sha256 `ac178d8f…67e134` 一致
- 独立下载复验：MATCH=True（远端下载 exe 与本地 sha256 全等）
- exe 版本资源 7.0.0（UTF-16LE 定位，无 6.7.0 残留）；G33-8 新模块 settings_panel/log_buffer/download_tools 已打包（各 3 处标识）
- 新 exe 离屏启动冒烟：进程驻留 + repos.db 创建 OK
- Release 页面：https://github.com/lza6/Git-clone-Max/releases/tag/v7.0.0

**v7.0.0 最终门禁**：
- [x] `python -m unittest discover -s tests` → **Ran 389 tests OK**
- [x] `coverage report` → **TOTAL 82%**（自 v6.7 的 81% ↑）
- [x] `ruff check gcm` → All checks passed!
- [x] `mypy gcm --ignore-missing-imports` → Success（32 source files）
- [x] `main_window.py` = 1300 行（≤1300 达标，自 1514 ↓）
- [x] Release 双产物 + sha256 + 独立下载复验 MATCH

## v7.1.0 已交付（G35 核心交互进阶）— 2026-09-15

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G35-1 | ✅ | 进度表右键菜单：FAILED/CANCELLED 重试（_launch 单仓调度）+ 复制错误详情 + 打开所在目录 | `test_g35_ui.py` → 12 OK（含重试调 launch/busy 拒绝/复制详情） | main_window 71% |
| G35-2 | ✅ | 完成提示音 settings.finish_sound（默认开）+ _on_engine_finished beep + _save_finish_sound | `test_g352_finish_sound_on/off` 通过 | settings 78% |
| G35-3 | ✅ | 输入区拖拽：txt/csv → 行并入；目录 → LocalReposDialog 扫描导入 | `test_g353_drop_txt/drop_dir` 通过 | main_window |
| G35-4 | ✅ | 新 gcm/db/lists.py TaskLists：data/lists/*.json 保存/载入/删除/分享 | `test_db_lists.py` → 15 OK（往返/覆盖/文件名清洗/损坏/删除） | lists 97% |
| G35-5 | ✅ | 详情页复制克隆命令：git clone [-b ref] <url> | `test_repo_detail.py` → 8 OK（含 -b 断言） | repo_detail 96% |
| G35-6 | ✅ | ManageModel.host_counts/host_summary + manage_stats 拼接 | `test_manage_ui.py` → 25 OK（含 host 分布断言） | manage_model 74% |
| G35-7 | ✅ | 快捷填充 settings.quick_repos（tuple 持久化 + _coerce 扩展） | `test_g357_quick_repos_from_settings` 通过 | settings 78% |
| G35-8 | ✅ | 状态格 tooltip = message + detail 拼接 | `test_g358_status_tooltip` 通过 | main_window |
| G35-9 | ✅ | 批量打标签（QInputDialog）+ 导出所选 CSV（utf-8-sig） | `test_g359_batch_tag/export_selected` 通过 | main_window |
| G35-10 | ✅ | 新 gcm/app/gh_api.py：fetch_starred/parse_starred_payload/next_page_url（分页+上限+token） | `test_gh_api.py` → 16 OK（mock 分页两页/token/失败/上限） | gh_api 100% |
| 全量门禁 | ✅ | 440 测试全绿；覆盖率 81%；ruff/mypy 全过 | `python -m unittest discover -s tests` → **Ran 440 tests OK**（自 389 净增 51）；`coverage report` → TOTAL 81%（gh_api 100%/lists 97%/repo_detail 96%） | 见上 |

**v7.1.0 发布闭环**：
- commit `369bbd2` · tag `v7.1.0` 待推送
- 双产物：onefile exe 98,380,488 bytes sha256 `2133e273…3fcc` + portable zip 193,193,895 bytes sha256 `eb9df5f0…76a0bf`
- exe 版本资源 7.1.0；gh_api/lists 已打包；离屏启动冒烟 OK（repos.db 创建）
- Release 页面：https://github.com/lza6/Git-clone-Max/releases/tag/v7.1.0
- 独立下载复验：MATCH=True（远端下载 exe 与本地 sha256 全等 `2133e273…3fcc`）
- commit 链：`369bbd2`(G35) → `ff2976c`(验收登记) → tag `v7.1.0` 已推送（ls-remote 确认 ff2976c）

## v7.2.0 已交付（G36 UI/UX 升维）— 2026-09-15

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G36-1 | ✅ | 新 gcm/ui/onboarding.py：OnboardingDialog 三步向导（选目录/选模式/完成；skip 标志）；主控 first_run_done 持久化 + 目录/模式回填 | `test_onboarding.py` → 13 OK；`test_g369_manual_theme` 等回归 | settings 78% |
| G36-2 | ✅ | 状态列符号双通道（成功✓失败✕冲突⚠取消⊘跳过→）；UserRole 存原始值供统计/排序/右键 | `test_g362_status_symbols` / `test_g362_finished_stats` 通过 | main_window |
| G36-3 | ✅ | theme.py std_icon（QStyle.StandardPixmap 16 键）+ 主窗关键按钮 setIcon；emoji 回退 | `import gcm.ui.theme` OK；ruff 通过 | theme 83% |
| G36-4 | ✅ | settings.geometry + _serialize/_restore（含 M: 最大化前缀、损坏串安全） | `test_g364_geometry_*` 3 用例通过 | settings 78% |
| G36-5 | ✅ | 空态 overlay（下载/管理表 QLabel 绝对定位；有数据隐藏；resizeEvent 跟随；初始显示） | `test_g365_empty_overlay_toggles` / `test_g365_manage_empty_overlay` 通过 | main_window |
| G36-6 | ✅ | QVariantAnimation 背景 1.2s 淡出（成功绿/失败红）；settings.animations 开关；offscreen 自动关 | `test_g366_animation_sets_background` / `test_g366_no_animation_offscreen` 通过 | main_window |
| G36-7 | ✅ | 设置页搜索框 → 分组标题+行内标签匹配显隐（settings_groups） | `test_settings_search.py` → 6 OK（代理/keyword/fetch/无匹配/清空） | settings_panel 98% |
| G36-8 | ✅ | Ctrl+Alt+S/U/M 全局热键（start_cancel/update_all/show_window） | `test_g368_hotkey_start_cancel` / `test_g368_hotkey_show_window` 通过 | main_window |
| G36-9 | ✅ | 新 gcm/app/system_theme.py（detect/map）；theme=auto 启动自适应切换 light/deep | `test_follow_system_theme.py` → 13 OK；`test_g369_*` 3 用例通过 | system_theme |
| 全量门禁 | ✅ | 486 测试全绿；覆盖 81%；ruff/mypy 全过 | `python -m unittest discover -s tests` → **Ran 486 tests OK**（自 440 净增 46） | 见上 |

## v7.2.0 发布闭环 — 2026-09-16

- commit 链：`1838613`(G36) → `d05fc75`(验收登记) → tag `v7.2.0` 已推送（ls-remote 确认）
- Release 双产物：onefile exe 98,422,654 bytes（asset 566711623）+ portable zip 193,260,893 bytes（asset 566714064）
- exe sha256 `139fed45…5fead`；zip sha256 `7bef3bb0…fa253`
- 独立下载复验：MATCH=True（远端下载 exe 与本地 sha256 全等）
- exe 版本资源 7.2.0；onboarding/system_theme 已打包；无 7.1.0 残留；离屏冒烟 OK
- Release 页面：https://github.com/lza6/Git-clone-Max/releases/tag/v7.2.0

## v7.3.0 已交付（G37 数据能力纵深）— 2026-09-16

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G37-1 | ✅ | stats_daily（30 天三序列聚合，补零/钳制 1..90）+ 统计中心 QPainter 柱状趋势（_TrendCanvas） | `test_g37_stats.py` → 7 OK（补零/聚合/钳制/画布数据/用量标签） | repo_db / statistics_dialog |
| G37-2 | ✅ | 详情页「检查远端」按钮：后台线程 git ls-remote 对比 head_sha → 已最新/有更新/无法检查；Qt 信号回主线程 | `test_repo_detail.py` → 14 OK（含远程对比/失败路径） | repo_detail 96% |
| G37-3 | ✅ | reports._fetch_rows 增 filters（start/end/host/status 参数化）+ export_csv/markdown 可选 filters | `test_reports2.py` → 14 OK（全量=空筛/按主机/按状态/日期范围/无效） | reports 84% |
| G37-4 | ✅ | settings.auto_update_minutes（0=关）+ QTimer 自动更新（busy 跳过）+ 设置页 SpinBox + _restart_auto_update_timer | `test_g374_376.py` TestG374AutoUpdate → 6 OK（默认关/启停/间隔/tick/save） | main_window / settings |
| G37-5 | ✅ | stats_usage（克隆/更新/新增提交聚合）+ 统计中心「累计」用量标签 | `test_g37_stats.TestStatsUsage` → 2 OK（计数/空库）；对话框标签断言 | repo_db |
| G37-6 | ✅ | repos.note 迁移 0002（_SCHEMA_VERSION=2 幂等加列）+ set_note + 详情页备注编辑/保存 | `test_g374_376.TestG376Note` → 3 OK（持久化/迁移列/详情保存）；`test_db_migration` 全绿 | repo_db 90% |
| 全量门禁 | ✅ | 522 测试全绿；覆盖 81%；ruff/mypy 全过；迁移兼容（旧库升级不丢数据） | `python -m unittest discover -s tests` → **Ran 522 tests OK**（自 486 净增 36） | 见上 |

## v7.3.0 发布闭环 — 2026-09-16

- commit 链：G37 提交 → `cb49d58`(main) → tag `v7.3.0` 已推送（ls-remote 确认 cb49d58）
- Release 双产物上传成功：onefile exe 98,446,838 bytes（asset 566816518）+ portable zip 193,300,002 bytes（asset 566819634）
- 脚本 300s 兜底在「服务端校验」步骤超时（上传均已完成）；**独立下载复验 MATCH=True**（exe + zip 服务端与本地产物 sha256 全等）
- exe sha256 `da48071c…1430c`；zip sha256 `9cbe136d…046d7`
- exe 版本资源 7.3.0；Analysis toc 确认 G37 模块（statistics_dialog/repo_detail/reports）已收集；离屏启动冒烟 OK
- Release 页面：https://github.com/lza6/Git-clone-Max/releases/tag/v7.3.0


---

## v7.3.1 发布闭环 — 2026-09-18

| 项目 | 证据 |
|------|------|
| commits 推送 | `86010f7` feat(G38 全链路) · `43eca62` test(G38 专项+G37-2) · `55a773a` fix(G50 收尾) → origin/main push OK（普通 push 无 -f） |
| tag | `v7.3.1` annotated tag 已 push（ls-remote 确认 `6f14d59b…`） |
| Release | https://github.com/lza6/Git-clone-Max/releases/tag/v7.3.1 · published；body 含 sha256 行 |
| 双产物上传 | exe 99,859,537 B `e8aacc91…b2283` · zip 199,297,973 B `9993dcac…ccfd7`；均 `state=uploaded` |
| 独立下载复验 | exe download sha256 **MATCH=True**（20s 下载）· zip download sha256 **MATCH=True**（20.6min 下载） |
| 版本一致性 | `gcm.__version__ == 7.3.1` == tag == spec 版本资源 7.3.1 |
| 回归门禁 | 590 tests OK (944s) · TOTAL 83% ≥82 · ruff gcm 0 · mypy 通过 · G38 E2E 12/12 |

> 发布期间 GitHub 上传通道不稳定（HTTP 500/EOF/TLS 超时），改用 `curl --ssl-no-revoke` 分块直传 uploads.github.com 完成，服务端 digest 与本地 sha256 全等。
