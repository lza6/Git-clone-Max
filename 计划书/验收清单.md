# Git-clone-Max v6.0 升级《验收清单》

> 实施方（其他 AI）每完成一个里程碑，逐条登记证据。**只有真实运行过验证命令并给出输出摘要，才可标 ✅**。
> 覆盖率命令：`python -m coverage run --source=gcm -m unittest discover -s tests && python -m coverage report`

## v5.0 已交付（M2 · 多平台克隆，含 M1 部分）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G00-1 | ✅ | 版本 5.0.0 + changelog 补全 4.2–5.0 | `gcm.__init__.__version__ == '5.0.0'`；`python -m gcm` 冒烟 | — |
| G01-1 | ✅ | url_lib 通用多平台解析 + `is_git_url`；详情页路径剥离 | `python -m unittest tests.test_multi_url -v` → Ran 34 OK | url_lib 85% |
| G01-2 | ✅ | 跨 host 命名 `host__owner__repo`（GitHub 保持 `owner__repo`）| 同 test_multi_url；`gitlab.com__gitlab-org__gitlab` 正确 | — |
| G01-5 | ✅ | scanner.parse_remote_url 复用通用解析器，子组保留 | `gitlab.com/group/sub/repo` → url 保留分组前缀 | scanner 92% |
| G02-4 | ✅ | url 历史已入 v5 计划（recent_urls 待 M3 实施）| — | — |
| G06 安全 | ✅ | `@` 凭据前置拒绝 / `..` 段拒绝 / 子组 URL 保留分组 | test_multi_url.TestDetailPagesAndSecurity 全 OK | — |
| 全量门禁 | ✅ | 233 测试全绿；核心覆盖 85%；Release v5.0.0 已发布 | `python -m unittest discover -s tests` → Ran 233 OK；`coverage report` → TOTAL 85% | 见上 |

**审查记录**：独立 code-reviewer（BLOCK→修复）与 security-reviewer（3 HIGH→修复 2，1 跨层记录）结论已闭环：
- CRITICAL 旧 API 三元解包崩溃 → 已修复 `parse_repo_url`/`is_github_url`/`host_of`
- HIGH 详情页 /raw /releases /actions /wiki 错解析 → 动作段名单补全
- HIGH 子组折叠错仓库 → URL 重建保留分组前缀
- HIGH token 泄露面 → url_lib 层 `@` 拒绝；service 层按 host 白名单发 token 记录为跨层增强（M5 实施）
- MEDIUM scanner 目录名 `..` 归一化 → 记录为 M4 实施项

## v5.1 已交付（M1 · 可靠性加固 G06）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G06-1 | ✅ | token 加密落盘（DPAPI>Keyring>XOR 降级）+ 解密读回 | `tests/test_token_secure.py` 9 用例全绿；E2E 落盘 `enc:v1:…` 非明文、读回一致 | settings.py 78% |
| G06-2 | ✅ | single-instance 进程锁 + PID 崩溃残留清理 | `tests/test_single_instance.py` 4 用例（含真实跨进程 E2E 拦截）；打包 exe 双实例拦截 exit=0 | single_instance.py 59% |
| G06-3 | ✅ | 全局 excepthook → data/error.log（幂等） | `tests/test_exc_dump.py` 3 用例；E2E 人为 1/0 → error.log 含 ZeroDivisionError | exc_dump.py 78% |
| G06-8 | ✅ | settings .bak 备份 + 损坏自动恢复 | `TestSettingsBackupRestore` 3 用例全绿 | settings.py 78% |
| 全量门禁 | ✅ | 249 测试全绿；覆盖 83%；v5.1.0 已发布 | `discover -s tests` → Ran 249 OK；`coverage report` → TOTAL 83% | — |
| 修复 | ✅ | test_launcher bytes/str 兼容（coverage 下 flaky） | test_launcher 通过 | — |

**审查/审计记录**：security-reviewer 上一轮 HIGH-2（service 层 token 按 host 白名单注入）记录为 M5 跨层项；
本次 G06 已把 url_lib 层 `@` 拒绝 + settings 加密落盘落地，泄露面收窄。

**v5.1.0 发布闭环**：commit `2becf1f` · tag `v5.1.0` 已推送 · Release 附件 98,143,922 bytes · 独立下载 sha256 `b73f532b…42fec4` 与本地产物一致 · 下载产物真实启动冒烟 OK · token 加密/异常兜底 E2E 通过。

## v5.2 已交付（M1 剩余 G06-6 + M3 起点 G02-4）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G06-6 | ✅ | DB 迁移（PRAGMA user_version + 幂等加 tags/favorite/excluded 列，不丢数据） | `tests/test_db_migration.py` 4 用例全绿 | repo_db.py 89% |
| G02-4 | ✅ | URL 历史（data/history.json；去重/上限200/右键回填/清空；同步成功自动留档） | `tests/test_url_history.py` 6 用例全绿；UI 接入 E2E 写入成功 | history.py 100% |
| 全量门禁 | ✅ | 259 测试全绿；覆盖 82%；v5.2.0 已发布 | `discover -s tests` → Ran 259 OK；`coverage report` → TOTAL 82% | — |
| 打包验证 | ✅ | exe 启动冒烟 / 双实例拦截 | 143 正常挂起；第二实例 exit=0 | — |

**v5.2.0 发布闭环**：commit `4e073ae` · tag `v5.2.0` 已推送 · Release 附件 98,155,701 bytes · 独立下载 sha256 `c1b28636…e9fc69` 与本地产物一致。

## M1 · 可靠性加固（G06 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G06-4 | 待办 | 关闭优雅等待 worker | | |
| G06-5 | 待办 | 崩溃恢复自检（progress.in_progress） | | |
| G06-9 | 待办 | SQLite BEGIN IMMEDIATE | | |

## v5.3 已交付（M3 核心交互 G02-1/2/3）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G02-1 | ✅ | 进度表搜索过滤框（防抖 200ms，按仓库名过滤行） | `tests/test_progress_search.TestTableFilter` OK；UI 冒烟 filter=1 | main_window 82% |
| G02-2 | ✅ | 表头点击排序（状态列优先级运行>等待>成功>冲突>失败>取消>跳过） | 排序开启冒烟 sorting=True | main_window 82% |
| G02-3 | ✅ | 单仓库暂停（engine.pause_task + 每 worker 独占 service 取消回调） | `TestSinglePause` E2E：暂停的任务 CANCELLED、其余 SUCCESS | engine 89% |
| 全量门禁 | ✅ | 261 测试全绿；覆盖 81%；v5.3.0 已发布 | `discover -s tests` → Ran 261 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.3.0 | — | — |

**审查记录**：engine 改造点（per-worker service 取消回调）已用真实 2 任务 E2E 验证：暂停任务 CANCELLED、未暂停任务 SUCCESS，互不影响。DB 迁移新列 tags/favorite/excluded 已就绪（G03 标签/收藏/排除复用）。

**v5.3.0 发布闭环**：commit `2c2b434` · tag `v5.3.0` 已推送 · Release 附件 98,164,448 bytes · 独立下载 sha256 `9a94c3bb…78584` 与本地产物一致。

## M3 · 核心交互增强（G02 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G02-5 | 待办 | 失败项重试按钮 | | |
| G02-6 | 待办 | 解析预览 | | |
| G02-7 | 待办 | 任务清单保存/加载 | | |
| G02-1/2/3 | ✅ | 搜索/排序/暂停（见上） | | |
| G02-4 | ✅ | URL 历史（v5.2） | | |

## M2 · 多平台克隆（已完成，见上表）

## M3 · 核心交互增强（G02）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G02-1/2 | 待办 | 进度表搜索/排序 | | |
| G02-3 | 待办 | 单仓库暂停 | | |
| G02-4 | 待办 | URL 历史 | | |

## v5.4 已交付（M4 仓库管理进阶 G03）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G03-3 | ✅ | 标签（tags 列 + set_tags/list_by_tag 竖线分隔） | `tests/test_repo_tags.TestRepoTags` 全绿 | repo_db 89% |
| G03-4 | ✅ | 收藏（favorite + list_repos 收藏优先排序） | `TestRepoFavorite` 全绿 | repo_db 89% |
| G03-5 | ✅ | 黑名单（excluded + list_pending_updates，一键更新排除） | `TestRepoExcluded` 全绿；UI E2E pending 排除 skip | repo_db 89% / main_window 77% |
| G03-1/6 | ✅ | ManageModel 过滤（folder/owner/repo/host/tags）+ is_stale 过期高亮 | `TestManageModelAdvanced` 全绿 | manage_model 76% |
| 全量门禁 | ✅ | 268 测试全绿；覆盖 81%；v5.4.0 已发布 | `discover -s tests` → Ran 268 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.4.0 | — | — |

**v5.4.0 发布闭环**：commit `7724f8f` · tag `v5.4.0` 已推送 · Release 附件 98,169,356 bytes · 独立下载 sha256 `9d3c6de3…3bcb` 与本地产物一致。

## v5.5 已交付（M5 网络与同步 G04-3 + G08-1）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G04-3 | ✅ | 代理自动检测（HTTPS>HTTP 环境变量 → Windows 注册表 ProxyEnable/ProxyServer）+ 设置页「自动检测」一键填入 | `tests/test_proxy_detect.py` 4 用例全绿（含优先级测试）；UI E2E 填入本机 HTTPS_PROXY | proxy.py 75% |
| G08-1 | ✅ | 子模块克隆（settings.submodule → clone 追加 --recurse-submodules + update --init 兜底，失败仅警告） | `tests/test_submodule.py` 真实 git E2E：False 目录空 / True 内容完整 | service.py 78% |
| 全量门禁 | ✅ | 273 测试全绿；覆盖 81%；v5.5.0 已发布 | `discover -s tests` → Ran 273 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.5.0 | — | — |
| 审计修复 | ✅ | git clone 选项必须追加在末尾（insert(1) 会被当全局选项 → unknown option） | E2E 实测发现并修复 | — |

**v5.5.0 发布闭环**：commit `cf3ca8b` · tag `v5.5.0` 已推送 · Release 附件 98,179,367 bytes · 独立下载 sha256 `0665bf8f…9526` 与本地产物一致（服务端校验步骤超时，独立下载复验通过）。

## v5.6 已交付（M4 数据能力 G03-8 + G07-1）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G03-8 | ✅ | 详情统计区（次数/成功/失败/冲突/取消/平均耗时；parent.db 聚合 + history 兜底） | 详情页 E2E：`共 2 次同步 · 成功 1 · 失败 1 · 平均耗时 2.0s` | repo_detail 96% |
| G07-1 | ✅ | 统计中心对话框（全局聚合 + host 分布）+ 设置页「📊 统计中心」入口 | `tests/test_statistics_dialog.py` 2 例；UI E2E summary/hosts 正确 | statistics_dialog 96% |
| DB 聚合 | ✅ | `Database.stats_for_repo` / `stats_overview` 聚合查询 | `tests/test_stats.py` 3 例全绿 | repo_db 89% |
| 全量门禁 | ✅ | 278 测试全绿；覆盖 81%；v5.6.0 已发布 | `discover -s tests` → Ran 278 OK；`coverage report` → TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟 143 正常；dry-run tag v5.6.0 | — | — |

**v5.6.0 发布闭环**：commit `3368b90` · tag `v5.6.0` 已推送 · Release 附件 98,192,249 bytes · 服务端 sha256 `6e015b2a…b727cd` 校验一致 · 发布脚本 exit=0。

## v5.7 已交付（M6 体验 G05-1 多主题）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G05-1 | ✅ | PALETTES 三套色板（Deep/Light/Nord）+ THEMES 中文标签 + apply_theme/current_palette + settings.theme | `tests/test_themes.py` 9 例全绿（含 UI 下拉/切换/持久化 E2E） | theme.py 83% |
| UI 应用 | ✅ | 启动按 settings.theme 应用；下拉切换即时 setStyleSheet + 子对话框 | UI E2E：切 light → settings.theme=light → 重开恢复 | — |
| 覆盖计量 | ✅ | theme.py 从 omit 移除，56%→83%（补 LogModel/highlighter/maybe_validate_url 测试） | coverage report theme 83% | — |
| 全量门禁 | ✅ | 287 测试全绿；覆盖 81%；v5.7.0 已发布 | `discover -s tests` → Ran 287 OK；TOTAL 81% | — |
| 打包验证 | ✅ | exe 启动冒烟正常；dry-run tag v5.7.0 | — | — |

**v5.7.0 发布闭环**：commit `13c6db6` · tag `v5.7.0` 已推送 · Release 附件 98,197,394 bytes · 服务端 sha256 `58ac2e1d…7aeb` 校验一致 · 发布脚本 exit=0。

## v5.8 已交付（M6 自动化 G09-1 剪贴板监听）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G09-1 | ✅ | ClipboardWatcher（QTimer 轮询 + 正则提取 + parse_any_repo_url 验证 + 去重 + 静态资源过滤 + 回调异常吞掉） | `tests/test_clipboard.py` 10 例全绿（含 UI E2E：复制 URL → 自动填入输入框） | clipboard 88% |
| 设置开关 | ✅ | settings.clipboard_watch 持久化；勾选立即 start/stop | UI E2E 开启后 check_once 填入 URL | — |
| 全量门禁 | ✅ | 297 测试全绿；覆盖 80%；v5.8.0 已发布 | `discover -s tests` → Ran 297 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 启动冒烟正常；dry-run tag v5.8.0 | — | — |

**v5.8.0 发布闭环**：commit `11dcbc8` · tag `v5.8.0` 已推送 · Release 附件 98,207,861 bytes · 服务端 sha256 `791c6614…8073` 校验一致 · 发布脚本 exit=0。

## v5.9 已交付（M4 G07-3 报表导出）— 2026-09-10

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G07-3 | ✅ | gcm/reports.py：CSV utf-8-sig（Excel 友好）+ Markdown 表格 + 状态中文映射；repos JOIN sync_history 全量（含无历史仓库） | `tests/test_reports.py` 3 例全绿；UI E2E mock 对话框导出 CSV 133 bytes | reports.py 84% |
| 设置页入口 | ✅ | 「📄 导出报表」按钮（QFileDialog 选 CSV/MD） | UI 导出脚本 E2E 通过 | — |
| 全量门禁 | ✅ | 300 测试全绿；覆盖 80%；v5.9.0 已发布 | `discover -s tests` → Ran 300 OK；TOTAL 80% | — |
| 打包验证 | ✅ | exe 冒烟正常；dry-run tag v5.9.0 | — | — |

**v5.9.0 发布闭环**：commit `1e26567` · tag `v5.9.0` 已推送 · Release 附件 98,217,206 bytes · 服务端 sha256 `6bb83a5e…27dcf` 校验一致 · exit=0。

## M4 · 库存（已全交付：G03-3/4/5/8 + G07-1/3 ✅）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G03-7 | 待办 | 历史按钮委托（低优先，当前共享按钮可用） | | |

## M6 · 体验与工程化（G05/G09/G10 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G05-1 | ✅ | 多主题（v5.7） | | |
| G05-2 | 待办 | 托盘进度 tooltip | | |
| G09-1 | ✅ | 剪贴板监听（见上） | | |
| G10-1 | 待办 | 字号缩放 | | |

## M5 · 网络与性能（G04 + G08 剩余）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G04-1 | 待办 | 弱网 treeless 重试 | | |
| G04-4 | 待办 | 下载限速 | | |
| G08-2 | 待办 | 指定分支/标签 @tag | | |
| (跨层) | 待办 | service 分层注入 token（按 host 白名单） | | |
| G04-3 | ✅ | 代理自动检测（见上） | | |
| G08-1 | ✅ | 子模块克隆（见上） | | |

## M6 · 体验与工程化（G05 + G09 + G10）

| 条目 | 状态 | 实现要点 | 验证命令与输出摘要 | 覆盖率证据 |
|------|------|----------|--------------------|-----------|
| G05-1 | 待办 | 多主题 | | |
| G09-1 | 待办 | 剪贴板监听 | | |
| G10-1 | 待办 | 字号缩放 | | |

## 全量门禁（每个里程碑结束时）

- [x] `python -m unittest discover -s tests -v` → **Ran 233 tests OK**（v5.0）
- [x] `python -m coverage report` → **TOTAL 85%**（核心 url_lib 85% / engine 89% / scanner 92% / repo_db 89%）
- [x] `git status` 干净；无新增硬编码密钥
- [x] 发布闭环：commit `7e609b5` · tag `v5.0.0` 已推送 · Release 附件 98,116,377 bytes · 独立下载 sha256 `66c048dd…eb1fe9` 与本地产物一致 · 下载产物真实启动冒烟 OK

---

*门禁不通过不得宣称该里程碑完成。*