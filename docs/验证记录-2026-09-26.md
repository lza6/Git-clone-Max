# 验证记录（2026-09-26，v8.2.0）

> 用途：记录本轮已跑过的验证与已改动区域，避免后续重复跑相同验证、重复审查已确认项。
> 下次改动某区域时，优先看本记录「已改区域」是否命中 → 只重跑相关验证。

## 已跑验证（本机 .venv，Python 3.14.3 / PyQt6 6.11.0）

| 验证 | 命令 | 结果 | 耗时 |
|------|------|------|------|
| 全量测试（分块） | coverage run --source=gcm -m unittest <4批> | 660+ 用例全绿 | ~25min |
| 覆盖率 | coverage report --fail-under=84 | **TOTAL 84%** | — |
| ruff | ruff check gcm scripts | All checks passed | ~5s |
| mypy | mypy gcm | Success（59 files） | ~20s |
| 变异测试 | python scripts/run_mutation_smoke.py | 击杀率 95.24% PASS | ~5s |
| QSS 校验 | tests.test_qss_validate | 9/9 OK | ~1s |
| 内存基线 | python scripts/mem_baseline.py | 峰值 21.6KB OK | ~3s |
| 真实 E2E | tests.test_g52_engine_integration | 6/6 OK（本地裸仓） | ~25s |
| CLI --json 契约 | 空输入实测 | rc=2 + 完整 schema | ~1s |
| 构建 | PyInstaller Git-clone-Max.spec | exe+portable 成功 | ~6min |
| 健康检查 | health_check.py --json | 版本 8.2.0 一致/启动 OK | ~15s |

## 已改区域（v8.1.0 + v8.2.0，共 9 个主题提交）

### v8.1.0（M8/M9/M12核心/M13核心）
- **G51 文档**：计划书/db_structure.md、project_specs.md、下一步改进指南.md
- **G52 可靠性**：engine.py、main_window.py、settings.py、retry_queue.py、runtime_sessions.py、watchdog.py、progress_gc.py、failure_panel.py
- **G55 工程**：theme.py、qss_validate.py、run_mutation_smoke.py、mem_baseline.py、慢测定位.md
- **G56 生态**：cli.py、__main__.py、mcp_server.py、http_api.py、skills/*、agent-contract.md

### v8.2.0（M10/M11/M14/M15）
- **G53 数据**：repo_db.py（历史保留参数化）、alerts.py、sched_reports.py、statistics_dialog.py（评分卡/存储块）
- **G54 UX**：diag.py（环境体检）、help_dialog.py（排错地图）
- **G57 响应式**：main_window.py（窗口语义档/resizeEvent 防抖）、theme.py（DPI 联动）
- **G58 网络**：rate_limit.py、host_probe.py

## 安全审计结论（2026-09-26）
- SQL：repo_db.py 数据操作全部参数化；f-string execute 仅 PRAGMA 内部常量 → **无注入风险**
- redact：updater token 仅进 Authorization 头；url 为公开 Release 资产 → **无凭据泄漏**
- 并发：SQLite 引擎单线程写 + BEGIN IMMEDIATE + busy_timeout=30000 → **无锁死**

## 下次改动提示
- 改 engine/worker/线程 → 必跑 tests.test_engine + test_g43_engine + test_g52_engine_integration
- 改 settings 字段 → 必跑 test_settings_* + test_g53_reports（历史保留）
- 改 main_window 布局 → 必跑 test_g49_minimized + test_g52_mainwindow_* + test_g57_responsive
- 改 SQL → 必跑 test_db_prune_backup + test_db_migration + 参数化确认
